# Insert Tab — Completeness Ledger

> **Living ledger** for the Insert-tab completeness pass — par with **Microsoft Word for Windows 16.0** (ADR-0006).
> Built 2026-06-26 from: 20-agent repo-grounding + real-Word web-research sweep, plus owner real-Word computer-use captures (Cover Page, Page Number + Format dialog, Header gallery, Shapes gallery, Table menu, Date & Time, Drop Cap Options, Quick Parts, Object).
> **74 controls across 10 groups.** As Phase B lands fixes, flip the relevant rows to ✅ with the commit hash.

**Totals:** 65 AUTO-FIX · 30 NEEDS-USER · 27 false-docs claims · 22 missing/incomplete contextual tabs.

## Sections
1. Pages
2. Tables
3. Illustrations
4. Media
5. Links
6. Comments
7. Header & Footer
8. Text
9. Symbols
10. E-signature

---

## AUTO-FIX queue (Phase B order: severity → effort)

| # | sev | effort | section | gap | NO-FORK/fork | bridge verb |
|---|---|---|---|---|---|---|
| 1 | high | one-line | Tables | Fix false docs claiming Convert Text to Table works and 'every control implemented' | NO-FORK | n/a (doc edit; pairs with the Convert wiring fix) |
| 2 | high | one-line | Text | Correct false 'works' docs claims (Signature Line ✅, Text Box ✅, Drop Cap ✅, Quick Parts ✅, Date & Time ✅) | NO-FORK | n/a (docs only) — align INSERT_TAB.md:52-57 and FEATURES.md:28 with repo evidence + NOT_IMPLEMENTED.md |
| 3 | high | small | Tables | ✅ DONE (68344ce) — Wire Convert Text to Table menu item to the existing real bridge verb | NO-FORK | WC.PM.textToTable (bridge/table.ts:233, already real → editor.commands.convertTextToTable) |
| 4 | high | small | Illustrations | Fix false 'works/✅' docs for Shapes/SmartArt/Chart/Online Pictures/Stock Images | NO-FORK | none (docs only) |
| 5 | high | small | Links | Fix cross-ref Page-number wrong field (REF \p above/below) → PAGEREF | additive-fork-edit | WC.PM.refCrossReference({display:'pageNumber'}) — separate pageNumber from aboveBelow so it emits PAGEREF, not REF \p (crossref-wrappers.ts:229) |
| 6 | high | small | Text | ✅ DONE (af91ac7) — Date & Time 'Update automatically' checkbox ignored (always inserts a field, never static text) | NO-FORK | extend WC.PM.xeDateTime(fmt,{auto}) in bridge/insert-exotica.ts:118-123 — insertField when auto=true, plain insertText run when auto=false |
| 7 | high | small | E-signature | Complete the Signature Setup dialog (E-mail, Instructions+default, Allow-comments, Show-date) and stop discarding Signer/Title | NO-FORK | WC.Insert.signatureLine() passes all 6 fields to the new insertSignatureLine bridge verb |
| 8 | high | medium | Header & Footer | Format Page Numbers dialog + setPageNumberFormat (sectPr pgNumType) | NO-FORK | new setPageNumberFormat writing sectPr <w:pgNumType> fmt/start/chapStyle/chapSep (owned-sectPr write pattern like columns/line-numbers) |
| 9 | high | medium | Symbols | Symbol dialog: build the Special Characters tab (em/en dash, NB hyphen, optional hyphen, NB space, copyright/registered/trademark, ellipsis, quotes, zero-width breaks) with shortcut keys | NO-FORK | WC.PM.insertSymbol(ch) for plain Unicode chars; add WC.PM.insertSpecialChar('noBreakHyphen'|'softHyphen'|'nbsp'|'zwsp') in insert.ts for the structural ones (<w:noBreakHyphen/>, <w:softHyphen/>, xml:space=preserve space, U+200B/U+FEFF) |
| 10 | high | medium | E-signature | Insert a REAL signature-line VML object instead of the no-op toast | NO-FORK | replace WC.PM.xeSignatureLine() with insertSignatureLine({signer,title,email,instructions,allowComments,showDate}) emitting <w:pict>/<o:signatureline> via src/renderer/bridge/insert.ts |
| 11 | high | large | Text | Shape Format (Drawing Tools) contextual tab absent for text-box/shape/WordArt selection | NO-FORK | detect shapeTextbox/vectorShape NodeSelection in bridge/state-sync.ts:282-285; add shape-tools-pm.js (mirroring picture-tools-pm.js) + ribbon-data groups via scripts/gen.js; ribbon.js showContextualTab |
| 12 | med | one-line | Symbols | Wire the Alt+= shortcut to insert an equation (parity with captured Word behavior) | NO-FORK | WC.PM.insertEquation (src/renderer/bridge/insert.ts:177) via H.equation |
| 13 | med | small | Illustrations | ✅ DONE (52a27cc) — Chart: wire the already-computed Insert.chartSVG into insertImage so the dialog stops discarding data | NO-FORK | call existing Insert.chartSVG (insert-features.js:168) -> WC.PM.insertImage as an interim static-image chart |
| 14 | med | small | Links | Add cross-ref 'Insert as hyperlink' (\h) + 'Include above/below' (\p) + auto _Ref bookmark | NO-FORK | WC.PM.refCrossReference({hyperlink,aboveBelow}); WC.PM.insertBookmark for the auto _Ref######## anchor |
| 15 | med | small | Header & Footer | Header Remove + Footer Remove menu items | NO-FORK additive | setSlotText(kind,'') (bridge already clears bands) |
| 16 | med | small | Header & Footer | Current Position should insert a PAGE field at the body caret | NO-FORK | insertPageNumber({position:'current'}) extended to the live body selection |
| 17 | med | small | Header & Footer | Fix false 'works' docs claims | NO-FORK (docs) | n/a |
| 18 | med | small | Text | Drop Cap Options dialog (Font / Lines to drop / Distance from text) missing; lines hard-coded to 3 | NO-FORK | thread params into WC.PM.xeDropCap(kind,{lines,font,distance}) in bridge/insert-exotica.ts:50-66; add the dialog in dialogs.js + dropCapMenu Options item in commands.js:502-508 |
| 19 | med | small | Symbols | Symbol dialog: add a Font selector so Wingdings/Webdings glyphs round-trip | NO-FORK | extend WC.PM.insertSymbol(ch, {font}) (src/renderer/bridge/insert.ts:166) to emit a w:rFonts / legacy w:sym symbol-font run |
| 20 | med | small | Symbols | Symbol dialog: add Character-code box with from: Unicode(hex)/ASCII(dec/hex) | NO-FORK | reuse WC.PM.insertSymbol(ch) (src/renderer/bridge/insert.ts:166) |
| 21 | med | small | Symbols | Fix false 'works'/'fully implemented' docs claims for Equation and Symbol | NO-FORK | none (docs only) |
| 22 | med | small | E-signature | Wire the split dropdown into 2 distinct items (real MS Office Signature Line + honest provider-marketplace notice) | NO-FORK | Commands.dropdown -> WC.Insert menu; item1 runs the Signature Setup flow (insertSignatureLine), item2 = explicit stub |
| 23 | med | medium | Pages | Cover Page writes design-specific content instead of one shared 3-paragraph stub | NO-FORK | WC.PM.xeCoverPage(name) — build a design-specific node tree per name (parse each design's existing coverHTML via createDocFromHTML or richer node JSON) instead of the fixed title/[Subtitle]/[Author] paragraphs |
| 24 | med | medium | Tables | Add AutoFit-behavior radios + Remember-dimensions to the Insert Table dialog | NO-FORK | WC.PM.insertTable extended {autoFit:'fixed'|'contents'|'window', fixedWidth} → w:tblW / w:tblLayout |
| 25 | med | medium | Tables | Add AutoFit + Separate-text-at fields to the Convert Text to Table dialog | NO-FORK | WC.PM.textToTable(delimiter, {autoFit}) |
| 26 | med | medium | Tables | Make Quick Tables insert styled, pre-populated templates (add With Subheads + Calendar 4) | NO-FORK | WC.PM.insertTable + style/shading writes, or a canned w:tbl template via WC.PM.insertContent |
| 27 | med | medium | Tables | Add Table Style Options group (6 w:tblLook checkboxes) to Table Design | NO-FORK | new WC.PM.tableSetLook({firstRow,lastRow,firstColumn,lastColumn,noHBand,noVBand}) → w:tblPr/w:tblLook |
| 28 | med | medium | Tables | Expand Borders group: per-edge borders + Line Style/Weight/Pen Color + Borders & Shading dialog | NO-FORK | extend WC.PM.tableSetCellBorders for per-edge w:tcBorders/w:tblBorders (val/sz/color) |
| 29 | med | medium | Tables | Add Table Layout — Data: Sort + Repeat Header Rows | NO-FORK | new WC.PM.tableSort({col,type,order}) (reorder w:tr); WC.PM.tableSetHeaderRow → w:trPr/w:tblHeader |
| 30 | med | medium | Illustrations | Icons: raise a distinct Graphics Format contextual tab for SVG/icon nodes | NO-FORK | tag icon nodes (isSvg/data-wc-svg on insertImage, insert-exotica.ts:147) + branch in state-sync.ts:285; new graphics-tools-pm.js via ribbon.js showContextualTab |
| 31 | med | medium | Links | Add the 5 missing cross-ref reference types (Numbered item, Footnote, Endnote, Equation/Figure/Table) | NO-FORK | WC.PM.refCrossReference({type,display}) with a switch map (REF \r/\n/\w/\p, PAGEREF for page, NOTEREF for footnote/endnote) |
| 32 | med | medium | Links | Link dialog: add 'Place in This Document' (anchor link to heading/bookmark) | NO-FORK | WC.PM.insertLink({anchor}) (setLink already supports anchor) + listBookmarks + heading enumeration to fill the tree |
| 33 | med | medium | Header & Footer | Page Number Top/Bottom design sub-galleries | NO-FORK | insertPageNumber({position}) + run alignment/style |
| 34 | med | medium | Text | Quick Parts > Field limited to 6 hard-coded field types | NO-FORK | reuse WC.PM.xeQuickPart/insertField in bridge/insert-exotica.ts:124-131 with a categorized field list + per-field switches; rebuild fieldDialog in insert-features.js:239-243 |
| 35 | med | medium | Text | Draw Text Box has no rubber-band click-drag placement (alias of Simple) | NO-FORK (renderer interaction) | WC.PM.xeTextBox + a draw-mode placement layer that feeds the drag rect into a:xfrm; commands.js:499 |
| 36 | med | large | Tables | Add Table Properties dialog (Table/Row/Column/Cell/Alt-Text tabs) | NO-FORK | aggregate existing tableSetAlignment/tableSetIndent/tableSetCellMargins + new writes → w:tblW/w:jc/w:trHeight/w:cantSplit/w:vAlign/w:tcMar |
| 37 | med | large | Comments | Build modern comment thread lifecycle (reply/resolve/edit/delete + commentsExtended/Ids/Extensible) | NO-FORK | WC.PM.addReply / resolveComment(done) / editComment / deleteComment on review.ts + comments-ui.ts |
| 38 | med | large | Header & Footer | Header/Footer built-in gallery flyout (replace plain-text modal) | NO-FORK | setSlotText('header'/'footer', …) / insertContent into the materialized part |
| 39 | med | large | Header & Footer | Page Margins (margin text-box) page number | NO-FORK (reuse floating-object machinery) | new insertPageNumberMargin → floating textbox in header story holding a PAGE field |
| 40 | low | one-line | Links | Correct false docs: cross-reference marked fully working / \p defect omitted | NO-FORK | none (docs edit: docs/INSERT_TAB.md:38, docs/NOT_IMPLEMENTED.md:107) |
| 41 | low | one-line | Comments | Fix stale no-op comment at commands.js:430-433 | NO-FORK | none (comment text only — H.newComment chain already live) |
| 42 | low | one-line | Comments | Wire Ctrl+Alt+M shortcut to the comment command | NO-FORK | none (keybinding -> H.comment) |
| 43 | low | one-line | Header & Footer | Wire Edit Header/Footer to on-page caret entry | NO-FORK | enterHeaderFooter('header'/'footer') |
| 44 | low | one-line | Symbols | Make the Symbol quick-grid + dialog recently-used list persistent (localStorage, not sessionStorage) | NO-FORK | none (UI/storage only — dialogs.js:67 / insert-features.js:288 MRU) |
| 45 | low | one-line | E-signature | Fix false 'works' docs claim for Signature Line | NO-FORK | none — doc edit only |
| 46 | low | small | Pages | Expand cover-page gallery to Word's 14+ named built-in designs | NO-FORK | WC.PM.xeCoverPage(name) + Insert.coverPageMenu (add the Word-named designs as flyItems) |
| 47 | low | small | Pages | Add 'Save Selection to Cover Page Gallery' (in-session custom list) | NO-FORK (in-session list only) | new Insert.saveCoverSelection + dialog; reads current selection |
| 48 | low | small | Tables | Add Table Layout — Table group: Select submenu + View Gridlines | NO-FORK | new WC.PM.tableSelect('cell'|'column'|'row'|'table'); View Gridlines = non-printing UI toggle (no OOXML) |
| 49 | low | small | Tables | Add Insert Cells / Delete Cells shift dialogs to Rows & Columns | NO-FORK | extend insert/delete verbs with shift semantics (w:tr/w:tc add/remove) |
| 50 | low | small | Illustrations | Pictures: add Insert-and-Link / Link-to-File variant (writes a:blip r:link) | additive-fork-edit | WC.PM.insertImage with a link flag (insert.ts:45) emitting a:blip r:link instead of r:embed |
| 51 | low | small | Media | Parse pasted <iframe> embed code in the Online Video dialog (Word accepts URL OR iframe) | NO-FORK | onlineVideoDialog (insert-features.js:314) parse branch feeding insertVideoThumbnail -> WC.PM.insertImage; no new bridge verb required for the parse itself |
| 52 | low | small | Links | Link dialog: add E-mail Address (mailto:) category | NO-FORK | WC.PM.insertLink({href:'mailto:addr?subject=...'}) |
| 53 | low | small | Links | Link dialog: add ScreenTip (w:tooltip) | NO-FORK | WC.PM.insertLink({tooltip}) → setLink mark + converter honors w:hyperlink/@w:tooltip |
| 54 | low | small | Links | Bookmark dialog: add Sort-by Name/Location + 'Hidden bookmarks' filter | NO-FORK | none (dialog-side sort/filter over WC.PM.listBookmarks()) |
| 55 | low | small | Links | Link split-dropdown 'Recent Items' MRU is decorative — wire a real MRU or remove | NO-FORK | none (renderer-side MRU store feeding WC.PM.insertLink) — or drop the item |
| 56 | low | small | Comments | Allow insertion-point (bare-caret) comments like Word's point anchor | NO-FORK | WC.PM.addComment (review.ts addComment) — accept a collapsed/point anchor, emit matching commentRangeStart/End + commentReference |
| 57 | low | small | Text | WordArt gallery exposes only 6 of Word's 15 preset styles | additive-fork-edit (NO src fork — extend wordArtMenu data) | reuse WC.PM.xeWordArt with 9 added preset style descriptors; extend wordArtMenu in insert-features.js:204-216 |
| 58 | low | small | Text | Quick Parts > Document Property always inserts Title only (1 of 15) | NO-FORK | WC.PM.xeQuickPart('docproperty',{prop}) for all 15 properties as honest DOCPROPERTY fields (no content-control databinding yet); submenu in insert-features.js:234 |
| 59 | low | small | Text | Ctrl+F9 empty field-code braces { } not implemented | NO-FORK | new WC.PM.xeFieldBraces() inserting an empty complex field (begin/end fldChar); Ctrl+F9 keybinding in commands.js |
| 60 | low | small | Text | Object > Text from File: no Range/bookmark, no Insert-as-Link (INCLUDETEXT) | NO-FORK | extend Insert.textFromFile in insert-features.js:308-313; reuse the field path for an INCLUDETEXT field on Insert-as-Link |
| 61 | low | small | Symbols | Expand the Equation gallery to Word's named built-ins (Quadratic Formula, Pythagorean Theorem, Fourier Series, Binomial Theorem, etc.) and label honestly | NO-FORK | WC.PM.insertEquation(text) (src/renderer/bridge/insert.ts:177); also render the advertised gallery items honestly in equationMenu (commands.js:509) |
| 62 | low | medium | Pages | Add document-property placeholders / richer cover content that round-trips | additive — richer node JSON, NO-FORK | WC.PM.xeCoverPage (emit w:sdt document-property placeholders for Title/Author) |
| 63 | low | medium | Header & Footer | H&F tab Position group (Header-from-Top/Footer-from-Bottom/Alignment Tab) | NO-FORK additive | new setHeaderDistance/setFooterDistance (sectPr <w:pgMar w:header/footer>); ptab insert |
| 64 | low | medium | Header & Footer | H&F tab Insert group fields (Date & Time first, then Quick Parts/Doc Info) | NO-FORK | insert DATE/TIME/DocProperty fields via the active story doc.fields.insert |
| 65 | low | medium | Text | Text Box built-in gallery (~40 styled) + Draw Vertical Text Box missing | additive-fork-edit (menu data + xeTextBox preset props) | WC.PM.xeTextBox with preset shape props; thumbnail gallery replacing the 2-item textBoxMenu in commands.js:496-501 |

## NEEDS-USER queue (decision required — surfaced, not auto-built)

| # | section | item | reason | recommendation |
|---|---|---|---|---|
| 1 | Pages | More Cover Pages from Office.com | Requires an external online building-blocks backend (Office.com fetch); no local feasible implementation. | keep honest stub / leave omitted — do not add a no-op menu item |
| 2 | Pages | Persist custom cover pages to a real Building Blocks.dotx glossary | Round-tripping a real w:docPart (gallery=coverPg) into a building-blocks template glossary that survives restart and re-import is a new subsystem, larger than an in-session list. | build new subsystem only if prioritized; otherwise ship the in-session custom list (AUTO-FIX) and keep persistence as an honest gap |
| 3 | Pages | Blank Page net +1 vs Word's +2 pages | The +1-page behavior is a deliberate, documented design choice (insert.ts:240-244) to avoid the prior '2-by-2 pages' bug; changing it to match Word exactly is a scope/UX judgment, not a clear correctness bug. | confirm with user before changing — current behavior is intentional and arguably better UX |
| 4 | Tables | Draw Table freehand pen (irregular cells, dividers, Eraser, pens) | Current clone derives only a uniform grid from the drawn rectangle; full freehand cell/divider/merge editing with an Eraser and Border-Style/Weight/Pen-Color pens is a large interaction subsystem likely requiring fork-level per-cell editing (w:gridSpan/w:vMerge/w:tcBorders). Flag as SPIKE-FIRST. | spike-first to prove NO-FORK; build new subsystem only if the spike is bounded, otherwise keep the current uniform-grid drag-builder as an honest shallow control |
| 5 | Tables | Excel Spreadsheet OLE embedding | Word embeds a live OLE Excel worksheet (o:OLEObject ProgID Excel.Sheet.12, an xlsx package in word/embeddings + EMF preview) with in-place activation. No OLE/embed runtime exists; this is not a w:tbl and needs an external host. | keep honest stub (current toast already explains 'needs a host runtime') — do not build unless OLE embedding becomes a project goal |
| 6 | Tables | Quick Tables 'Save Selection to Quick Tables Gallery…' building-block store | Saving a selection to the gallery requires a persistent building-block store writing w:docPart entries to a glossary/Building Blocks.dotx — a new subsystem beyond inserting canned templates. | keep absent for now (the inline styled-template AUTO-FIX delivers most user value); build a building-block store only if a persistent gallery is prioritized |
| 7 | Illustrations | Shapes auto-shape engine (wps:sp insert + paint + round-trip) | Full gallery UI exists but Insert.insertShape (insert-features.js:118) is a toast and there is NO xeShape bridge verb; a real DrawingML auto-shape needs a new PM node type + PE paint of a:prstGeom presets + docx round-trip — likely large fork work. | SPIKE-FIRST then build new subsystem; until proven NO-FORK, make the gallery insert HONEST (clear 'not yet' message) rather than a full-looking gallery that drops everything |
| 8 | Illustrations | New Drawing Canvas (wpc:wpc container) | Requires a canvas container node grouping shapes/connectors; entirely depends on the Shapes auto-shape subsystem existing first. | build new subsystem (rides the Shapes engine) |
| 9 | Illustrations | Pictures: Stock Images + Online Pictures backends | Stock Images is absent from the picturesMenu flyout and Online Pictures is a silent toast; both need external licensed-content / Bing image-search backends with no in-clone equivalent. | keep honest stub — add a Stock Images entry that clearly states 'needs Office Content backend' and relabel Online Pictures from a fake insert to an honest message; decide with user whether to remove from the flyout entirely |
| 10 | Illustrations | SmartArt diagram engine (dgm: data/layout/colors/quickStyle) | xeSmartArt (insert-exotica.ts:179) toasts and discards the layout kind; a real SmartArt graphic needs a 4-part dgm: diagram subsystem plus SmartArt Design + Format contextual tabs — a large new subsystem. | build new subsystem OR keep honest stub / remove from ribbon until built (menu currently lists only 4 of Word's 8 categories and inserts nothing) |
| 11 | Illustrations | Chart engine (live c:chartSpace + embedded Excel workbook) | Beyond the interim chartSVG-as-image AUTO-FIX, a true Word chart embeds a c:chartSpace part plus an editable embedded .xlsx workbook and raises Chart Design + Format tabs — a new external/OLE-grade subsystem. | build new subsystem (live chart) — keep the interim static-SVG insert honest in the meantime |
| 12 | Illustrations | 3D Models viewer/runtime | H['3dModels'] is an honest toast; rendering/rotating fbx/obj/glb/gltf requires a 3D viewer runtime not present in the clone (ribbon-data feasible:'no'). | keep honest stub (already documented in NOT_IMPLEMENTED.md) or remove from ribbon |
| 13 | Illustrations | Illustration contextual tabs (Shape Format, SmartArt Design+Format, Chart Design+Format, 3D Model) | Each tab can only be raised once its parent object can be inserted/selected; all the parent objects above are missing or stub. | build each tab alongside its parent subsystem (Graphics Format is the one exception — buildable now as an AUTO-FIX since icon objects already exist) |
| 14 | Media | Make Online Video a faithful Word online-video (emit wp15:webVideoPr embed + external a:hlinkClick relationship; real provider poster + in-document playback) | In-document playback needs an external provider runtime / an unsandboxed embed surface that the app's CSP (img-src 'self' data: blob:) and sandbox currently block, plus fetching the real provider poster. The faithful OOXML emit (wp15:webVideoPr ext + External hlinkClick rel on the existing pic:pic) is an additive but real change to the super-converter image exporter and warrants a spec-kit plan and the user's call. | Build the OOXML-emit subsystem so the saved .docx round-trips as a real Word online video (poster picture + webVideoPr embed payload + external hyperlink), but keep the in-app object an honest static poster — document playback as a known sandbox limitation. Do NOT remove from ribbon; do NOT leave it silently degraded. |
| 15 | Links | Link 'Existing File or Web Page' file-browser (Look in / Browse for File / Recent Files side buttons) | The Address box already produces a correct external-relationship link, but the Look-in file browser and Browse-for-File side buttons need a native filesystem picker wired through main-process IPC — out of bridge scope and a design call on how far to go. | keep honest stub — ship the Address box + 'Place in This Document'/'E-mail' categories NO-FORK; defer the fs file-browser side buttons, or add a single 'Browse...' via the existing main-process file dialog if the user wants it. |
| 16 | Links | Link 'Create New Document' (create + open a brand-new file) | The hyperlink markup (external rel to a path) is NO-FORK, but actually creating the new .docx on disk and optionally opening it ('Edit the new document now') requires a filesystem/Office runtime action beyond the document-write bridge. | build new subsystem only if desired — otherwise keep the category honest by writing the path-link markup without performing the file create/open, or omit the category until the user prioritizes it. |
| 17 | Comments | @mention picker + Assign-to task assignment (cloud identity / people.xml backend) | Word's @mention people picker and task assignment require an external cloud-identity directory backend that does not exist in the offline clone; not an additive bridge edit. | keep honest stub (omit the @mention/Assign-to affordances from the composer card; do not render a non-functional people picker) |
| 18 | Header & Footer | Link to Previous + Previous/Next Section navigation | Requires a real multi-section header/footer-linking model (per-section headerReference inheritance/override); the clone is single-section, so this is a scope/design judgment, not bounded wiring. | keep honest stub (current toasts) until a multi-section model is on the roadmap |
| 19 | Header & Footer | Save Selection to Header/Footer Gallery (building-block persistence) | Needs a new building-block/glossary persistence subsystem (Create New Building Block dialog + word/glossary/document.xml docPart store) that does not exist anywhere in the app. | build new subsystem (long-tail) OR keep honest stub for v1 |
| 20 | Header & Footer | 'More Headers/Footers/Page Numbers from Office.com' | Requires an external online-gallery fetch runtime; not implementable offline and out of clone scope. | keep honest stub or remove from the menu (like the Home Dictate/Sensitivity/Add-ins removal) |
| 21 | Text | Signature Line is a toast-only stub behind a complete Signature Setup dialog | xeSignatureLine (insert-exotica.ts:181) only toasts and discards the dialog inputs. Inserting the visual o:signatureline shape is a NO-FORK slice, but the actual 'Sign' capability (digital ID/certificate, _xmlsignatures package part) needs an e-sign/signature-provider subsystem. | build new subsystem (or, near-term, build the honest insert-the-signature-line-shape slice and keep the Sign step a stub) — do NOT leave the current full-looking dialog over a pure toast |
| 22 | Text | Object > Create New / Create from File OLE embedding | OLE embed/link (o:OLEObject, /word/embeddings/*.bin, in-place server activation) requires an external OLE/Office embedding runtime that the clone has no path to. Menu is an honest inline toast today. | keep honest stub (or remove Create-New from the ribbon) — building a real OLE runtime is out of scope |
| 23 | Text | Quick Parts building-block store (Save-to-gallery, AutoText, Building Blocks Organizer) | All three depend on a glossary w:docPart building-block store (Building Blocks.dotx / Normal.dotm). No store exists, so there is nothing to save, browse, or organize — these are new-subsystem features, not wiring gaps. | build new subsystem (a building-block/glossary store) — until then keep AutoText and Building Blocks Organizer as honest stubs or drop them from the menu rather than dead ribbon items |
| 24 | Text | WordArt Text Fill / Outline / Effects / Transform | These live in the WordArt Styles group of the Shape Format contextual tab, which does not exist yet. Feasible NO-FORK once the Shape Format tab ships, but a design/scope judgment is needed on how much effects coverage (Transform presets, 3-D) to build. | build new subsystem — sequence after the Shape Format contextual tab; decide effect/transform coverage with the user |
| 25 | Symbols | Real OOXML equations (<m:oMath>/<m:oMathPara>) with an equation editor | Today's WC.PM.insertEquation inserts Cambria-Math-italic styled TEXT (explicit KNOWN DEVIATION at insert.ts:181). A faithful equation requires a NEW math subsystem: a math-zone node the caret can be inside, an OMML export/import path through the converter, and 2-D build-up (professional/linear) editing. This is a large, likely fork-gated feature and a scope decision. | build new subsystem (spec-kit, SPIKE-FIRST to confirm whether the SuperDoc fork already carries any OMML support before sizing the fork edit) |
| 26 | Symbols | Equation contextual tab (Tools / Symbols / Structures groups) | Word raises this tab whenever the caret enters a math zone (captured this session: Symbols + Structures groups). The clone has no selectable math object to detect and no math-symbol/structure galleries, and showContextualTab is wired only for Header&Footer/Table/Picture Tools. It is entirely dependent on the equation subsystem above. | build new subsystem (gated on the equation engine; defer until the math zone exists) |
| 27 | Symbols | Ink Equation (handwriting-to-math recognition) | Advertised in ribbon-data.js:925 but never rendered and no handler exists. A real implementation needs an ink canvas plus a handwriting-to-math recognition engine (external/ML runtime) — well beyond an additive bridge edit. | keep honest stub (render the item but show a 'not available' affordance, like the Home Dictate/Sensitivity treatment) until/unless a recognition backend is in scope |
| 28 | Symbols | Save Selection to Equation Gallery (Create New Building Block) | Advertised in ribbon-data.js:926 but never rendered, no handler, and there is no user equation gallery or Building-Blocks store to save into; it also presupposes a selectable equation object (which does not exist yet). | keep honest stub until a Quick-Parts/Building-Blocks store AND real equation objects exist; then revisit as a spec-kit feature |
| 29 | E-signature | Cryptographic Sign / View-Signatures pipeline (digital ID, XML-DSig/XAdES OPC parts, Final/read-only, Signatures task pane) | Requires a new signing subsystem: a digital ID, XML-DSig/XAdES signature generation, and OPC digital-signature parts (/_xmlsignatures/origin.sigs, sigN.xml, content-type override) plus a non-ribbon Signatures task pane. No document-body bridge verb covers this; it is an external/crypto runtime, not bounded NO-FORK body content. | keep honest stub — ship the placeholder signature-line OBJECT now (AUTO-FIX) and surface signing itself as an explicit honest stub; build the subsystem only if the user wants real signing |
| 30 | E-signature | Adobe Acrobat 'Esignature' add-in group / 'Get Signatures' (Adobe Acrobat Sign) | Not native Word — surfaces only with the Adobe add-in installed. Produces a PDF cloud agreement, NOT a .docx OOXML construct; requires an external Adobe runtime and an Office add-in manifest. No bridge verb applies. | remove from ribbon / do not add — not native Word fidelity; at most keep a single honest add-in-entry stub, mirroring the Home Dictate/Sensitivity/Add-ins honest-stub treatment |

## False "works" docs claims to correct

- **[Tables]** docs/INSERT_TAB.md:3 — blanket claim 'Every Insert-tab control is implemented and working. Verified by 27 dedicated cases…' is FALSE; Convert Text to Table and Excel Spreadsheet are toast-only stubs (insert-features.js:44, :83).
- **[Tables]** docs/INSERT_TAB.md:13 — '✅ live grid + Insert Table dialog + Convert Text to Table + right-click editing — Table' falsely lists Convert Text to Table as working; the Insert-menu item only shows WC.toast and never calls the real WC.PM.textToTable verb. Also omits that Excel Spreadsheet is a stub.
- **[Tables]** docs/FEATURES.md:28 — Insert row lists 'live-grid Tables' as working but does not flag that the Table dropdown's Convert Text to Table and Excel Spreadsheet sub-items are stubs (it lists only 3D Models/OLE Object/Online Pictures/Add-ins/video as stubs, omitting the two table stubs).
- **[Illustrations]** docs/INSERT_TAB.md:3 — 'Every Insert-tab control is implemented and working ... 86/86 total pass' is FALSE (Shapes/SmartArt/Chart/3D are toast stubs; scripts/test-suite.js is the retired legacy overlay suite, not the paged gate scripts/test-suite-pm.js).
- **[Illustrations]** docs/INSERT_TAB.md:18 — '✅ shape gallery -> inline SVG ... Shapes' is FALSE on the paged engine (Insert.insertShape, insert-features.js:118, is a WC.toast; no shape inserted).
- **[Illustrations]** docs/INSERT_TAB.md:21 — '✅ List/Process/Cycle/Hierarchy (editable) — SmartArt' is FALSE (xeSmartArt, insert-exotica.ts:179, is a toast no-op; nothing inserted/editable).
- **[Illustrations]** docs/INSERT_TAB.md:22 — '✅ Column/Bar/Line/Pie from entered data -> inline SVG — Chart' is FALSE (xeChart, insert-exotica.ts:178, is a toast no-op; dialog data discarded; Insert.chartSVG is dead code).
- **[Illustrations]** docs/INSERT_TAB.md:17 — '✅ This Device + Online Pictures — Pictures' is PARTLY FALSE (Online Pictures is a toast with no backend; Stock Images is missing from the menu; only This Device inserts).
- **[Illustrations]** docs/FEATURES.md:28 — Insert row claims 'Shapes/Icons/SmartArt/Chart as inline SVG' as working; FALSE for Shapes/SmartArt/Chart (toast stubs) — only Icons truly inserts. Move Shapes/SmartArt/Chart out of the working set (the same line already correctly lists 3D Models/OLE/Online Pictures as stubs).
- **[Links]** docs/INSERT_TAB.md:38 — '✅ to headings/bookmarks — Cross-reference' marks Cross-reference as fully working, but only 2 of ~7 reference types are exposed and Page-number emits the wrong field switch (REF \p = above/below, not PAGEREF); downgrade the green check to 🟡 shallow with the BUG-013 note.
- **[Links]** docs/NOT_IMPLEMENTED.md:107 — 'cross-refs via bookmarks and REF fields' is listed as partial (🟡) but omits the active wrong-output defect that Page-number cross-refs emit \p (above/below) instead of PAGEREF; add that caveat so readers don't assume page-number cross-refs work.
- **[Comments]** NONE in docs: docs/INSERT_TAB.md:42 marks Comment as working and that is ACCURATE. The only false 'no-op' claim is the STALE CODE COMMENT at src/renderer/public/js/commands.js:430-433 ('a guarded no-op until WC.CommentsUI lands') — WC.CommentsUI has landed (comments-ui.ts, installed index.ts:701) so the control works; fix the comment (task_55c214e5).
- **[Header & Footer]** docs/INSERT_TAB.md:46 — '✅ edit mode + built-ins + remove — Header' falsely claims a built-in gallery + Remove; code (H.header, commands.js:470) only opens a plain-text Edit Header modal — no gallery, no Remove item exist.
- **[Header & Footer]** docs/INSERT_TAB.md:47 — '✅ edit mode + page-number preset + remove — Footer' falsely claims a page-number preset + remove inside the Footer dropdown; H.footer (commands.js:471) opens only the plain-text Edit Footer modal — no footer gallery/preset/Remove.
- **[Header & Footer]** docs/INSERT_TAB.md:48 — '✅ top/bottom/current + format + auto-update — Page Number' falsely claims 'format' (Format Page Numbers) and Page Margins; the flyout (commands.js:474-480) renders only Top/Bottom/Current + Remove, no Format Page Numbers dialog and no Page Margins, and Current Position cannot target the body caret.
- **[Header & Footer]** docs/FEATURES.md:28 — Insert row lists 'Header/Footer/Page Number' as working without flagging that header/footer editing is plain-text-only (no galleries, no Remove) and Page Number lacks Page Margins + Format Page Numbers.
- **[Header & Footer]** docs/INSERT_TAB.md:67 — 'Header/Footer appear once on screen (not repeated per sheet)… True per-sheet repetition needs a full pagination engine.' STALE: the paged engine now paints per-page header/footer bands (header-footer.ts:295-296); the note predates the paged migration.
- **[Text]** docs/INSERT_TAB.md:56 — '✅ Signature Line' is false; bridge/insert-exotica.ts:181 xeSignatureLine() is a toast-only no-op that inserts nothing (contradicts docs/NOT_IMPLEMENTED.md:192 which correctly lists it as not implemented). Mark as not-implemented.
- **[Text]** docs/INSERT_TAB.md:52 — '✅ Text Box' overstates: working insert only; there is no built-in gallery and 'Draw Text Box' is a no-op alias (commands.js:496-501 both call H.textBox()). Qualify to insert-only.
- **[Text]** docs/INSERT_TAB.md:55 — '✅ None/Dropped/In Margin — Drop Cap' omits that 'Drop Cap Options...' (ribbon-data.js:873) is missing entirely and lines is hard-coded to 3 in xeDropCap. Note the Options branch is absent.
- **[Text]** docs/INSERT_TAB.md:53 / docs/FEATURES.md:28 — Quick Parts listed ✅ but only the 6 field shortcuts exist; Building Blocks Organizer, AutoText, and Ctrl+F9 are all missing. Qualify Quick Parts completeness.
- **[Text]** docs/INSERT_TAB.md:57 — '✅ format dialog — Date & Time' overstates: the 'Update automatically' checkbox is collected but ignored (xeDateTime always inserts a DATE field, never the static-text option). Note the checkbox is a no-op.
- **[Symbols]** docs/INSERT_TAB.md:3 — 'Every Insert-tab control is implemented and working. Verified by 27 dedicated cases…' is FALSE: Ink Equation, Save Selection to Equation Gallery, Alt+= professional/linear/convert, and the Symbol dialog's Special Characters tab are all missing/unwired.
- **[Symbols]** docs/INSERT_TAB.md:62 — '✅ gallery + insert — Equation' is MISLEADING: the gallery + dialog insert Cambria-Math-italic styled TEXT, not real OOXML <m:oMath>; Ink Equation, Save-to-Gallery, and Alt+= are unimplemented.
- **[Symbols]** docs/INSERT_TAB.md:63 — '✅ quick grid + full character map (subsets, recently used) — Symbol' OVERSTATES it: 6 fixed subsets, no Font selector, no code-point entry, and the Special Characters tab is entirely absent.
- **[Symbols]** docs/FEATURES.md:28 — lists 'Equation/Symbol' in the working set (only 3D Models/OLE/Online Pictures/Add-ins/video flagged as stubs); FALSE by omission — Equation's Ink/Save-to-Gallery/Alt+= and Symbol's Special Characters tab are stubs/missing but not flagged.
- **[E-signature]** docs/INSERT_TAB.md:56 — '- ✅ — **Signature Line**' marks Signature Line as working (green check), but the handler ends in xeSignatureLine() no-op toast (insert-exotica.ts:181). Change to a stub marker to match docs/NOT_IMPLEMENTED.md:192, docs/bug-hunt/FEATURE-IMPROVEMENTS.md:105, COMPLETENESS-BACKLOG.md:115, and BUG-LEDGER.md:564 which already classify it correctly.

## Missing / incomplete contextual tabs

- **[Tables]** Table Design — group MISSING: Table Style Options (6 w:tblLook checkboxes: Header Row/Total Row/Banded Rows/First Column/Last Column/Banded Columns)
- **[Tables]** Table Design — group INCOMPLETE: full Borders (per-edge Bottom/Top/Left/Right/All/Outside/Inside/Diagonal, Line Style/Line Weight/Pen Color pens, Border Painter, Borders and Shading… dialog) — clone only does All/No borders + 6 shading swatches
- **[Tables]** Table Layout — group MISSING: Table group (Select submenu Cell/Column/Row/Table, View Gridlines toggle, Properties → Table Properties dialog)
- **[Tables]** Table Layout — Draw group MISSING the Eraser (and freehand pen)
- **[Tables]** Table Layout — Data group MISSING: Sort dialog, Formula dialog, and Repeat Header Rows rendering (w:tblHeader)
- **[Tables]** Excel in-place / Graphics Format tab — Word raises Excel's own in-place UI (or generic Graphics Format when deactivated) for an embedded Excel object; not applicable to the clone until OLE embedding exists (NEEDS-USER, not a missing Table tab)
- **[Illustrations]** Shape Format (Insert Shapes / Shape Styles / WordArt Styles / Text / Arrange / Size) — needs a real shape object first (xeShape verb + shape node; rides the Shapes auto-shape engine)
- **[Illustrations]** Graphics Format (Change / Graphics Styles / Accessibility / Arrange / Size) — buildable NOW: icon objects already exist; needs an SVG/raster discriminator in state-sync.ts:285 so icons raise it instead of Picture Format
- **[Illustrations]** SmartArt Design (Create Graphic / Layouts / SmartArt Styles / Reset) — blocked on the SmartArt diagram engine
- **[Illustrations]** SmartArt Format (Shapes / Shape Styles / WordArt Styles / Arrange / Size) — blocked on the SmartArt diagram engine
- **[Illustrations]** Chart Design (Chart Layouts / Chart Styles / Data / Type) — blocked on the chart engine
- **[Illustrations]** Chart Format (Current Selection / Insert Shapes / Shape Styles / WordArt Styles / Arrange / Size) — blocked on the chart engine
- **[Illustrations]** 3D Model (Adjust / 3D Model Views / Accessibility / Arrange / Size) — blocked on the 3D viewer runtime
- **[Header & Footer]** Header & Footer tab — Insert group (Date & Time dialog → DATE/TIME field; Document Info → DocProperty fields; Quick Parts → Field / Building Blocks Organizer / AutoText; Pictures; Online Pictures) — currently the four long-tail buttons that exist are honest toasts (commands.js:408-411)
- **[Header & Footer]** Header & Footer tab — Position group (Header from Top spinner → sectPr w:pgMar w:header; Footer from Bottom spinner → w:pgMar w:footer; Insert Alignment Tab → w:ptab) — entirely absent
- **[Header & Footer]** Header & Footer tab — Navigation extras (Previous Section / Next Section, Link to Previous toggle) — present as honest single-section toasts, not real multi-section linking
- **[Text]** Drawing Tools / Shape Format (raised for any selected Text Box, Draw Text Box, or WordArt wps:wsp shape; needs groups: Insert Shapes, Shape Styles [Shape Fill/Outline/Effects + Format Shape], WordArt Styles [Text Fill/Outline/Effects incl. Transform], Text [Text Direction/Align Text/Create Link], Accessibility [Alt Text], Arrange [Position/Wrap/Bring Forward/Send Backward/Selection Pane/Align/Group/Rotate], Size [Height/Width/Layout launcher] — detect shapeTextbox/vectorShape in state-sync.ts:282-285 + a shape-tools-pm.js)
- **[Text]** WordArt Format / WordArt Styles (in modern Word this is the WordArt Styles group inside Shape Format, not a separate tab — covered by the Shape Format gap above)
- **[Symbols]** Equation (modern) / Equation Tools | Design — raised when the caret enters a math zone; needs a real math-zone object + state-sync equation detection to fire showContextualTab('equation'), plus the Tools group (mini gallery, Professional, Linear, Normal Text, Convert Unicode/LaTeX, Equation Options), the Symbols group (math-symbol mini-gallery with category subsets), and the Structures group (11 template dropdowns: Fraction, Script, Radical, Integral, Large Operator, Bracket, Function, Accent, Limit and Log, Operator, Matrix). Gated on the equation subsystem (NEEDS-USER).
- **[E-signature]** No signature RIBBON contextual tab is required — real Word raises NONE on insert (selecting the inline placeholder shows the standard Picture/Shape Format tab, which the clone already has via Picture Tools).
- **[E-signature]** Signatures task pane (non-ribbon panel; sections Requested Signatures / Valid Signatures) — MISSING; NEEDS-USER, gated on the signing subsystem; would need Sign / See Signature Details / Remove Signature / Signature Setup item actions.
- **[E-signature]** Adobe Acrobat Sign task pane / 'Acrobat' top-level tab — MISSING and non-native (add-in only); recommend not adding.

---

# Per-section detail

## Section 1 — Pages
*Real-Word group:* Insert ▸ **Pages** (the leftmost Insert group: Cover Page, Blank Page, Page Break) · *State:* Blank Page and Page Break are genuinely working real-document writes (deliberate `pageBreakBefore`-paragraph model); Cover Page is **shallow** — the flyout shows 6 rich design previews but every pick inserts the same 3-paragraph placeholder SDT, and the gallery is far smaller/poorer than Word's 14+ distinct building blocks. Docs are honest (no false "works" claims). No contextual tab is owed by any of the three (correct).

### Feature table
| feature | cmd id | file:line | state | gap vs real Word |
|---------|--------|-----------|-------|------------------|
| Cover Page | `coverPage` | `src/renderer/public/js/commands.js:437` | shallow | Inserts a plain 3-paragraph SDT (bold title / `[Subtitle]` / `[Author] — year`), discarding the menu's own rich `coverHTML` preview; menu has only 6 designs vs Word's 14+ distinct building blocks; omits *Save Selection to Cover Page Gallery* and *More Cover Pages from Office.com*; no document-property data-binding, no DrawingML design graphics. |
| Blank Page | `blankPage` | `src/renderer/public/js/commands.js:400` | working | Inserts ONE `pageBreakBefore` paragraph (net +1 page) instead of Word's two-break building block (net +2); deliberate per `insert.ts:240-244` to avoid the "2-by-2 pages" bug. Behaviorally faithful. |
| Page Break | `pageBreak` | `src/renderer/public/js/commands.js:399` | working | Modeled as a `pageBreakBefore` paragraph (caret-visibility fix per `docs/PAGE_BREAK_ROOT_CAUSE.md`) rather than an inline `<w:br w:type="page"/>` run; imported inline breaks are normalized to the same model. Ctrl+Enter wired. Faithful. |

### Flows

**Cover Page** (`xeCoverPage`/`xeRemoveCoverPage` in `bridge/insert-exotica.ts:69,103`; menu in `insert-features.js:24`)
- **Insert built-in cover page from gallery** — open dropdown → click a design; Word prepends a full cover sheet at page 1 (regardless of cursor), wrapped in `w:sdt > w:docPartObj > w:docPartGallery w:val="Cover Pages"`, with document-property content controls (Title/Subtitle/Author/Company/Date/Abstract via `w:dataBinding`) + DrawingML design shapes + terminating `w:sectPr`; no page break added; only one cover may exist. **(captured ✓)** Real Word this session showed 14+ DISTINCT designs (Austin, Banded, Facet, Filigree, Grid, Integral, Ion Dark/Light, Motion, Retrospect, Sideline, Slice, ViewMaster, Whisp…); inserting **Austin** produced a richly-formatted block (~36 words spanning onto page 2) with an **auto-filled Author** from the account name. **[exists? partially — flyout exists with only 6 designs (Banded, Facet, Filigree, Ion, Motion, Retrospect); / works? partially — it DOES write a real `documentPartObject` SDT at doc start (replaces any existing) and removes correctly, but ignores the per-design `coverHTML` and always inserts the same 3 paragraphs; / feasible-in-clone? YES, NO-FORK — `xeCoverPage(name)` already builds a node tree; make it design-specific by parsing each design's HTML via `createDocFromHTML` or richer node JSON, and expand the gallery to Word's named set]**
- **Remove Current Cover Page** — deletes the entire `docPartObj` sdt. **[exists? YES (flyItem at `insert-features.js:29` → `xeRemoveCoverPage`); / works? YES — finds the first `documentPartObject` and deletes its range, toasts "No cover page found" otherwise; / feasible-in-clone? already done]**
- **Save Selection to Cover Page Gallery…** — opens *Create New Building Block* (Name / Gallery=Cover Pages / Category / Description / Save in / Options); writes a `w:docPart` (`w:gallery w:val="coverPg"`) into the building-blocks template glossary; shortcut Alt+F3. **(captured ✓ — present in real Word's menu this session)** **[exists? NO; / works? NO; / feasible-in-clone? PARTIAL — capturing the selection into a JS-side custom-cover list + dialog is NO-FORK, but persisting a real Building Blocks.dotx glossary part round-tripping to OOXML is a larger lift; honest in-session custom gallery is the bounded win]**
- **More Cover Pages from Office.com** — legacy/online fetch. **[exists? NO; / works? NO; / feasible-in-clone? NO — external online backend; keep omitted / honest]**
- **Edit Properties / Organize and Delete** (right-click a gallery item → *Modify Building Block* dialog / Building Blocks Organizer). **[exists? NO; / works? NO; / feasible-in-clone? deferred — depends on a building-blocks store; not owed for v1]**

**Blank Page** (`insertBlankPage` in `bridge/insert.ts:240`)
- **Insert a blank page at the cursor** — Word inserts two run-level `w:br w:type="page"` to create a fully empty page between before/after content; KeyTip Alt, N, B; no chord; no dialog; no contextual tab. **(captured ✓ — confirmed working in real Word this session)** **[exists? YES; / works? YES — real PM transaction inserting a `pageBreakBefore` paragraph and moving the caret onto it; / feasible-in-clone? done, with a DELIBERATE +1 (not +2) deviation documented at `insert.ts:240-244`]**

**Page Break** (`insertPageBreak` in `bridge/insert.ts:233`)
- **Insert a manual page break** — pushes following content to the next page top; `w:p > w:r > w:br w:type="page"` (NOT a `w:sectPr`); shortcut **Ctrl+Enter**. **(captured ✓ — confirmed working in real Word this session)** **[exists? YES; / works? YES — real transaction; Ctrl+Enter wired at `commands.js:557,2410`; exports a genuine page break (as a `pageBreakBefore` paragraph; imported inline breaks normalized to match); / feasible-in-clone? done]**
- **View page breaks (Show/Hide ¶)** — Home ▸ Paragraph ▸ Show/Hide ¶ renders the "Page Break" mark; Ctrl+* (Ctrl+Shift+8); no OOXML change. **[exists? Home-tab concern, out of Pages scope; / works? n/a here; / feasible-in-clone? n/a — covered by the Home tab review]**
- **Delete a manual page break** — cursor before the mark + Delete reflows; removes the `w:br`. **[exists? YES (the break is a real paragraph, deletable via normal editing); / works? YES; / feasible-in-clone? done]**

### Contextual tabs
Real Word raises **NO dedicated contextual tab** for any Pages-group command. It can incidentally surface **Shape Format (Drawing Tools)** or **Picture Format (Picture Tools)** only when the user later selects a DrawingML shape / picture *inside* an inserted cover-page design — not from the insert action itself. Since the clone's inserted cover page is a plain 3-paragraph SDT with no embedded shapes/pictures, there is nothing to select, so no tab is owed. The clone correctly raises no Pages-group contextual tab (`ribbon.js:526`; state-sync only surfaces Table/Picture/Header-Footer Tools). **Missing tabs: none.** (If Cover Page is later upgraded to embed DrawingML graphics, selecting one should reuse the existing Picture/Shape contextual-tab machinery — already present for those object types.)

### Fix plan
| gap | NO-FORK / fork | spec-kit / direct-TDD | bridge verb(s) | regression test | oracle check | severity | autonomy |
|-----|----------------|------------------------|----------------|-----------------|--------------|----------|----------|
| Cover Page ignores per-design layout — every pick writes the same 3-paragraph stub | NO-FORK | spec-kit (gallery + per-design node builders) | `WC.PM.xeCoverPage(name)` (build design-specific node tree per name; richer placeholders) | `scripts/test-suite-pm.js`: insert "Banded" vs "Facet" → asserts distinct node content / styling, not identical 3 paras | export `.docx`, COM read-back: docPartObj present, design text distinguishable per name | med | AUTO-FIX |
| Cover gallery has 6 designs; Word has 14+ distinct named building blocks | NO-FORK | direct-TDD | `WC.PM.xeCoverPage(name)` + `Insert.coverPageMenu` (`insert-features.js:24`) | suite asserts the Word-named set (Banded, Facet, Filigree, Grid, Integral, Ion Dark/Light, Motion, Retrospect, Sideline, Slice, ViewMaster, Whisp, Austin…) renders as flyItems | COM: each inserts a valid `Cover Pages` docPartObj that Word opens without repair | low | AUTO-FIX |
| No document-property content controls / DrawingML in inserted cover | NO-FORK (richer node JSON) | spec-kit (folded into design-builder work) | `WC.PM.xeCoverPage` | suite: inserted cover exports `w:sdt` placeholders that round-trip | COM read-back: title/author placeholders survive open + re-save | low | AUTO-FIX |
| *Save Selection to Cover Page Gallery…* missing from flyout | NO-FORK for in-session list; building-blocks-store persistence is larger | direct-TDD (in-session) | new `Insert.saveCoverSelection` + dialog; `WC.PM` read selection | suite: save selection → custom entry appears in `coverPageMenu` and re-inserts | n/a (in-session) / COM only if persisted to glossary | low | AUTO-FIX (in-session list only; full Building Blocks.dotx persistence = NEEDS-USER) |
| *More Cover Pages from Office.com* | n/a | n/a | n/a — external online backend | n/a | n/a | low | NEEDS-USER (keep omitted / honest — no online backend) |
| Blank Page nets +1 page vs Word's +2 | NO-FORK | direct-TDD (only if reverting the deviation) | `WC.PM.insertBlankPage()` | existing page-break suite | COM page-count after insert | low | NEEDS-USER (deliberate documented design choice — confirm with user before changing) |

**Notes for the queue:** `falseDocsToFix` is **empty** — `docs/INSERT_TAB.md:7-9` mark all three done and `docs/NOT_IMPLEMENTED.md:227-229` correctly list them under "Partial / approximated," consistent with the shallow/working states above. `contextualTabsMissing` is **empty** — Word owes none for this group.


## Section 2 — Tables
*Real-Word group:* Insert ▸ **Tables** (single "Table" split button) · *State:* Core insertion is faithful (every working path funnels through the real `WC.PM.insertTable` → `editor.chain().insertTable()` producing a genuine `w:tbl`), and both contextual tabs (Table Design + Table Layout) are present and route ~30 `tbl*` commands to real bridge verbs. Two menu items are honest-degrade stubs (**Convert Text to Table** = no-op toast despite a real bridge verb already existing; **Excel Spreadsheet** = no OLE runtime), and Quick Tables / Draw Table are shallow (real but plain/uniform grids, not Word's styled templates / freehand pen).

### Feature table
| feature | cmd id | file:line | state | gap vs real Word |
|---|---|---|---|---|
| Table — Insert Table grid picker | table (insert.tables.table) | src/renderer/public/js/insert-features.js:47 | working | Grid is 8×10; Word offers 10×8 max in the picker (+ larger via dialog). Faithful `w:tbl`. Minor. |
| Table — Insert Table dialog | table (Insert Table… item) | src/renderer/public/js/dialogs.js:11 | working | No AutoFit-behavior radios (Fixed width / AutoFit contents / AutoFit window) and no "Remember dimensions for new tables". Geometry only. |
| Table — Draw Table | table (Draw Table item) | src/renderer/public/js/insert-features.js:64 | shallow | Derives only a uniform grid from the drawn rectangle size; no freehand cell/row/column dividers or irregular cells like Word's pen. |
| Table — Convert Text to Table… | table (Convert Text to Table item) | src/renderer/public/js/insert-features.js:44 | stub | No-op toast; a real `WC.PM.textToTable` verb exists (bridge/table.ts:233) but the menu item never calls it. Pure UI-wiring gap. |
| Table — Excel Spreadsheet | table (Excel Spreadsheet item) | src/renderer/public/js/insert-features.js:83 | stub | Toast only; Word embeds a live OLE Excel worksheet. No OLE/embed subsystem. Honest degrade. |
| Table — Quick Tables | table (Quick Tables item) | src/renderer/public/js/insert-features.js:88 | shallow | 4 entries insert real but empty plain grids of the right dimensions; Word inserts preformatted, pre-populated styled building-block templates. |
| Table Design contextual tab | table-design (tblStyles/tblShading/tblBorders/tblAlign*/tblIndent) | src/renderer/public/js/table-tools-pm.js:63 | working | Subset of Word's tab: a Table Styles list (no Table Style Options checkboxes / `w:tblLook`), 6 shading swatches, All/No borders, align + indent. Real but reduced. |
| Table Layout contextual tab | table-layout (tblInsert*/tblDelete*/tblMerge/tblSplit*/cell-size/valign/data) | src/renderer/public/js/table-tools-pm.js:22 | working | Broad parity for rows/cols/merge/split/size/align/data. Missing: Select submenu, View Gridlines, Properties dialog, Sort/Formula, Repeat Header Rows rendering. Verbs are real. |

### Flows

**Table — Grid picker (Insert Table grid)** — main flow.
- Live grid picker, hover highlights M×N + "N×M Table" caption, click commits. **[exists ✓ / works ✓ / feasible ✓]** — clone grid is 8×10 vs Word's 10×8 (captured ✓: real Word shows a 10×8 grid). OOXML: `w:tbl > w:tblPr(w:tblStyle="TableGrid", w:tblW, w:tblBorders, w:tblLook) > w:tblGrid/w:gridCol > w:tr/w:tc/w:tcPr/w:p`. Raises Table Design + Table Layout. Shortcut Alt, N, T.
  - Note: `H.table` *direct* click (commands.js:108) opens `WC.Dialogs.insertTable` instead of the grid; the grid is reached via `WC.Insert.tableMenu`. **[works ✓ / feasible ✓]**

**Table — Insert Table… dialog** — sub-flow (captured ✓).
- Opens the Insert Table dialog. **[exists ✓ / works ✓ / feasible ✓]** — clone has grid + numeric Columns/Rows (validated 1–1000) → `WC.PM.insertTable`.
- Dialog field: **Number of columns** (spinner, Word default 5) **[exists ✓ / works ✓]**
- Dialog field: **Number of rows** (spinner, Word default 2) **[exists ✓ / works ✓]**
- Dialog field: **AutoFit behavior — Fixed column width (Auto / measurement)** **[missing / feasible ✓ NO-FORK]** (captured ✓ in real Word's dialog)
- Dialog field: **AutoFit behavior — AutoFit to contents** **[missing / feasible ✓ NO-FORK]** (captured ✓) — OOXML `w:tblW w:type="auto"`
- Dialog field: **AutoFit behavior — AutoFit to window** **[missing / feasible ✓ NO-FORK]** (captured ✓) — OOXML `w:tblLayout w:type="autofit"` / `w:tblW pct 5000`
- Dialog field: **Remember dimensions for new tables (checkbox)** **[missing / feasible ✓ NO-FORK]** (captured ✓) — persist defaults
- OK / Cancel **[exists ✓]**. OOXML: Fixed → fixed dxa + `w:tblLayout="fixed"`. Shortcut Alt, N, T, I.

**Table — Draw Table** — sub-flow (captured ✓: "Draw Table" present in the real Table menu).
- Pen mode: drag outer boundary, then draw internal dividers freehand (non-uniform cells). **[exists (partial) / works (partial) / feasible: full pen = LARGE]** — clone has a real crosshair rubber-band that derives `cols=round(w/90), rows=round(h/36)` → a uniform real table (insert-features.js:64–82). It cannot draw individual dividers or merges.
- Raises Table Design + Table Layout (Draw group: Draw Table + Eraser; pens Line Style/Weight/Pen Color). **[missing eraser+pens / feasible: NEEDS-USER spike]**. OOXML: drawn merges → `w:gridSpan` (horizontal) / `w:vMerge` (vertical), per-edge `w:tcBorders`. Shortcut Alt, N, T, D.
  - (Distinct from the Home/Borders-menu "Draw Table" at commands.js:2237, which is `WC.notImplemented` — do not conflate.)

**Table — Convert Text to Table…** — sub-flow (captured ✓: item is ENABLED only when text is selected, matching Word).
- Opens Convert Text to Table dialog; splits selected paragraphs into rows and delimiters into columns → `w:tbl`. **[exists (menu item) / works ✗ — no-op toast / feasible ✓ NO-FORK, engine already supports it]**. A real `WC.PM.textToTable` (bridge/table.ts:233 → `editor.commands.convertTextToTable`, a genuine fork command) is wired but the menu item ignores it (insert-features.js:44).
- Dialog field: **Number of columns** **[missing / feasible ✓]**
- Dialog field: **Number of rows (derived from paragraph count)** **[missing / feasible ✓]**
- Dialog fields: **AutoFit — Fixed / AutoFit contents / AutoFit window** **[missing / feasible ✓ NO-FORK]**
- Dialog fields: **Separate text at — Paragraphs / Tabs / Commas / Other (single-char box)** **[missing / feasible ✓ NO-FORK — pass delimiter into textToTable]**
- OK / Cancel. OOXML: one `w:tr` per source paragraph, one `w:tc` per delimited field. Shortcut Alt, N, T, V. Raises Table Design + Table Layout.

**Table — Excel Spreadsheet** — sub-flow (captured ✓: item present, embeds an OLE Excel worksheet).
- Inserts a live embedded Excel worksheet (OLE in-place activation). **[exists (menu item) / works ✗ — toast / feasible: NEEDS-USER — no OLE/embed subsystem]**. OOXML: `w:object > o:OLEObject(ProgID Excel.Sheet.12, Type="Embed", r:id → /word/embeddings/oleObjectN.xlsx)` + EMF/PNG preview; the embedding is a full xlsx package. Does NOT raise a Table tab (deactivated → generic Graphics/Picture Format tab). Shortcut Alt, N, T, X.

**Table — Quick Tables (gallery submenu)** — sub-flow (captured ✓: real submenu = Calendar 1–3, Tabular List, Matrix, Double Table, With Subheads — preformatted STYLED templates).
- Submenu of pre-built building-block tables (gallery "Tables", category "Built-In"); hover previews, click inserts a formatted, pre-populated `w:tbl`. **[exists (4 entries) / works (partial) / feasible ✓ NO-FORK — author canned styled `w:tbl` templates]** — clone's 4 entries (Calendar / Tabular List / Matrix / Double Table) build real but EMPTY plain grids of the right size only.
- Gallery items (Word, captured ✓): **Calendar 1, Calendar 2, Calendar 3 (Word also has Calendar 4), Double Table, Matrix, Tabular List, With Subheads 1, With Subheads 2** **[missing styled templates: With Subheads + Calendar 4; existing 4 unstyled / feasible ✓]**
- Gallery item: **Save Selection to Quick Tables Gallery…** → Create New Building Block dialog (Name, Gallery=Tables, Category, Description, Save in, Options) **[missing / feasible: medium NO-FORK if a building-block store is added; otherwise keep absent]**
- OOXML: inserted content is `w:tbl` (style + grid + sample paragraphs); the gallery entries live as `w:docPart` in Building Blocks.dotx. Shortcut Alt, N, T, T. Raises Table Design + Table Layout.

**Table Design contextual tab** — raised when caret is in a table (injected by `syncContextualTabs`, table-tools-pm.js:84–97, from state-sync.ts:281). Word access key Alt, J, T.
- **Table Style Options group** (6 checkboxes: Header Row / Total Row / Banded Rows / First Column / Last Column / Banded Columns). **[missing in clone / feasible ✓ NO-FORK]** — OOXML: `w:tblPr > w:tblLook` bits (firstRow/lastRow/firstColumn/lastColumn/noHBand/noVBand).
- **Table Styles gallery** (Plain / Grid / List Tables, Accent 1–6 variants; + Modify / Clear / New Table Style…). **[exists (partial) — clone has a Table Styles LIST via `tblStyles`→`tableSetStyle/getTableStyles` / works ✓ for the subset / feasible: full gallery + Modify/New dialogs = medium]** — OOXML: `w:tblStyle w:val`, style defs in styles.xml with `w:tblStylePr` bands.
- **Borders — Shading** (split button: Theme/Standard palette, No Color, More Colors…). **[exists — `tblShading`→`tableSetCellShading`, 6 swatches / works ✓ / feasible: full palette NO-FORK]** — OOXML `w:tc/w:tcPr/w:shd`.
- **Borders — Border Styles / pens / Borders dropdown / Border Painter** (Line Style, Line Weight ¼–6pt, Pen Color, Borders edges Bottom/Top/Left/Right/All/Outside/Inside/Diagonal/None, Borders and Shading…, Border Painter). **[exists (partial) — `tblBorders`→`tableSetCellBorders` does All/No / works ✓ for All/No / feasible: per-edge + pens NO-FORK, Border Painter = medium]** — OOXML `w:tblBorders` / `w:tcBorders`.
- **Alignment group (clone-side):** `tblAlignLeft/Center/Right`→`tableSetAlignment`, `tblIndent`→`tableSetIndent` **[exists ✓ / works ✓]** (this is a clone grouping; Word puts table alignment in Table Layout ▸ Properties).

**Table Layout contextual tab** — raised alongside Table Design (table-tools-pm.js:22). Word access key Alt, J, L.
- **Table group** (Select dropdown: Cell/Column/Row/Table; View Gridlines toggle; Properties → Table Properties dialog). **[missing in clone / feasible ✓ NO-FORK for Select + Gridlines; Properties dialog = medium]** — Properties writes `w:tblW/w:jc/w:tblpPr/w:tblCellMar/w:tblInd`, `w:trHeight/w:cantSplit/w:tblHeader`, `w:tcW/w:vAlign/w:tcMar`, alt text.
- **Draw group** (Draw Table + Eraser). **[partial — Draw Table is the shallow drag-builder; Eraser missing / feasible: spike]** — OOXML `w:gridSpan/w:vMerge/w:tcBorders`.
- **Rows & Columns group** (Delete dropdown incl. Delete Cells…; Insert Above/Below/Left/Right; Insert Cells launcher). **[exists ✓ — `tblInsertAbove/Below/Left/Right`, `tblDeleteRow/Column/Table` / works ✓]** — Delete Cells… / Insert Cells shift-dialogs **[missing / feasible ✓ NO-FORK]**. OOXML: adds/removes `w:tr`/`w:gridCol`+`w:tc`.
- **Merge group** (Merge Cells; Split Cells… dialog; Split Table). **[exists ✓ — `tblMerge`, `tblSplitCell`, `tblSplitTable` / works ✓]** — Split Cells… dialog fields (cols, rows, merge-before-split) **[partial / feasible ✓]**. OOXML `w:gridSpan`/`w:vMerge`; Split Table inserts a `w:p`.
- **Cell Size group** (Height, Width, AutoFit dropdown, Distribute Rows, Distribute Columns). **[exists ✓ — `tblRowHeight/tblColWidth/tblAutoFit/tblDistRows/tblDistCols`, `tblCellMargins` / works ✓]**. OOXML `w:trHeight/w:tcW/w:tblGrid/w:tblLayout`.
- **Alignment group** (9 cell-content alignment buttons; Text Direction; Cell Margins). **[exists ✓ — `tblVAlign*`, `tblTextDir`, `tblCellMargins` / works ✓]** — Word has a full 9-button grid (vertical × horizontal); clone covers vAlign + text dir. OOXML `w:vAlign/w:textDirection/w:tcMar` + cell `w:pPr/w:jc`.
- **Data group** (Sort dialog; Repeat Header Rows toggle; Convert to Text dialog; Formula dialog). **[partial — `tblToText`→`tableToText('\t')` real ✓; `tblHeaderRow/tblHeaderCol` exist / Sort, Formula, Repeat-Header rendering MISSING / feasible: Sort + Repeat-Header NO-FORK, Formula = medium]**. OOXML: Sort reorders `w:tr`; Repeat Header → `w:trPr/w:tblHeader`; Convert to Text → `w:p`; Formula → `w:fldSimple w:instr="=SUM(ABOVE)"`.

### Contextual tabs
- **Table Design** — Word raises it whenever the caret is in a `w:tbl` (and during Draw Table). **Clone has it** (table-tools-pm.js:63, passive `activate:false` like Word). Present but reduced: clone groups are Table Styles (`tblStyles/tblShading/tblBorders`) + Alignment (`tblAlignLeft/Center/Right`, `tblIndent`). **Missing vs Word:** the **Table Style Options** group (6 `w:tblLook` checkboxes) and the full **Borders** group (per-edge borders, Line Style/Weight/Pen Color pens, Border Painter, Borders and Shading… dialog). All present handlers route to real `WC.PM` verbs — no stubs.
- **Table Layout** — Word raises it alongside Table Design. **Clone has it** (table-tools-pm.js:22) with Rows & Columns, Merge, Cell Size, Alignment, Data groups, all wired to real verbs. **Missing vs Word:** the **Table** group (Select submenu, View Gridlines, **Properties dialog**), the **Draw** group's **Eraser**, and Data-group **Sort / Formula** + **Repeat Header Rows** rendering.
- **Excel in-place / Graphics Format** — Word does NOT raise a Table tab for an embedded Excel object (it hosts Excel's own UI in-place, or shows the generic Picture/Graphics Format tab when deactivated). Not applicable to the clone until/unless OLE embedding exists (NEEDS-USER).

### Fix plan
| gap | NO-FORK / fork | spec-kit / direct-TDD | bridge verb(s) | regression test | oracle check | severity | autonomy |
|---|---|---|---|---|---|---|---|
| Convert Text to Table menu item is a no-op toast (real verb exists) | NO-FORK | direct-TDD | `WC.PM.textToTable` (bridge/table.ts:233, already real) — wire insert-features.js:44 → it; add the Convert Text to Table dialog (cols + Separate-text-at delimiter) | scripts/test-suite-pm.js: select delimited text, run menu item, assert a `w:tbl` with expected rows/cols | Word COM: select text → Convert to Table → `Selection.Tables(1).Rows.Count/Columns.Count` matches | high | AUTO-FIX |
| False docs: INSERT_TAB.md / FEATURES.md claim Convert Text to Table works + "every control implemented" | NO-FORK (docs) | direct-TDD | n/a (doc edit; pairs with the wiring fix above) | n/a | n/a | high | AUTO-FIX |
| Insert Table dialog missing AutoFit-behavior radios + Remember dimensions | NO-FORK | spec-kit (new feature) | `WC.PM.insertTable` extended w/ `{autoFit:'fixed'\|'contents'\|'window', fixedWidth}`; emit `w:tblW`/`w:tblLayout` | new probe: insert each AutoFit mode, assert exported `w:tblLayout`/`w:tblW` type | Word COM: `Table.AllowAutoFit` / `Table.PreferredWidthType` after each radio (captured ✓ radios) | med | AUTO-FIX |
| Convert Text to Table dialog missing AutoFit + Separate-text-at | NO-FORK | spec-kit (with the wiring fix) | `WC.PM.textToTable(delimiter, {autoFit})` | probe: tab/comma/other delimiters → expected column split | Word COM column counts per delimiter | med | AUTO-FIX |
| Quick Tables insert empty plain grids, not styled templates; missing With Subheads + Calendar 4 | NO-FORK | spec-kit | new `WC.PM.insertTable` + style/shading writes, or a canned `w:tbl` template inserter via `WC.PM.insertContent` | probe per template: assert table style + sample content cells | Word COM: insert each Quick Table, read `Table.Style` + cell text (captured ✓ template names) | med | AUTO-FIX |
| Table Design: Table Style Options (6 `w:tblLook` checkboxes) missing | NO-FORK | spec-kit | new `WC.PM.tableSetLook({firstRow,lastRow,firstColumn,lastColumn,noHBand,noVBand})` → `w:tblPr/w:tblLook` | probe: toggle each, assert `w:tblLook` bits in export | Word COM: `Table.ApplyStyleHeadingRows/.ApplyStyleLastRow` etc. | med | AUTO-FIX |
| Table Design: full Borders group (per-edge, Line Style/Weight/Pen Color, Border Painter, Borders & Shading dialog) | NO-FORK (per-edge/pens) | spec-kit | extend `tableSetCellBorders` for per-edge `w:tcBorders`/`w:tblBorders` (val/sz/color); Border Painter = medium | probe: apply each edge + weight/color, assert `w:tcBorders` children | Word COM: `Cell.Borders(wdBorderTop).LineStyle/LineWidth/Color` | med | AUTO-FIX |
| Table Layout: Table group — Select submenu + View Gridlines | NO-FORK | direct-TDD | new `WC.PM.tableSelect('cell'\|'column'\|'row'\|'table')`; View Gridlines = UI toggle (non-printing, no OOXML) | probe: assert selection spans expected cells | n/a (selection is non-OOXML) | low | AUTO-FIX |
| Table Layout: Table Properties dialog (Table/Row/Column/Cell/Alt-Text tabs) | NO-FORK (verbs exist) | spec-kit | reuse `tableSetAlignment/tableSetIndent/tableSetCellMargins/setImagePosition`-style writes → `w:tblW/w:jc/w:trHeight/w:cantSplit/w:vAlign/w:tcMar`; aggregate into one dialog | probe: set each tab field, assert exported pPr/trPr/tcPr | Word COM: `Table.PreferredWidth/.Rows.HeightRule/.Cell.VerticalAlignment` | med | AUTO-FIX |
| Table Layout: Data — Sort + Repeat Header Rows | NO-FORK | spec-kit | new `WC.PM.tableSort({col,type,order})` (reorder `w:tr`); `WC.PM.tableSetHeaderRow` → `w:trPr/w:tblHeader` | probe: sort asc/desc, assert row order; toggle header, assert `w:tblHeader` | Word COM: `Table.Sort` result order; `Row.HeadingFormat` | med | AUTO-FIX |
| Table Layout: Insert Cells / Delete Cells shift dialogs | NO-FORK | direct-TDD | extend insert/delete verbs with shift semantics (`w:tr`/`w:tc` add/remove) | probe: shift-left/up/row/column cases | Word COM cell counts after shift | low | AUTO-FIX |
| Draw Table = uniform grid only; no freehand dividers / Eraser / pens | likely fork (SPIKE-FIRST) | spec-kit after spike | would need per-cell divider/merge editing (`w:gridSpan`/`w:vMerge`/`w:tcBorders`) + pen state | spike probe: draw irregular cells, assert merges | Word COM: irregular `Cell.Width`/`gridSpan` | med | NEEDS-USER |
| Excel Spreadsheet = OLE embed (no subsystem) | fork + new subsystem | n/a until decided | none (no OLE write path) | n/a | n/a (OLE object, not `w:tbl`) | low | NEEDS-USER |
| Quick Tables "Save Selection to Quick Tables Gallery…" (building-block store) | NO-FORK but needs a building-block store | spec-kit | new persistent building-block store + `w:docPart` write to glossary | probe: save selection, re-insert | Word COM: `Templates.BuildingBlockEntries` | low | NEEDS-USER |


## Section 3 — Illustrations
*Real-Word group:* Insert ▸ **Illustrations** (Pictures, Shapes, Icons, SmartArt, Chart, 3D Models — plus Screenshot in real Word) · *State:* Only **Pictures (This Device)** and **Icons** actually mutate the document; **Shapes, SmartArt, Chart, 3D Models** are toast-only stubs, and **every** illustration contextual tab except **Picture Format** is missing. Docs claim "all working" — false for 4 of 6 controls.

### Feature table
| feature | cmd id | file:line | state | gap vs real Word |
|---|---|---|---|---|
| Pictures (This Device / Stock Images / Online Pictures) | `insert.illustrations.pictures` (cmd: pictures) | commands.js:392 (H.pictures); menu commands.js:490; dispatch commands.js:1846 | shallow | This Device inserts a real image; Stock Images is absent from the flyout and Online Pictures is a toast no-op (no image-search backend). |
| Shapes (auto-shapes gallery) | `insert.illustrations.shapes` (cmd: shapes) | insert-features.js:118 (insertShape); menu insert-features.js:104; dispatch commands.js:1847/438 | stub | Full categorized gallery UI, but every click is `WC.toast('isn't available on the new engine yet')` — no `WC.PM` verb, no DrawingML `wps:sp` inserted. |
| Icons (stock icon picker) | `insert.illustrations.icons` (cmd: icons) | insert-features.js:125 (iconsPicker) → insert-exotica.ts:142 (xeIcon) | working | Inserts the app's own Fluent SVG icons as a normal image via `insertImage` — not Word's licensed stock-icon library, and selecting it shows Picture Format, not a distinct Graphics Format tab. |
| SmartArt | `insert.illustrations.smartart` (cmd: smartart) | insert-features.js:150 (insertSmartArt) → insert-exotica.ts:179 (xeSmartArt) | stub | Menu offers 4 layouts but `xeSmartArt` is a pure toast (`return true` with no mutation); the `kind` arg is discarded. Word builds a real `dgm:` diagram. |
| Chart | `insert.illustrations.chart` (cmd: chart) | insert-features.js:155 (chartDialog) → insert-exotica.ts:178 (xeChart) | stub | Dialog gathers type + data, then `xeChart` toasts and discards it; `Insert.chartSVG` (line 168) is dead/uncalled. Word embeds a real `c:chartSpace` + Excel workbook. |
| 3D Models | `insert.illustrations.3d-models` (cmd: 3dModels) | commands.js:442 (H['3dModels']); dispatch commands.js:1861 | stub | Honest documented toast (`requires a 3D model viewer/runtime`); split items This Device / Stock 3D Models share no handler. ribbon-data marks feasible:'no'. |
| Shape Format contextual tab | (no cmd / no tab definition) | state-sync.ts:276-285 (only Table + Picture driven); no renderer definition | missing | Zero hits in renderer; nothing to select (Shapes never insert) so no tab can show. |
| SmartArt Design + SmartArt Format contextual tabs | (no cmd / no tab definition) | no renderer definition (grep `smartArtDesign` = 0) | missing | Entirely absent; no SmartArt object exists to trigger them. |
| Chart Design + Chart Format contextual tabs | (no cmd / no tab definition) | no renderer definition (grep `chartDesign` = 0) | missing | Entirely absent; no chart object exists to trigger them. |
| Graphics Format contextual tab (Icons/SVG) | (no cmd / no tab definition) | no renderer definition (grep `graphicsFormat` = 0); picture-tools-pm.js:25 fallback | missing | No distinct tab; an inserted icon is a plain image so it triggers Picture Format (state-sync.ts:285) instead of Graphics Format. |

### Flows

**Pictures** — split/menu button, 3 sources; inserted picture defaults to In-Line-With-Text and raises **Picture Format**. OOXML: `w:drawing > wp:inline (default) | wp:anchor > a:graphic > a:graphicData uri=.../picture > pic:pic > pic:blipFill/a:blip r:embed -> word/media/imageN.ext`.
- **This Device** — Insert Picture file-open dialog (location pane, file-name box, files-of-type filter, Insert split = Insert | Link to File | Insert and Link, Tools menu); KeyTips Alt,N,P,D. **[exists ✓ / works ✓ / feasible-in-clone ✓]** — `wordAPI.pickImage()` → `WC.PM.insertImage` (insert.ts:45 `setImage().run()`). Missing: the Insert-split Link-to-File / Insert-and-Link variants (always embeds). OOXML matches.
- **Stock Images** — Office Content picker (collection tabs Images/Icons/Cutout People/Stickers/Illustrations/Cartoon People, search box, category chips, multi-select grid, Insert); KeyTips Alt,N,P,S. OOXML: raster = same `pic:pic`; SVG = `a:blip + a:extLst/asvg:svgBlip` (PNG fallback). **[exists ✗ / works ✗ / feasible-in-clone — partial]** — not even in the flyout; would need a licensed Office Content backend (NEEDS-USER). A local-SVG subset is feasible but not Word-equivalent.
- **Online Pictures** — Bing-powered dialog (search box, suggested category tiles Animals/Business/Flowers/Nature/People/Places/Technology, Creative-Commons-only filter, OneDrive when signed in, size/license filter, Insert); KeyTips Alt,N,P,O. OOXML: `pic:pic/a:blip r:embed -> word/media` (web-linked variant uses `a:blip r:link`). **[exists — menu entry only / works ✗ / feasible-in-clone — needs backend]** — present in `picturesMenu` but is a `WC.toast` no-op; needs a Bing/web-image backend (NEEDS-USER).

**Shapes** *(captured ✓ this session)* — gallery dropdown; choosing a shape gives a crosshair cursor → click-drag draws a DrawingML auto-shape (`wps:sp`) and raises **Shape Format**; Shift constrains, Ctrl draws from center; New Drawing Canvas at the bottom; KeyTips Alt,N,S,H. Captured categories in real Word: **Recently Used Shapes, Lines, Rectangles, Basic Shapes, Block Arrows, Equation Shapes, Flowchart, Stars and Banners, Callouts + New Drawing Canvas.** Clone status for ALL of the below: **[exists ✓ gallery UI / works ✗ inserts nothing / feasible-in-clone — SPIKE-FIRST]** — `Insert.insertShape` (insert-features.js:118) is a toast; there is **no `xeShape` bridge verb**. OOXML per flow: `wps:wsp` (via `mc:AlternateContent/mc:Choice Requires="wps"`) with `a:prstGeom prst=<preset>`.
- **Recently Used Shapes** (captured ✓) — dynamic top row (Line/Arrow/Rectangle/Oval/Text Box). `prst` of each reused shape.
- **Lines** (captured ✓) — Line, Line Arrow, Line Arrow Double, Connector: Elbow/Elbow Arrow/Elbow Double-Arrow/Curved/Curved Arrow/Curved Double-Arrow, Curve, Freeform: Shape, Freeform: Scribble. OOXML `prst="line"/"straightConnector1"/"bentConnector3"/"curvedConnector3"`; freeform = `a:custGeom/a:path`.
- **Rectangles** (captured ✓) — Rectangle, Rounded Rectangle, Snip Single/Same-Side/Diagonal Corner, Snip-and-Round Single, Round Single/Same-Side/Diagonal Corner. `prst="rect"/"roundRect"/"snip1Rect"/"snip2SameRect"/"snip2DiagRect"/"round1Rect"`.
- **Basic Shapes** (captured ✓) — Oval, Right/Isosceles Triangle, Parallelogram, Trapezoid, Diamond, Pentagon, Hexagon…Dodecagon, Pie, Chord, Teardrop, Frame, Half/L-Shape, Diagonal Stripe, Cross, Cube, Bevel, Arc, Block Arc, Smiley, Heart, Lightning, Sun, Moon, Cloud, Brackets/Braces, Can, Donut, Folded Corner, Plaque. `prst="ellipse"/"triangle"/"diamond"/"hexagon"/"can"/"cube"/"smileyFace"/"heart"/"sun"/"moon"/"cloud"/"arc"/"bracketPair"/"bracePair"`.
- **Block Arrows** (captured ✓) — Right/Left/Up/Down, Left-Right, Up-Down, Quad, Bent, U-Turn, Left-Right-Up, Bent-Up, Curved variants, Striped/Notched Right, Pentagon, Chevron, the 6 Callout arrows, Circular. `prst="rightArrow"/"leftRightArrow"/"quadArrow"/"bentArrow"/"uturnArrow"/"homePlate"/"chevron"/"circularArrow"`.
- **Equation Shapes** (captured ✓) — Plus, Minus, Multiply, Divide, Equal, Not Equal (symbol shapes, not real equations). `prst="mathPlus"/"mathMinus"/"mathMultiply"/"mathDivide"/"mathEqual"/"mathNotEqual"`.
- **Flowchart** (captured ✓) — Process, Alternate Process, Decision, Data, Predefined Process, Internal Storage, Document/Multidocument, Terminator, Preparation, Manual Input/Operation, Connector, Off-page Connector, Card, Punched Tape, Summing Junction, Or, Collate, Sort, Extract, Merge, Stored Data, Delay, Sequential/Magnetic/Direct Access Storage, Display. `prst="flowChartProcess"/"flowChartDecision"/"flowChartInputOutput"/"flowChartTerminator"/"flowChartConnector"/"flowChartDocument"`.
- **Stars and Banners** (captured ✓) — Explosion 1/2, Star 4/5/6/7/8/10/12/16/24/32 Points, Up/Down/Curved-Up/Curved-Down Ribbon, Vertical/Horizontal Scroll, Wave, Double Wave. `prst="star5"/"star24"/"irregularSeal1"/"ribbon"/"ribbon2"/"verticalScroll"/"wave"/"doubleWave"`.
- **Callouts** (captured ✓) — Speech Bubble: Rectangle/Rounded Rectangle/Oval, Thought Bubble: Cloud, Line Callout 1/2/3 (+ Border/Accent-Bar/No-Border variants). `prst="wedgeRectCallout"/"wedgeRoundRectCallout"/"wedgeEllipseCallout"/"cloudCallout"/"borderCallout1"/"accentCallout1"`.
- **New Drawing Canvas** (captured ✓) — bottom command; inserts a bounded canvas grouping shapes/connectors; KeyTips Alt,N,S,H,C. OOXML `a:graphicData uri=.../wordprocessingCanvas > wpc:wpc` containing `wps:wsp/pic:pic` children. **[exists ✗ in clone]**.

**Icons** *(captured ✓ — opens an Office icon library dialog: searchable, categorized)* — real Word opens the Office Content picker on the Icons collection; vector SVG, recolorable; inserted icon raises **Graphics Format** (Change ▸ Convert to Shape). KeyTips Alt,N,F.
- **Insert Icon (picker)** — modal with search box + left category list (Accessibility, Animals, Apparel, Arrows, Arts, Body parts, Bugs, Business, Cartoons, Celebration, Education, Electronics, Emoji, Faces, Food and drinks, Furniture, Hobbies, Holidays, Home, Insects, Interface, Justice, Land vehicles, Maps, Medical, Music, Nature, People, Phones, Plants, Religion, Restaurant, Science, Security, Shapes, Signs and symbols, Sports, Technology, Tools and building, Transportation, Vehicles, VIP, Weather), multi-select grid, Insert. OOXML: `pic:pic with a:blip + a:extLst/asvg:svgBlip r:embed -> word/media/imageN.svg` (+ PNG fallback). **[exists ✓ / works ✓ — but app's own Fluent icons / feasible-in-clone ✓ as-is]** — `iconsPicker` → `xeIcon` → `insertImage`; genuine document write. Gaps vs Word: not the licensed library; categories are the clone's own Fluent set, not Word's 45 categories; selecting the result shows Picture Format, not Graphics Format; no Convert-to-Shape.

**SmartArt** *(captured ✓ — opens 'Choose a SmartArt Graphic' dialog: List/Process/Cycle/Hierarchy/Relationship/Matrix/Pyramid/Picture)* — clone status for ALL below: **[exists — partial menu UI / works ✗ / feasible-in-clone — NEEDS new diagram engine]** — `xeSmartArt` (insert-exotica.ts:179) toasts and inserts nothing; clone menu only lists List/Process/Cycle/Hierarchy (4 of the captured 8 categories).
- **Choose a SmartArt Graphic dialog** (captured ✓) — left category list All/List/Process/Cycle/Hierarchy/Relationship/Matrix/Pyramid/Picture/Office.com/Other; thumbnail grid; preview + description pane; OK/Cancel. KeyTips Alt,N,M. OOXML: `a:graphicData uri=.../diagram > dgm:relIds r:dm/r:lo/r:qs/r:cs` → four parts (data1.xml `dgm:dataModel`, layout1.xml `dgm:layoutDef`, colors1.xml, quickStyle1.xml) + drawing1.xml `dsp:drawing` fallback. Raises **SmartArt Design + Format**.
- **SmartArt Design tab** — Create Graphic (Add Shape After/Before/Above/Below/Assistant, Add Bullet, Text Pane, Promote/Demote, Move Up/Down, Right-to-Left, Layout), Layouts gallery + More Layouts, Change Colors, SmartArt Styles, Reset Graphic, Convert (to Text / to Shapes). OOXML edits update `data1.xml dgm:pt/dgm:cxn`. **[exists ✗]**.
- **SmartArt Format tab** — Edit in 2-D, Change Shape, Larger/Smaller; Shape Styles + Fill/Outline/Effects; WordArt Styles + Text Fill/Outline/Effects; Arrange (Position/Wrap/Align/Group/Rotate/Selection Pane); Height/Width. OOXML per-shape overrides in `drawing1.xml dsp:sp`. **[exists ✗]**.

**Chart** *(captured ✓ — opens 'Insert Chart' then an embedded Excel data sheet + Chart Design/Format tabs)* — clone status for ALL below: **[exists — dialog UI / works ✗ / feasible-in-clone — NEEDS new chart engine]** — `xeChart` (insert-exotica.ts:178) toasts; dialog data discarded; `chartSVG` is dead code.
- **Insert Chart dialog** (captured ✓) — tabs All Charts / Recommended; category list Recent, Templates, Column, Line, Pie, Bar, Area, X Y (Scatter), Map, Stock, Surface, Radar, Treemap, Sunburst, Histogram, Box & Whisker, Waterfall, Funnel, Combo; subtype strip; preview; OK/Cancel. KeyTips Alt,N,C. OOXML: `a:graphicData uri=.../chart > c:chart r:id -> word/charts/chart1.xml` + embedded `word/embeddings/*.xlsx` + colors/style parts. Raises **Chart Design + Format**.
- **Embedded data sheet** (captured ✓) — "Chart in Microsoft Word" Excel grid auto-opens; replace sample data, drag range handle; Edit Data reopens it. OOXML: embedded `.xlsx` workbook; `c:numRef/c:strRef` + `c:numCache/c:strCache`. **[exists ✗]** — needs an embedded-spreadsheet runtime.
- **Chart Design tab** — Add Chart Element (Axes/Axis Titles/Chart Title/Data Labels/Data Table/Error Bars/Gridlines/Legend/Lines/Trendline/Up-Down Bars), Quick Layout, Change Colors, Chart Styles, Switch Row/Column, Select Data, Edit Data, Refresh Data, Change Chart Type. **[exists ✗]**.
- **Chart Format tab** — Current Selection (element selector, Format Selection, Reset to Match Style), Insert Shapes, Shape Styles + Fill/Outline/Effects, WordArt Styles, Arrange, Height/Width. **[exists ✗]**.

**3D Models** *(captured ✓ — present in the group alongside Screenshot)* — split button This Device / Stock 3D Models; on-object 3D rotation control; raises **3D Model** tab; formats fbx/obj/3mf/ply/stl/glb/gltf. KeyTips Alt,N,3. Clone status: **[exists — toast only / works ✗ / feasible-in-clone — NEEDS 3D viewer runtime]** — `H['3dModels']` is an honest documented toast (no bridge verb); ribbon-data feasible:'no'.
- **This Device** — file dialog (3D Models filter `*.fbx;*.obj;*.3mf;*.ply;*.stl;*.glb;*.gltf`), rotation handle. KeyTips Alt,N,3,D. OOXML: `a:graphicData uri=.../picture with a:extLst/am3d:model3D` + 3D binary in word/media + 2D preview PNG. **[exists ✗]**.
- **Stock 3D Models** — online 3D content picker (category tiles Animals/Emoji/Space/Shapes, search, animated filter, Insert); animated models add Scenes/animation. KeyTips Alt,N,3,S. **[exists ✗]**.

> *Note:* real Word's Illustrations group also has **Screenshot** (screen/window capture → Screen Clipping). It is not in the repo control list for this group; treat as a separate NEEDS-USER capture subsystem if/when surfaced.

### Contextual tabs
Real Word raises **eight** contextual tabs across this group's objects:
- **Picture Format** (Adjust / Picture Styles / Accessibility / Arrange / Size) — for raster Pictures.
- **Graphics Format** (Change / Graphics Styles / Accessibility / Arrange / Size) — for SVG Icons/Illustrations/Stickers.
- **Shape Format** (Insert Shapes / Shape Styles / WordArt Styles / Text / Arrange / Size) — for Shapes/Text Box/Canvas/WordArt.
- **SmartArt Design** (Create Graphic / Layouts / SmartArt Styles / Reset) and **Format (SmartArt)** (Shapes / Shape Styles / WordArt Styles / Arrange / Size).
- **Chart Design** (Chart Layouts / Chart Styles / Data / Type) and **Format (Chart)** (Current Selection / Insert Shapes / Shape Styles / WordArt Styles / Arrange / Size).
- **3D Model** (Adjust / 3D Model Views / Accessibility / Arrange / Size).

**The clone has exactly one of these: Picture Format** (picture-tools-pm.js:25, driven by state-sync.ts:282-285 on a single-image NodeSelection). It also serves inserted Icons, since icons are materialized as plain image nodes — so it stands in for the missing Graphics Format.

**Missing tabs and what each needs:**
- **Shape Format** — blocked: needs a real shape object first (an `xeShape` bridge verb + a shape node in the model). Tab needs Insert Shapes, Shape Styles, WordArt Styles, Text, Arrange, Size groups. Build only after the Shapes engine spike.
- **Graphics Format** — could exist now: an inserted icon already exists as a selectable node. Needs an SVG-vs-raster discriminator in state-sync.ts so an SVG/icon node raises Graphics Format (Change/Graphics Styles/Accessibility/Arrange/Size) instead of Picture Format. AUTO-FIX candidate.
- **SmartArt Design / Format** — blocked on the SmartArt diagram engine.
- **Chart Design / Format** — blocked on the chart engine.
- **3D Model** — blocked on the 3D viewer runtime.

### Fix plan
| gap | NO-FORK / fork | spec-kit / direct-TDD | bridge verb(s) | regression test | oracle check | severity | autonomy |
|---|---|---|---|---|---|---|---|
| Pictures: Stock Images missing from the flyout; Online Pictures is a silent toast | NO-FORK (UI honesty) | direct-TDD | n/a (menu wiring in commands.js:490 `picturesMenu`) | test: flyout lists This Device + Stock Images + Online Pictures; Online Pictures shows a clear "needs backend" message, not a fake insert | n/a (UI) | med | NEEDS-USER (decide: add Stock Images entry as honest stub + relabel Online Pictures, or keep just This Device) |
| Pictures: Insert-split Link-to-File / Insert-and-Link variants absent | NO-FORK (additive) | direct-TDD | `WC.PM.insertImage` w/ a link flag (insert.ts:45) → `a:blip r:link` | test: Link-to-File path writes `r:link` not `r:embed` | COM read-back: `InlineShape.LinkFormat` present | low | AUTO-FIX (additive bridge arg) |
| Shapes: gallery inserts nothing (no `xeShape` verb) | SPIKE-FIRST — likely large fork work (PM node + PE paint of `wps:sp`) | spec-kit | new `WC.PM.xeShape(prst, geom)` in insert.ts → `wps:wsp/a:prstGeom prst` | test: insert prst="rect" → a shape node exists + survives round-trip | COM read-back: `Shape.Type=msoAutoShape`, `AutoShapeType` matches preset | high | NEEDS-USER (build new auto-shape subsystem — spike to prove NO-FORK first; until then keep gallery but make insert HONEST) |
| Shapes: New Drawing Canvas absent | (depends on Shapes engine) | spec-kit | `WC.PM.xeCanvas()` → `wpc:wpc` | test: canvas node + nested shape round-trips | COM: `Shape.Type=msoCanvas` | low | NEEDS-USER (rides the Shapes subsystem) |
| Icons: no distinct Graphics Format tab (icons fall back to Picture Format) | NO-FORK | direct-TDD | tag icon nodes (e.g. `data-wc-svg`/`isSvg` on `insertImage`, insert-exotica.ts:147) + branch in state-sync.ts:285 | test: select an inserted icon → Graphics Format tab shows; select a raster → Picture Format shows | n/a (clone-internal tab) | med | AUTO-FIX (object already exists; additive discriminator + tab def via showContextualTab) |
| Icons: not the licensed Office library / no Convert-to-Shape | external content + Shapes engine | spec-kit | n/a | n/a | n/a | low | NEEDS-USER (keep honest — clone's own Fluent set; Convert-to-Shape rides Shapes engine) |
| SmartArt: `xeSmartArt` toasts; `kind` discarded; menu only 4 of 8 categories | NO-FORK to make HONEST; full = new fork subsystem | spec-kit | n/a now (xeSmartArt insert-exotica.ts:179) | test: SmartArt menu either inserts a real `dgm:` diagram OR the control is removed/honest (no silent `return true`) | COM: `Shape.HasSmartArt`, `SmartArt.Layout` | high | NEEDS-USER (build SmartArt diagram engine — large; recommend honest stub or remove from ribbon until built) |
| Chart: `xeChart` toasts; dialog data discarded; `chartSVG` dead code | NO-FORK partial (wire `chartSVG`→image) vs full chart engine (fork) | spec-kit | wire existing `Insert.chartSVG` (insert-features.js:168) → `WC.PM.insertImage` as an interim static-image chart | test: chart dialog data → an SVG image actually inserted (interim), full path → `c:chartSpace` | COM: `Shape.HasChart`, `Chart.ChartType` | high | AUTO-FIX (interim: wire the already-computed `chartSVG` into `insertImage` so the dialog stops discarding data) **+** NEEDS-USER (live `c:chartSpace` + embedded Excel = new subsystem) |
| 3D Models: toast stub | NEEDS external 3D runtime | n/a | n/a | n/a | n/a | low | NEEDS-USER (keep honest stub — already documented; or remove from ribbon) |
| Contextual tabs Shape Format / SmartArt Design+Format / Chart Design+Format / 3D Model all missing | (each blocked on its object's engine) | spec-kit | ribbon.js `showContextualTab` + new `*-tools-pm.js` modules + state-sync.ts driver | per-tab: selecting the object raises the tab | COM read-back of the selected object | med | NEEDS-USER (each rides its parent subsystem above) |
| False docs: INSERT_TAB.md:3/17/18/21/22 + FEATURES.md:28 claim Shapes/SmartArt/Chart (and Online Pictures/Stock Images) "✅ working" | NO-FORK (docs) | direct-TDD | n/a | n/a (doc correction) | n/a | high | AUTO-FIX (correct the stale ✅/works claims to match the toast-stub reality; NOT_IMPLEMENTED.md is already honest) |


## Section 4 — Media
*Real-Word group:* Insert tab → **Media** group (a single control, "Online Videos", in current Word for Windows 16.0). · *State:* The one control exists and genuinely writes the document, but degrades to a static SVG poster image rather than Word's real playable web-video embed — **shallow**, no false docs, Picture Tools correctly raised.

### Feature table
| feature | cmd id | file:line | state | gap vs real Word |
|---|---|---|---|---|
| Online Video | `onlineVideo` | src/renderer/public/js/commands.js:445 | shallow | Word inserts a poster picture carrying a `wp15:webVideoPr` embed + external hyperlink so it plays in-document; clone inserts only a self-contained SVG thumbnail image (no playback, no embed payload, no external hyperlink). |

### Flows

**Online Video** (`onlineVideo` → `H.onlineVideo` commands.js:445 → `WC.Insert.onlineVideoDialog()` insert-features.js:314 → `insertVideoThumbnail(u)` insert-features.js:326 → `WC.Commands.insertPictureFromDataUrl` commands.js:375 → `WC.PM.insertImage` bridge/insert.ts:45)

- **Main action — open "Online Videos" button** (real-Word `idMso='InsertVideoOnline'`, single push button, not a split/gallery). Opens the modal "Enter the address for the online video" dialog. **[exists ✓ / works ✓ / feasible ✓]** — clone opens `onlineVideoDialog` with a single URL field, runs the value through `WC.safeUrl`.
  - Shortcut: **Alt, N, then the Online Videos KeyTip** (Word assigns no dedicated Ctrl/Fn chord). **[exists? KeyTip path not modeled in clone / works? n/a / feasible ✓]**

- **Sub-flow — Insert an online video by URL (captured ✓ this session)**: real Word's "Insert a Video" dialog has a single URL field; pasting a supported watch URL (YouTube/Vimeo/SlideShare/TED/SharePoint/OneDrive-for-Business) embeds a real web-video object with a **poster frame**, and the OOXML is `wp:inline` → `a:graphicFrame`/`pic:pic` with the **web-video ext** (`wp15:webVideoPr`). Dialog fields: **Address field** (single-line text box; accepts a watch URL OR a pasted `<iframe>` snippet), **Insert** (disabled until non-empty / recognized URL), **Cancel**.
  - **[exists? partial — clone has the Address field + Insert/Cancel, but no recognized-URL gating beyond `safeUrl` / works? shallow — it commits a placeholder image, not an embed / feasible-in-clone? partial]**
  - **OOXML (real Word):** `w:p > w:r > w:drawing > wp:inline` (with `wp:extent`, `wp:docPr` carrying `a:hlinkClick r:id` → External video-URL relationship, `wp:cNvGraphicFramePr`) `> a:graphic > a:graphicData(uri=picture) > pic:pic`; `pic:blipFill > a:blip r:embed='rIdPoster'`; the web-video payload lives in `a:blip > a:extLst > a:ext`(web-video GUID)`> wp15:webVideoPr embeddedHtml='<iframe…>' h=… w=…` (ns `wp15 = …/2012/wordprocessingDrawing`). Round-trip parts: `word/document.xml`, `word/_rels/document.xml.rels` (External rel to video URL + Image rel to poster), `word/media/*` (poster).
  - **Clone reality:** `bridge/insert.ts:45` runs a real `editor.chain().setImage({src,alt,rId,size}).run()` with a valid bridge `rId` and a 50MB guard, so it writes a true inline `pic:pic` image — but `src` is a generated SVG data-URL poster and the URL is only preserved as `alt='Online video: '+u`. No `wp15:webVideoPr` ext and no external `a:hlinkClick` relationship are emitted. CSP `img-src 'self' data: blob:` blocks remote provider posters; the app sandbox blocks embedded playback.

- **Sub-flow — Insert an online video by embed code**: same dialog, user pastes a full `<iframe>…</iframe>` snippet (provider Share → Embed); Word extracts src + dimensions, stores the raw iframe in `wp15:webVideoPr/@embeddedHtml`, derives a poster. Identical OOXML structure to the URL flow (hlinkClick targets the iframe src). **[exists? no — clone's single field treats input as a plain URL via `safeUrl`, does not parse a pasted iframe / works? no / feasible ✓ — additive URL/iframe parse]**

- **Sub-flow — Play the inserted video**: click the Play glyph (or click the thumbnail in Read Mode/Web Layout while online); Word swaps the poster for an in-document playback surface rendering the persisted `wp15:webVideoPr/@embeddedHtml`. Runtime behavior, no document mutation; requires internet + provider-permitted embedding. **[exists? no / works? no — clone's poster is a static image with no playback affordance / feasible-in-clone? NO — needs an external runtime / unsandboxed embed surface (NEEDS-USER)]**
  - **OOXML:** none on playback (the persisted `webVideoPr/@embeddedHtml` drives the rendered surface).

- **Post-insert — object behaves like a picture**: the inserted object is a real `image` NodeSelection; it is selectable/movable/deletable and selecting it surfaces Picture Tools (`state-sync.ts:284`). **[exists ✓ / works ✓ / feasible ✓]** — matches Word, which also exposes only Picture Format (no Video Tools tab) for this object.

### Contextual tabs
- **Real Word raises:** **Picture Format** (tabSet "Picture Tools", `idMso='TabPictureToolsFormat'`) — groups Adjust, Picture Styles, Accessibility, Arrange, Size. Word raises **no** PowerPoint-style "Video Tools / Playback" tab for an online video (Word has no Playback/Trim controls); the rest state is a poster picture.
- **Clone has:** **Picture Tools (Picture Format)** — present, `src/renderer/bridge/state-sync.ts:282`. Because the Online Video output is a genuine `image` NodeSelection, selecting it triggers `PictureToolsPM.syncContextualTab(true)` (`state-sync.ts:284-285` → `picture-tools-pm.js`). This matches Word exactly.
- **Missing tabs:** **none.** No video-specific contextual tab exists or is needed in either Word or the clone. (Any gaps in the *content* of Picture Format itself — Arrange/Size completeness — belong to the Pictures/Illustrations group's ledger, not Media.)

### Fix plan
| gap | NO-FORK / fork | spec-kit / direct-TDD | bridge verb(s) | regression test | oracle check | severity | autonomy |
|---|---|---|---|---|---|---|---|
| Online Video inserts a placeholder SVG poster with no `wp15:webVideoPr` ext and no external `a:hlinkClick` relationship — watch URL only kept as `alt` text, so the .docx does not round-trip as a Word online video | additive fork (new exporter ext: emit `a:blip/a:extLst/a:ext/wp15:webVideoPr` + an External `a:hlinkClick` rel on the existing `pic:pic`) — likely small/additive but touches the super-converter image exporter | spec-kit (new web-video OOXML emit is a bounded subsystem-ish change worth a plan) | extend `WC.PM.insertImage` (insert.ts:45) to accept/carry a `webVideo:{url,embeddedHtml,h,w}` payload + emit the ext on export | scripts/test-suite-pm.js: insert online video → export → assert `wp15:webVideoPr@embeddedHtml` + External hlinkClick rel present; round-trip survives | Word COM: open the exported .docx, confirm the drawing reads back as an online video (poster picture + playable embed payload); compare `webVideoPr`/hlinkClick to an authored-in-Word fixture | med | **NEEDS-USER** (requires the actual provider poster fetch + a real playback/embed surface that the CSP+sandbox currently block; recommend: build the OOXML-emit subsystem so the .docx is faithful, but **keep the in-app object an honest poster** — playback stays a documented sandbox limitation; do NOT remove from ribbon) |
| Dialog accepts only a plain URL; a pasted `<iframe>` embed-code snippet is not parsed (Word accepts either) | NO-FORK | direct-TDD | `onlineVideoDialog` (insert-features.js:314) parse branch feeding `insertVideoThumbnail`/`insertImage` | scripts/test-suite-pm.js: paste an `<iframe src=… width=… height=…>` → assert src + dims extracted and carried | Word COM: author the same iframe in Word's dialog, diff the resulting `webVideoPr@embeddedHtml`/`@h`/`@w` | low | **AUTO-FIX** (bounded NO-FORK parse of the existing field; pairs with the emit work above but standalone-testable) |

After this section: the Media group is honest in docs (no false "works" claims to fix), needs no new contextual tab, and its single substantive gap (true online-video OOXML + playback) is correctly a NEEDS-USER subsystem call with an AUTO-FIX-able iframe-parse sub-task.


## Section 5 — Links
*Real-Word group:* Insert > **Links** (Link / Bookmark / Cross-reference) · *State:* Link and Bookmark both reach real bridge verbs and round-trip as real OOXML (working); Cross-reference writes a real REF field but is shallow (2 of ~7 reference types) and carries a wrong-output bug (Page-number → `\p` above/below instead of `PAGEREF`). No contextual tabs in this group.

### Feature table
| feature | cmd id | file:line | state | gap vs real Word |
|---------|--------|-----------|-------|------------------|
| Link (Insert Hyperlink dialog) | `link` | src/renderer/public/js/commands.js:397 | working | Primary insert works (real `setLink` mark + relationship id) but dialog is Text+Address only — missing the 4 left-rail categories (Existing File/Web, Place in This Document, Create New Document, E-mail), ScreenTip, Target Frame, Bookmark target picker; split-dropdown 'Insert Link'/'Recent Items' are decorative (no MRU). |
| Bookmark | `bookmark` | src/renderer/public/js/commands.js:446 | working | Functionally complete (paired bookmarkStart/End, list/GoTo/Delete, real OOXML). Missing only Sort-by Name/Location and the 'Hidden bookmarks' checkbox; spaces coerced to underscores. |
| Cross-reference | `crossReference` | src/renderer/public/js/commands.js:447 | shallow | Real REF field node, but only 2 of ~7 reference types (Heading, Bookmark) — no Numbered item / Figure / Table / Equation / Footnote / Endnote; no 'Insert as hyperlink'/'Include above/below'; BUG-013: Page-number emits `REF \p` (above/below) instead of `PAGEREF`. |

### Flows

**Link (Insert Hyperlink) — Ctrl+K**
- Open Insert/Edit Hyperlink dialog (Ctrl+K; reopens as 'Edit Hyperlink' with a 'Remove Link' button when caret sits on a link). Shared controls: 'Text to display' (greyed for objects), 'ScreenTip...' button. **[exists? partial — clone opens a Text+Address dialog only / works? primary insert works / feasible? yes, NO-FORK rail+ScreenTip]** OOXML: `w:hyperlink` (CT_Hyperlink) with `r:id` OR `w:anchor`; `w:rStyle w:val=Hyperlink` on run. **(captured ✓)** real-Word left rail = Existing File or Web Page / Place in This Document / Create New Document / E-mail Address; plus Text to display, ScreenTip..., Bookmark..., Target Frame.
- Existing File or Web Page (default category): Address box; Look in browser with 'Current Folder' / 'Browsed Pages' / 'Recent Files' side buttons; Browse-for-File / Browse-the-Web / Bookmark... (into target file → `#fragment`). **[exists? Address only / works? URL link works / feasible? Address works today; file-browser side buttons not feasible (no fs picker integration); Bookmark-in-target feasible later]** OOXML: `w:hyperlink r:id`; Relationship Type=.../hyperlink Target=`<url>` TargetMode=External; optional `#fragment`.
- Place in This Document: tree of 'Top of the Document' / 'Headings' (every Heading 1-9, indented) / 'Bookmarks' (user bookmarks); fills `w:anchor`, no relationship. **[exists? no / works? no / feasible? yes, NO-FORK — clone already enumerates headings (TOC) and bookmarks (listBookmarks); setLink supports anchor]** OOXML: `w:hyperlink w:anchor=<bookmark-or-heading-anchor>` (no r:id).
- Create New Document: 'Name of new document' + Change... path; 'When to edit' radios (later/now). **[exists? no / works? no / feasible? NO-FORK for the markup (external rel to a path); the actual file-create/open is NEEDS-USER (fs/Office runtime)]** OOXML: `w:hyperlink r:id`; Relationship Target=`<newfile path>` TargetMode=External.
- E-mail Address: 'E-mail address' (auto 'mailto:' prefix) + 'Subject' (`?subject=`) + 'Recently used e-mail addresses' list. **[exists? no / works? no / feasible? yes, NO-FORK — a mailto: target is just an external-relationship URL]** OOXML: `w:hyperlink r:id`; Target=`mailto:addr?subject=...` TargetMode=External.
- Set Hyperlink ScreenTip (sub-dialog): single 'ScreenTip text' box → `w:tooltip` on the hyperlink. **[exists? no / works? no / feasible? yes, NO-FORK — add a `tooltip` field to insertLink → setLink already emits the mark; needs converter to honor `w:tooltip`]** OOXML: `w:hyperlink/@w:tooltip`.
- Remove Link / context menu: right-click → Edit Hyperlink / Open Hyperlink / Copy Hyperlink / Select Hyperlink / Remove Hyperlink (strips wrapper + relationship + Hyperlink style). **[exists? unverified in clone (no captured Remove path) / works? unknown / feasible? yes, NO-FORK — unsetLink mark + context-menu entry]** OOXML: removes `w:hyperlink` wrapper + the .../hyperlink Relationship.

**Bookmark — Ctrl+Shift+F5 (assignable; no default ribbon hotkey)**
- Open Bookmark dialog: name box, list, Add / Delete / Go To, Sort-by Name/Location, 'Hidden bookmarks' checkbox, Close. **[exists? yes (name + list + Add/GoTo/Delete) / works? yes — paired bookmarkStart/End in one tr / feasible? Sort + Hidden checkbox NO-FORK additive]** OOXML: `w:bookmarkStart w:id w:name` + matching `w:bookmarkEnd w:id` (paired by id). **(captured ✓)** real-Word dialog = name box, Add/Delete/Go To, Sort by Name/Location, Hidden bookmarks checkbox.
- Add a bookmark: range (select) or point (caret) bookmark; valid name (letter-first, no spaces). **[exists? yes / works? yes / feasible? n/a — done; clone coerces spaces→underscore (Word rejects spaces, so acceptable but not identical UX)]** OOXML: `<w:bookmarkStart w:id=N w:name=Name/> ... <w:bookmarkEnd w:id=N/>`.
- Delete / Go To bookmark: removes the start/End pair (keeps text) or selects/scrolls to it. **[exists? yes / works? yes (removeBookmark / goToBookmark) / feasible? n/a — done]** OOXML: removes the `w:bookmarkStart/w:bookmarkEnd` pair.
- Show hidden bookmarks / sort: 'Hidden bookmarks' lists auto names (`_Ref########`, `_Toc`, `_GoBack`, `_Hlk`); Sort-by Name (alpha) / Location (doc order). **[exists? no / works? no / feasible? yes, NO-FORK — list filter + sort comparator in the dialog over listBookmarks]** OOXML: hidden bookmarks are the same markup with underscore-prefixed names.
- Show bookmark brackets (Word Options > Advanced > Show bookmarks): display-only. **[exists? no / works? no / feasible? low value — settings-level display toggle, no document change]** OOXML: display-only; no document.xml change.

**Cross-reference — (no default shortcut)**
- Open Cross-reference dialog: Reference type dropdown; 'Insert reference to' dropdown (varies by type); 'Insert as hyperlink' (default ON); 'Include above/below'; 'Separate numbers with'; 'For which...' target list; modeless (stays open). **[exists? yes (Type + Insert-as + target list) / works? real REF field written / feasible? full type+switch coverage NO-FORK at bridge, the `\p` fix is a small fork edit in crossref-wrappers.ts]** OOXML: field via `w:fldChar(begin/separate/end)` + `w:instrText`; name = REF | PAGEREF | NOTEREF. **(captured ✓)** real-Word type list = Numbered item / Heading / Bookmark / Footnote / Endnote / Equation / Figure / Table; 'Insert as hyperlink'; 'Include above/below'; 'Separate numbers'.
- Reference type: **Numbered item** — Page number / Paragraph number / Para number (no context) / Para number (full context) / Above-below / Paragraph text. **[exists? no / works? no / feasible? yes, NO-FORK — additive type + switch map (\r/\n/\w/\p; PAGEREF for page)]** OOXML: PAGEREF (page); REF `\r` / `\n` / `\w` / `\p`.
- Reference type: **Heading** — Heading text / Page number / Heading number / Heading number (no context) / Heading number (full context) / Above-below; auto hidden `_Ref########` bookmark. **[exists? yes (type present) / works? partial — only Text/Page/Above-below display modes wired, Page is BUGGED / feasible? widen switch coverage + auto-_Ref bookmark NO-FORK]** OOXML: REF (heading text); PAGEREF (page); REF `\r`/`\n`/`\w`; REF `\p`.
- Reference type: **Bookmark** — Bookmark text / Page number / Paragraph number (+no/full context) / Above-below. Oracle: ` REF BM1 \h `. **[exists? yes / works? partial (Page bugged) / feasible? yes, NO-FORK to add para-number + PAGEREF]** OOXML: REF `<name>` (e.g. `REF BM1 \h`); PAGEREF; REF `\r`/`\n`/`\w`; REF `\p`.
- Reference type: **Footnote** — Footnote number / Page number / Above-below / Footnote number (formatted, `\f`). **[exists? no / works? no / feasible? yes, NO-FORK — emit NOTEREF (+`\f`); clone already has footnotes]** OOXML: NOTEREF; NOTEREF `\f`; PAGEREF; REF `\p`.
- Reference type: **Endnote** — Endnote number / Page number / Above-below / Endnote number (formatted, `\f`). **[exists? no / works? no / feasible? yes, NO-FORK — NOTEREF + `\f`]** OOXML: NOTEREF; NOTEREF `\f`; PAGEREF; REF `\p`.
- Reference type: **Equation / Figure / Table (+ custom caption labels)** — Entire caption / Only label and number / Only caption text / Page number / Above-below; one entry per caption label in the doc. **[exists? no / works? no / feasible? yes, NO-FORK — clone has captions; map to REF over the caption's hidden bookmark + PAGEREF]** OOXML: REF `<captionBookmark>`; PAGEREF; REF `\p`; auto `_Ref########` bookmark on caption.
- Insert as hyperlink (`\h`) + Include above/below (`\p`) + Separate numbers (`\d`): hyperlink default ON, auto hidden `_Ref########` bookmark for heading/numbered/caption targets without a bookmark. **[exists? no checkboxes in clone dialog / works? no / feasible? yes, NO-FORK — append switches; bookmark auto-create NO-FORK via insertBookmark]** OOXML: `\h`; `\p`; `\d`; hidden `_Ref########` bookmark auto-created.

### Contextual tabs
Real Word raises **no contextual tab** for any Links-group control — Link, Bookmark, and Cross-reference are field/markup constructs. Hyperlinks expose a right-click **context menu** (Edit / Open / Copy / Select / Remove Hyperlink), not a ribbon tab. The clone correctly has no Links contextual tab; nothing is missing here. (The only Link-side gap is the right-click context-menu entries, which are a menu, not a contextual tab.)

### Fix plan
| gap | NO-FORK / fork | spec-kit / direct-TDD | bridge verb(s) | regression test | oracle check | severity | autonomy |
|-----|----------------|-----------------------|----------------|-----------------|--------------|----------|----------|
| Cross-ref Page-number emits `REF \p` (above/below) instead of `PAGEREF` — BUG-013 wrong output | additive fork edit (crossref-wrappers.ts:229) | direct-TDD | `WC.PM.refCrossReference({display:'pageNumber'})` → emit PAGEREF (separate `display` from aboveBelow's `\p`) | scripts/test-suite-pm.js: assert pageNumber ref instrText === `PAGEREF <tgt> \h` (not `REF .. \p`) | COM: InsertCrossReference page-number → field code starts `PAGEREF` | high | AUTO-FIX |
| Cross-ref missing 5 reference types (Numbered item, Footnote, Endnote, Equation/Figure/Table) | NO-FORK | spec-kit (extend 018 / new) | `refCrossReference({type, display})` + switch map (`\r\n\w\p\f`, PAGEREF, NOTEREF) | probe asserts each type emits correct field name+switch | COM read-back of each type's field code | med | AUTO-FIX |
| Cross-ref missing 'Insert as hyperlink' (`\h`) + 'Include above/below' (`\p`) + auto `_Ref` bookmark | NO-FORK | direct-TDD | `refCrossReference({hyperlink,aboveBelow})` + `insertBookmark` for auto `_Ref########` | assert `\h`/`\p` appended; auto-bookmark present | COM: `REF BM1 \h` matches oracle | med | AUTO-FIX |
| Link dialog missing 'Place in This Document' (anchor link to heading/bookmark) | NO-FORK | direct-TDD | `WC.PM.insertLink({anchor})` (setLink supports anchor) + listBookmarks/heading enum | assert `w:hyperlink w:anchor=` emitted, no rel | COM: internal link has `w:anchor`, no External rel | med | AUTO-FIX |
| Link dialog missing E-mail Address (mailto:) category | NO-FORK | direct-TDD | `insertLink({href:'mailto:...?subject='})` | assert mailto target + `?subject=` in rel | COM: external rel Target starts `mailto:` | low | AUTO-FIX |
| Link dialog missing ScreenTip (`w:tooltip`) | NO-FORK | direct-TDD | `insertLink({tooltip})` → setLink + converter honors `w:tooltip` | assert `w:hyperlink/@w:tooltip` present | COM: hyperlink `w:tooltip` == entered text | low | AUTO-FIX |
| Bookmark dialog missing Sort-by Name/Location + 'Hidden bookmarks' filter | NO-FORK | direct-TDD | dialog-side sort/filter over `WC.PM.listBookmarks()` | assert sorted order + underscore-name visibility toggle | n/a (UI-only; bookmarks already oracle-verified) | low | AUTO-FIX |
| Link split-dropdown 'Recent Items' MRU is decorative (no handler) | NO-FORK | direct-TDD or honest-stub | none (or wire a real MRU store) | assert item either populates MRU or is removed | n/a | low | AUTO-FIX |
| Link 'Existing File or Web Page' file-browser (Look in / Browse for File side buttons) | NO-FORK markup; NEEDS-USER fs picker | (judgment) | `insertLink({href})` (Address box already works) | n/a | n/a | low | NEEDS-USER |
| Link 'Create New Document' — actually create + open a new file | fork/runtime | (judgment) | `insertLink({href:path})` for markup; file create/open is external | n/a | n/a | low | NEEDS-USER |
| False docs: INSERT_TAB.md:38 marks Cross-reference fully working (✅); NOT_IMPLEMENTED.md:107 omits the `\p` defect | docs-only | direct edit | n/a | n/a | n/a | low | AUTO-FIX |


## Section 6 — Comments
*Real-Word group:* Insert tab → **Comments** group (single "Comment" button — a re-surfacing of Review → New Comment). · *State:* WORKING end-to-end — the lone control inserts a real comment entity (commentRangeStart/End + commentReference + comments.xml) via the live Document API; the only gaps are entry-flow nuance, the modern thread/companion-part lifecycle (reply/resolve/edit/delete + commentsExtended/Ids/Extensible), and a stale code comment.

### Feature table
| feature | cmd id | file:line | state | gap vs real Word |
|---------|--------|-----------|-------|------------------|
| Comment (New Comment) | comment | src/renderer/public/js/commands.js:429 | working | Clone opens a margin composer card and refuses empty text / a bare caret not in a word; Word inserts an empty balloon you type into in place. End result is an equivalent real comment entity that survives docx export. Thread lifecycle (reply/resolve/edit/delete) + modern companion parts (commentsExtended/Ids/Extensible.xml, people.xml) are not exercised by this Insert-tab alias. |

### Flows
**Comment (New Comment)** — one button `insert.comments.comment` (cmd `comment`, ribbon-data.js:756-761), a pure alias re-dispatching to Review's `newComment` (commands.js:429 → H.newComment commands.js:434 → WC.CommentsUI.compose() comments-ui.ts:153).

- **Insert a new comment on a selection** *(captured ✓)* — Insert > Comments > Comment (or Review > New Comment, or **Ctrl+Alt+M**, or right-click > New Comment). **[exists? YES / works? YES / feasible-in-clone? already done]** Captured this session in real Word: inserting on a selection anchors the comment to the selection and opens the modern comment card; OOXML = `w:comment` + `commentRangeStart`/`commentRangeEnd` + `commentReference` (+ people.xml). Clone: H.comment → composer card → Post / Ctrl+Enter → pm.cmd('addComment') → review.ts:296 addComment → editor.doc.comments.create({text,target}) (review.ts:302), a real entity-store write that exports to word/comments.xml. **Sub-flow @mention / Assign-to** **[exists? NO / works? n/a / feasible-in-clone? cloud-only, out of scope]** — Word's people picker + task assignment require a cloud identity backend; honest gap, not a no-op. **OOXML:** `<w:commentRangeStart w:id="N"/>` … `<w:commentRangeEnd w:id="N"/>` + `<w:r><w:rPr><w:rStyle w:val="CommentReference"/></w:rPr><w:commentReference w:id="N"/></w:r>` in document.xml; body in comments.xml; modern companions commentsExtended.xml (`w15:done`), commentsIds.xml (`w16cid:durableId`), commentsExtensible.xml (`w16cex:dateUtc`), optional people.xml. **Shortcut Ctrl+Alt+M / Post Ctrl+Enter** **[exists? Ctrl+Enter posts in the card; Ctrl+Alt+M wiring not confirmed in scope / works? Post works / feasible-in-clone? Ctrl+Alt+M trivially wireable]**.

- **Insert a comment at the insertion point (no selection)** **[exists? PARTIAL / works? clone refuses a bare caret / feasible-in-clone? YES, additive]** — Word anchors to the insertion point as a single anchor. Clone's addComment (review.ts:297-298) refuses an empty/collapsed caret unless it can expand to the containing word, so a caret in whitespace is rejected — diverges from Word's point-anchor behavior. **OOXML:** per ECMA-376 a `commentRangeStart` with no range content acts as a point anchor; Word still emits the matching `commentRangeEnd` + `commentReference` mark and the same comments.xml/commentsExtended/Ids/Extensible entries.

- **Reply within a comment thread** **[exists? NO (not via this Insert control) / works? n/a / feasible-in-clone? YES, NO-FORK via the entity store]** — Word: type in a posted card's Reply box, Ctrl+Enter → child comment. Clone's Insert alias only creates top-level comments; replies would need a thread-aware composer + paraIdParent linkage. **OOXML:** new `<w:comment w:id="M">` appended to comments.xml; threading via `<w15:commentEx w15:paraId="…" w15:paraIdParent="…" w15:done="0"/>` in commentsExtended.xml; its own durable-id/dateUtc entries.

- **Resolve / reopen a comment thread** **[exists? NO / works? n/a / feasible-in-clone? YES, NO-FORK]** — Word: card "…" > Resolve thread / Reopen. Clone has no resolve UI on this path. **OOXML:** toggles `w15:done` on the thread's `<w15:commentEx>` in commentsExtended.xml (1 resolved / 0 active); comments.xml text unchanged.

- **Edit / delete a comment** **[exists? NO (via this control) / works? n/a / feasible-in-clone? YES, NO-FORK]** — Word: pencil to edit, "…" > Delete thread, or Review > Delete dropdown (incl. Delete All Comments). Clone's Insert alias is insert-only. **OOXML:** editing rewrites the `<w:p>/<w:r><w:t>` of that `<w:comment>`; deleting removes the `<w:comment>` plus its `commentRangeStart`/`End`/`commentReference` (matching w:id) and its commentsExtended/Ids/Extensible entries; Delete-All removes the comment parts + rels + content-type overrides.

### Contextual tabs
Real Word raises **no Ribbon contextual tab** for comments — modern comments use an inline comment card (Contextual view) or the Comments pane (List view); affordances are the editable text box, @mention picker, "Assign to" checkbox, "Post comment", and per-thread Reply/Edit/Resolve/"More thread actions" (Delete thread / Resolve / Link to this comment). The clone correctly has **no contextual tab** for this group; instead it renders an in-margin contextual composer card via src/renderer/bridge/comments-ui.ts (not a ribbon tab). **No missing contextual tab** — parity is correct here. The persistent comment commands (New Comment, Delete dropdown, Previous/Next, Show Comments) live on the Review tab, outside this Insert-group scope.

### Fix plan
| gap | NO-FORK / fork | spec-kit / direct-TDD | bridge verb(s) | regression test | oracle check | severity | autonomy |
|-----|----------------|-----------------------|----------------|-----------------|--------------|----------|----------|
| Stale code comment at commands.js:430-433 falsely says H.newComment is "a guarded no-op until WC.CommentsUI lands" (WC.CommentsUI has landed; installed index.ts:701) | NO-FORK | direct-TDD (comment-only edit, task_55c214e5) | none (comment text only) | n/a (doc/comment change) | n/a | low | AUTO-FIX |
| Bare-caret / insertion-point comment refused unless caret expands to a word (review.ts:297-298), unlike Word's point anchor | NO-FORK | direct-TDD | WC.PM.addComment (review.ts addComment) — allow a collapsed/point anchor, emit matching start/end + reference | test-suite-pm: insert comment with caret in whitespace → expect a point-anchored comment entity + valid export | Word COM: open exported docx, assert comments.Count==1 and the range is a point anchor | low | AUTO-FIX |
| Ctrl+Alt+M shortcut not confirmed wired to the Insert/Review comment command | NO-FORK | direct-TDD | none (keybinding → H.comment) | test-suite-pm: simulate Ctrl+Alt+M → composer opens | n/a (UI) | low | AUTO-FIX |
| Reply / Resolve / Edit / Delete thread lifecycle + modern companion parts (commentsExtended/Ids/Extensible.xml) not exercised by the Insert path | NO-FORK | spec-kit (new comment-thread-lifecycle feature) | WC.PM.* on review.ts/comments-ui.ts: addReply, resolveComment(done), editComment, deleteComment | test-suite-pm: create→reply→resolve→edit→delete, assert comments.xml + commentsExtended w15:done/paraIdParent | Word COM: read back thread parent/child + Resolved state after round-trip | med | AUTO-FIX |
| @mention picker + "Assign to" task assignment (cloud identity / people.xml backend) | NEEDS-USER (new external-identity subsystem) | spec-kit if pursued | n/a | n/a | n/a | low | NEEDS-USER |


## Section 7 — Header & Footer
*Real-Word group:* Insert ▸ **Header & Footer** (Header, Footer, Page Number) · *State:* Bridge writes are REAL and Word-COM-validated (headerN/footerN.xml, sectPr titlePg, settings.xml evenAndOddHeaders, real PAGE fields) — the document-write layer is sound. The GAPS are all in the UI layer: Header/Footer dropdowns expose only a plain-text Edit modal (no built-in gallery, no Remove, no Save-Selection), and the Page Number flyout is a flat 3-position list (no design sub-galleries, no Page Margins, no Format Page Numbers dialog, and Current Position can't target the body caret). The Header & Footer contextual tab is real and event-driven.

### Feature table
| feature | cmd id | file:line | state | gap vs real Word |
|---|---|---|---|---|
| Header (dropdown) — Edit Header | insert.header-footer.header / cmd:header | src/renderer/public/js/commands.js:470 (handler), :452-469 (dialog), dispatch :1849 | shallow | Opens only a single-textarea Edit modal; no gallery / Remove / Save-Selection / Office.com items. Plain-text round-trip, not Word's rich band. |
| Header — Built-in gallery | insert.header-footer.header (item 'Built-in gallery') | src/renderer/public/js/commands.js:452-471 (none built) | missing | ~20 built-in header designs (Blank, Austin, Banded…) entirely absent. |
| Header — Remove Header | insert.header-footer.header (item 'Remove Header') | src/renderer/public/js/commands.js:452-471 (no item) | missing | No 'Remove Header' menu item; setHeaderText('') exists but is unwired. |
| Header — Save Selection / More from Office.com | insert.header-footer.header (items) | n/a — no handler | missing | Building-block save + online gallery fetch absent (acceptable long-tail). |
| Footer (dropdown) — Edit Footer | insert.header-footer.footer / cmd:footer | src/renderer/public/js/commands.js:471 (handler), :452-469 (shared dialog), dispatch :1850 | shallow | Same shape as Header: plain-text Edit modal only; no gallery / Remove / Save-Selection. |
| Footer — Built-in gallery | insert.header-footer.footer (item 'Built-in gallery') | src/renderer/public/js/commands.js:452-471 (none) | missing | Built-in footer designs absent. |
| Footer — Remove Footer | insert.header-footer.footer (item 'Remove Footer') | src/renderer/public/js/commands.js:452-471 (none) | missing | No 'Remove Footer' item; header/footer body has no Remove action. |
| Footer — Save Selection / More from Office.com | insert.header-footer.footer (items) | n/a — no handler | missing | Long-tail building-block features absent. |
| Page Number (dropdown root) | insert.header-footer.page-number / cmd:pageNumber | src/renderer/public/js/commands.js:474-480, dispatch :1851 | shallow | Flat list of 3 positions + Remove; Word shows 4 submenu galleries + Format Page Numbers. |
| Page Number — Top of Page | insert.header-footer.page-number (item 'Top of Page') | src/renderer/public/js/commands.js:475 | shallow | Inserts one default left-aligned header PAGE field; no ~10-design sub-gallery, no alignment/format. |
| Page Number — Bottom of Page | insert.header-footer.page-number (item 'Bottom of Page') | src/renderer/public/js/commands.js:476 | shallow | Single default footer field; no bottom design sub-gallery, no options. |
| Page Number — Page Margins | insert.header-footer.page-number (item 'Page Margins') | n/a — not rendered (header-footer.ts:190-195 type only top\|bottom\|current) | missing | Margin text-box/frame page number entirely absent. |
| Page Number — Current Position | insert.header-footer.page-number (item 'Current Position') | src/renderer/public/js/commands.js:477 | shallow | Always lands in a header/footer part, never the body caret (Word inserts at the body insertion point); no design sub-gallery. |
| Page Number — Format Page Numbers… | insert.header-footer.page-number (item 'Format Page Numbers…') | n/a — not rendered; no dialog | missing | No format/start-at/chapter dialog; insertPageNumber hard-codes ' PAGE '. |
| Page Number — Remove Page Numbers | insert.header-footer.page-number (item 'Remove Page Numbers') | src/renderer/public/js/commands.js:479; bridge header-footer.ts:281-292 | working | Matches Word for page-number-only bands; mixed title+PAGE band gets fully cleared (v1). |
| H&F tab — Go to Header | header-footer-tools / cmd:goToHeader | src/renderer/public/js/commands.js:404; bridge header-footer.ts:345-373 | working | Faithful; single-section only. |
| H&F tab — Go to Footer | header-footer-tools / cmd:goToFooter | src/renderer/public/js/commands.js:405; bridge header-footer.ts:345-373 | working | Faithful for single section. |
| H&F tab — Close Header and Footer | header-footer-tools / cmd:closeHeaderFooter | src/renderer/public/js/commands.js:406; bridge header-footer.ts:376-378 | working | Faithful. |
| H&F tab — Different First Page | header-footer-tools / cmd:differentFirstPage | src/renderer/public/js/commands.js:414-418; bridge header-footer.ts:148-157 | working | Faithful: real sectPr w:titlePg; first-page variant via opts.variant 'first'. |
| H&F tab — Different Odd & Even Pages | header-footer-tools / cmd:differentOddEven | src/renderer/public/js/commands.js:419-423; bridge header-footer.ts:159-167 | working | Faithful: settings.xml evenAndOddHeaders; even variant via opts.variant 'even'. |
| H&F tab — Header/Footer/PageNumber (re-exposed) | header-footer-tools / cmd:header,footer,pageNumber | src/renderer/public/js/header-footer-tools-pm.js:32-34 | shallow | Re-uses Insert handlers → inherits the plain-text/no-gallery/3-position gaps. |
| H&F tab — long-tail (Date&Time, Doc Info, Show Doc Text, Link to Previous) | cmd:dateAndTime,docInfo,showDocText,linkToPrevious | src/renderer/public/js/commands.js:408-411 | stub | Honest WC.toast() messages; no bridge call (out of strict Insert scope but flagged). |

### Flows

**Header (Insert ▸ Header & Footer)** — split button: top half re-applies last-used gallery item; arrow opens gallery + commands. OOXML: header part `word/headerN.xml` (`<w:hdr>`), referenced from `<w:sectPr>` via `<w:headerReference w:type="default|first|even" r:id=…>`.
- **Header gallery (built-in building blocks)** — **(captured ✓:** real Word shows Blank, Blank (Three Columns), + ~14 designs: Austin, Banded, Facet, Filigree, Grid, Integral, Ion Dark/Light, Retrospect, Sideline, Semaphore, Slice, ViewMaster, Whisp; bottom items More Headers from Office.com / Edit Header / Remove Header / Save Selection to Header Gallery.) OOXML: each block is normal block content; property fields use `<w:sdt>`/`<w:docPartObj>`, page numbers `<w:fldSimple w:instr=" PAGE \* MERGEFORMAT ">`. Shortcut Alt,N,H. **[exists? NO / works? — / feasible-in-clone? YES, NO-FORK — render the gallery flyout + ship a small fixed set of building-block templates that author via setHeaderText/insertContent into the materialized header part]**
- **Edit Header** — switches caret into the existing header without inserting a block; raises H&F tab. Shortcut Alt,N,H,E. OOXML materializes `word/headerN.xml` referenced by `<w:headerReference w:type="default">`. **[exists? YES (the clone's only header flow — but it is a plain-text textarea modal, not on-page caret entry from this dropdown) / works? PARTIAL — writes real headerN.xml but only plain text; on-page entry is via the contextual tab's Go to Header / works? feasible to route the dropdown's Edit item straight to enterHeaderFooter('header') / feasible-in-clone? YES, NO-FORK]**
- **Remove Header** — deletes the section's header content + reference. Shortcut Alt,N,H,R. OOXML removes `<w:headerReference>` from `<w:sectPr>`; drops/empties headerN.xml. **[exists? NO / works? — / feasible-in-clone? YES, NO-FORK — add a Remove Header item wired to setSlotText('header','') (the bridge already clears bands); ideally also drop the headerReference]**
- **Save Selection to Header Gallery** — Create New Building Block dialog (Name, Gallery=Headers, Category, Description, Save in, Options=content only/own paragraph/own page). Shortcut Alt,N,H,S. OOXML persists `<w:docPart>` into `word/glossary/document.xml`. **[exists? NO / works? — / feasible-in-clone? PARTIAL — needs a building-block/glossary persistence store; long-tail, keep honest stub for v1]**

**Footer (Insert ▸ Header & Footer)** — mirror of Header; split button, same last-used re-apply. OOXML: `word/footerN.xml` (`<w:ftr>`), `<w:footerReference w:type="default|first|even" r:id=…>` in `<w:sectPr>`.
- **Footer gallery (built-in building blocks)** — **(captured ✓:** same gallery shape as Header — Blank, Blank (Three Columns), Austin…Whisp, + More Footers from Office.com / Edit Footer / Remove Footer / Save Selection to Footer Gallery.) Most footers embed a PAGE field. OOXML: `<w:fldSimple w:instr=" PAGE ">` often wrapped in a Page-Numbers `<w:sdt>`. **[exists? NO / works? — / feasible-in-clone? YES, NO-FORK — same gallery-flyout + template approach as Header]**
- **Edit Footer** — caret into existing footer; raises H&F tab. Shortcut Alt,N,O,E. OOXML materializes `word/footerN.xml`. **[exists? YES (plain-text Edit modal only) / works? PARTIAL — real footerN.xml, plain text; route Edit → enterHeaderFooter('footer') / feasible-in-clone? YES, NO-FORK]**
- **Remove Footer** — deletes footer content + reference. Shortcut Alt,N,O,R. OOXML removes `<w:footerReference>`; drops footerN.xml. **[exists? NO / works? — / feasible-in-clone? YES, NO-FORK — Remove Footer item → setSlotText('footer','')]**
- **Save Selection to Footer Gallery** — Create New Building Block (Gallery=Footers). Shortcut Alt,N,O,S. OOXML `<w:docPart>` with `<w:gallery w:val="ftrs"/>`. **[exists? NO / works? — / feasible-in-clone? PARTIAL — building-block store needed; keep honest stub for v1]**

**Page Number (Insert ▸ Header & Footer)** — dropdown (not split). **(captured ✓:** real Word menu = Top of Page ▸, Bottom of Page ▸, Page Margins ▸, Current Position ▸, Format Page Numbers…, Remove Page Numbers; each of the first four is a building-block GALLERY — Plain Number 1/2/3, Accent Bar, Brackets, Page X of Y, etc.)
- **Top of Page (gallery)** — submenu gallery placed in the HEADER (Plain Number 1/2/3, Accent Bar 1-4, Page X / Page X of Y Bold Numbers, Brackets, Dots, Large Color, Roman Numeral, Vertical Outline 1/2, More from Office.com, Save Selection). Shortcut Alt,N,N,T. OOXML: `<w:sdt>` with `<w:docPartGallery w:val="Page Numbers (Top of Page)"/>` (enum pgNumT) wrapping the PAGE field. **[exists? PARTIAL — inserts ONE un-styled left header PAGE field, no gallery / works? PARTIAL — real header PAGE field via story runtime / feasible-in-clone? YES, NO-FORK — render the design sub-gallery; each item still ends in insertPageNumber({position:'top'}) plus alignment/style on the field run]**
- **Bottom of Page (gallery)** — same gallery placed in the FOOTER. Shortcut Alt,N,N,B. OOXML: `<w:sdt>` `<w:docPartGallery w:val="Page Numbers (Bottom of Page)"/>` (pgNumB). **[exists? PARTIAL — one default footer field / works? PARTIAL — real footer PAGE field / feasible-in-clone? YES, NO-FORK — sub-gallery over insertPageNumber({position:'bottom'})]**
- **Page Margins (gallery)** — places the number in the side margin as a positioned text box in the header story (Plain Number Vertical L/R, Accent Bar L/R, Circle, Rounded Rectangle, Vertical). Shortcut Alt,N,N,M. OOXML: `<w:sdt>` `<w:docPartGallery w:val="Page Numbers (Margins)"/>` (pgNumMargins) with a floating `<w:drawing>/<wp:anchor>` text box holding the PAGE field. **[exists? NO / works? — / feasible-in-clone? PARTIAL — needs a floating-textbox-in-header-story insert path; bigger than top/bottom; defer or honest stub for v1, then build on the existing floating-object machinery]**
- **Current Position (gallery)** — inserts a page-number block at the CURRENT cursor (body/header/footer) without changing layout (Plain Number, Accent Bar, Page X, Page X of Y, Brackets, Dots, Roman Numeral, Vertical Outline). Shortcut Alt,N,N,U. OOXML: `<w:sdt>` `<w:docPartGallery w:val="Page Numbers"/>` (pgNum) at the caret; no header/footer reference created. **[exists? PARTIAL — item exists but regionForPosition('current') always resolves to a header/footer part (header-footer.ts:194), never the body caret / works? NO for body insertion — wrong target / feasible-in-clone? YES, NO-FORK — extend insertPageNumber to insert a PAGE field at the live body selection when no H/F session is active]**
- **Format Page Numbers (dialog)** — **(captured ✓:** Number format dropdown (1,2,3 / a,b,c / A,B,C / i,ii,iii / I,II,III), Include chapter number (Chapter starts with style + Use separator), Page numbering = Continue from previous section / Start at spinner.) Shortcut Alt,N,N,F. OOXML: writes `<w:pgNumType>` in `<w:sectPr>` — `w:fmt`/`w:start`/`w:chapStyle`/`w:chapSep`; plus the PAGE field format switch. **[exists? NO / works? — / feasible-in-clone? YES, NO-FORK — add the dialog + a bridge verb writing pgNumType onto the section sectPr (the same owned-sectPr write pattern used by columns/line-numbers)]**
- **Remove Page Numbers** — removes the page-number blocks/fields Word inserted from the galleries. Shortcut Alt,N,N,R. OOXML deletes the page-number `<w:sdt>`/PAGE fields from headerN/footerN.xml (does not by itself drop `<w:pgNumType>`). **[exists? YES / works? YES — bridge removePageNumbers walks slots, clears only PAGE-field bands via slotHasPageField guard (header-footer.ts:255-292) / feasible-in-clone? already working; v1 mixed-content band gets fully cleared]**

### Contextual tabs
Real Word raises a single **Header & Footer** tab (Word 2016/2013: "Header & Footer Tools ▸ Design") whenever the caret enters a header/footer story, with groups: **Header & Footer** (Header/Footer/Page Number), **Insert** (Date & Time, Document Info, Quick Parts, Pictures, Online Pictures), **Navigation** (Go to Header, Go to Footer, Previous/Show Previous, Next/Show Next, Link to Previous), **Options** (Different First Page, Different Odd & Even Pages, Show Document Text), **Position** (Header from Top, Footer from Bottom, Insert Alignment Tab), **Close** (Close Header and Footer).

The clone HAS this tab — `src/renderer/public/js/header-footer-tools-pm.js` — and it is REAL and event-driven (the paged PE fires `headerFooterModeChanged` → bridge re-emits `wc:hf-mode` → showContextualTab/hideContextualTab; NOT via state-sync.ts, which is correct for this surface). Present groups: Header & Footer (re-exposed dropdowns), Navigation (Go to Header/Footer), Options (Different First Page / Odd & Even, painted from getHeaderFooterOptions), Close.

Missing from the clone tab vs real Word:
- **Insert group** (Date & Time dialog → DATE/TIME field; Document Info → DocProperty fields; Quick Parts → Field/Building Blocks Organizer/AutoText; Pictures / Online Pictures). The four long-tail buttons that exist are honest toasts (commands.js:408-411).
- **Position group** (Header from Top / Footer from Bottom spinners → `<w:pgMar w:header/w:footer>`; Insert Alignment Tab → `<w:ptab>`). Entirely absent.
- **Navigation extras**: Previous Section / Next Section and **Link to Previous** are honest toasts (single-section only).
The target object (the painted header/footer story) already exists, so the missing groups are additive wiring, not a new subsystem.

### Fix plan
| gap | NO-FORK / fork | spec-kit / direct-TDD | bridge verb(s) | regression test | oracle check | severity | autonomy |
|---|---|---|---|---|---|---|---|
| Header/Footer dropdown = plain-text modal; no gallery flyout | NO-FORK | spec-kit (gallery + built-in templates) | setSlotText('header'/'footer', …) / insertContent into materialized part | probe asserts a chosen design lands in headerN/footerN.xml | Word COM read-back: header/footer part exists + content matches template | med | AUTO-FIX |
| Header Remove + Footer Remove missing | NO-FORK | direct-TDD | setSlotText(kind,'') (add Remove items) | pm: Remove clears the band part | COM: header/footerReference dropped or part emptied | med | AUTO-FIX |
| Edit Header / Edit Footer not wired to on-page caret entry | NO-FORK | direct-TDD | enterHeaderFooter('header'/'footer') | pm: Edit item activates H&F mode (isActiveHF) | n/a (in-app behavior) | low | AUTO-FIX |
| Page Number Top/Bottom have no design sub-gallery | NO-FORK | spec-kit | insertPageNumber({position}) + run alignment/style | probe: each sub-gallery item inserts the styled field | COM wdFieldPage resolves; alignment matches | med | AUTO-FIX |
| Page Number — Current Position can't target body caret | NO-FORK | direct-TDD | insertPageNumber({position:'current'}) extended to body selection | pm: with no H/F session, PAGE field lands at body caret | COM: PAGE field present in body run | med | AUTO-FIX |
| Page Number — Format Page Numbers dialog missing | NO-FORK | spec-kit (016/new) | new setPageNumberFormat (writes sectPr `<w:pgNumType>` fmt/start/chapStyle/chapSep) | roundtrip:paged asserts pgNumType in sectPr | COM: section PageNumbers.NumberStyle / StartingNumber == authored | high | AUTO-FIX |
| Page Number — Page Margins (margin text box) missing | NO-FORK (reuse floating-object machinery) | spec-kit | new insertPageNumberMargin → floating textbox in header story + PAGE field | probe: margin block authored with anchored drawing + PAGE | COM: shape in header with PAGE field | med | AUTO-FIX (defer; honest stub acceptable v1) |
| H&F tab Position group (Header-from-Top / Footer-from-Bottom / Alignment Tab) missing | NO-FORK | spec-kit | new setHeaderDistance/setFooterDistance (sectPr `<w:pgMar w:header/footer>`); ptab insert | probe: pgMar header/footer updated | COM: PageSetup.HeaderDistance/FooterDistance | low | AUTO-FIX |
| H&F tab Insert group (Date&Time, Quick Parts, Doc Info) are toasts | NO-FORK (fields) | spec-kit | insert DATE/TIME/DocProperty fields via story doc.fields.insert | probe: field present in active story | COM field code read-back | low | AUTO-FIX (long-tail; date/time first) |
| Link to Previous + Previous/Next Section are toasts | NO-FORK but needs multi-section model | NEEDS-USER (scope) | (section linking) | n/a | n/a | low | NEEDS-USER |
| Save Selection to Header/Footer Gallery (building-block persistence) | NO-FORK but needs glossary store | NEEDS-USER (new subsystem) | (building-block/glossary store) | n/a | n/a | low | NEEDS-USER |
| 'More Headers/Footers/Page Numbers from Office.com' (online gallery fetch) | external runtime | NEEDS-USER (keep honest stub / remove) | n/a | n/a | n/a | low | NEEDS-USER |
| docs/INSERT_TAB.md:46-48 + FEATURES.md:28 + INSERT_TAB.md:67 false 'works' claims | n/a (docs) | direct-TDD (doc fix) | n/a | n/a | n/a | med | AUTO-FIX |


## Section 8 — Text
*Real-Word group:* Insert ▸ Text (Text Box, Quick Parts, WordArt, Drop Cap, Signature Line, Date & Time, Object) · *State:* Inserts work for the simple paths (Text Box, WordArt, Drop Cap None/Dropped/In-margin, Date & Time DATE field, Quick Parts 6 fixed fields) but every gallery/dialog is shallow; Signature Line and Object>Create-New are toast-only stubs behind complete-looking UI; Building Blocks Organizer / AutoText / Ctrl+F9 / Drop Cap Options are listed in the ribbon but have no handler; and the Shape Format / WordArt Format contextual tab is entirely absent (only `type==='image'` raises Picture Format).

### Feature table
| feature | cmd id | file:line | state | gap vs real Word |
|---------|--------|-----------|-------|------------------|
| Text Box (gallery + dropdown) | textBox | src/renderer/public/js/commands.js:426 (H.textBox), :496-501 (textBoxMenu), :1852 (menu dispatch) | shallow | Inserts a real editable v:textbox, but no built-in gallery (Word ~40 styled built-ins) and no "Save Selection to Text Box Gallery"; the 2 menu items are placeholders. |
| Draw Text Box | textBox | src/renderer/public/js/commands.js:499 | shallow | "Draw Text Box" is identical to Simple — inserts a default box at caret; no rubber-band click-drag placement/sizing. |
| Quick Parts > Field... | quickParts | src/renderer/public/js/insert-features.js:239-243 (fieldDialog/insertField), bridge/insert-exotica.ts:124-131 (xeQuickPart) | shallow | Real field insert, but only 6 hard-coded field types; Word has ~90 fields across 9 categories with per-field switches, "Preserve formatting", Field Codes/Options view. |
| Quick Parts > Document Property | quickParts | src/renderer/public/js/insert-features.js:234, :243 | shallow | Always inserts Title only, as a plain field; Word shows a 15-item property submenu inserting databound content controls. |
| Quick Parts > Building Blocks Organizer... | quickParts | src/renderer/public/js/insert-features.js:230-237 (quickPartsMenu) | missing | Listed in ribbon-data.js:847 but no item/handler/dialog — the whole Organizer (browse/insert/delete/edit) is absent. |
| Quick Parts > AutoText | quickParts | src/renderer/public/js/insert-features.js:230-237 | missing | Listed in ribbon-data.js:844 but no AutoText gallery/save/insert path; grep finds nothing. |
| Quick Parts > Ctrl+F9 empty field code | quickParts | (no handler) | missing | No Ctrl+F9 keybinding / empty `{ }` field-braces insert; F9 update exists in the fork but Ctrl+F9 does not. |
| WordArt (style gallery) | wordart | src/renderer/public/js/insert-features.js:204-216 (wordArtMenu/insertWordArt), bridge/insert-exotica.ts:164-166 (xeWordArt) | working | Real DrawingML insert, but gallery is 6 CSS-approximated styles vs Word's 15 presets; no Text Fill/Outline/Effects/Transform. |
| Drop Cap (None / Dropped / In Margin) | dropCap | src/renderer/public/js/commands.js:502-508 (dropCapMenu), bridge/insert-exotica.ts:50-66 (xeDropCap) | working | Core 3 modes write real w:framePr; missing font / lines-to-drop / distance-from-text (live in Options below). |
| Drop Cap Options... | dropCap | src/renderer/public/js/commands.js:502-508 | missing | Listed in ribbon-data.js:873 but dropCapMenu renders only None/Dropped/In Margin; lines hard-coded to 3, no Font/Distance dialog. |
| Signature Line (Microsoft Office Signature Line) | signatureLine | src/renderer/public/js/insert-features.js:219-227 (signatureLine), bridge/insert-exotica.ts:181 (xeSignatureLine) | stub | Dialog collects signer/title but OK only toasts "future update" and returns — no document mutation; inputs discarded. |
| Date & Time | dateTime | src/renderer/public/js/insert-features.js:186-196 (dateTimeDialog), bridge/insert-exotica.ts:118-123 (xeDateTime) | working | Real DATE field, but "Update automatically" is collected & ignored (always a field, never static text); 6 fixed formats; no Language / Set As Default. |
| Object > Object... (Create New OLE) | object | src/renderer/public/js/insert-features.js:302-307 (objectMenu), bridge/insert-exotica.ts:180 (xeObject) | stub | Menu item is an inline toast "not available"; no OLE embed (Create New / from File) at all. |
| Object > Text from File... | object | src/renderer/public/js/insert-features.js:304, :308-313 (textFromFile) | shallow | Inserts file content via generic open→HTML→paste; no Range/bookmark, no link-to-file (INCLUDETEXT), fidelity bounded by docx→HTML. |
| Drawing Tools / Shape Format contextual tab | (contextual) | src/renderer/bridge/state-sync.ts:282-285; src/renderer/public/js/ribbon.js:526-578 | missing | A selected text-box/shape NodeSelection isn't `type==='image'` → no tab; no shape fill/outline/effects/arrange/size controls anywhere. |
| WordArt Format contextual tab | (contextual) | src/renderer/bridge/state-sync.ts:282-285 | missing | A selected WordArt (vectorShape) isn't `type==='image'` → no tab; no text fill/outline/effects/transform UI. |

### Flows

**Text Box**
- **Text Box gallery (Built-in)** — gallery of ~40 styled building blocks (Simple Text Box, Austin Quote/Sidebar, Banded, Facet, Filigree, Grid, Integral, Ion Dark/Light, Motion, Retrospect, Semaphore, Sidebar Bar, Slice, Whisp, ViewMaster …). **[exists? partial / works? no / feasible? yes]** — only "Simple Text Box" exists and it's a plain `xeTextBox('')`; the styled built-ins are absent (placeholder menu). OOXML: `w:drawing > wp:anchor > a:graphicData(wordprocessingShape) > wps:wsp/wps:txbx/w:txbxContent` (mc:AlternateContent VML `v:textbox` fallback). Feasible NO-FORK by extending the menu to a thumbnail gallery that calls `xeTextBox` with preset shape props. Raises **Shape Format (Drawing Tools)**.
- **More Text Boxes from Office.com** — online fetch of extra built-ins. **[exists? no / works? no / feasible? no — needs Office.com backend]**. Same wps:wsp OOXML. NEEDS-USER (online backend) or keep absent.
- **Draw Text Box** — crosshair click-drag to place an arbitrary-size empty box. **[exists? as no-op alias / works? no / feasible? yes-ish]** — current item duplicates Simple Text Box (commands.js:499). OOXML: empty `wps:wsp` with `wps:spPr a:prstGeom prst='rect'` + sized `a:xfrm`. A true rubber-band draw is a renderer interaction (medium); inserting a default-sized box already works. Raises **Shape Format**.
- **Draw Vertical Text Box** — vertical text flow variant. **[exists? no / works? no / feasible? yes]**. OOXML: `wps:bodyPr vert='vert'`. NO-FORK extension once Draw Text Box exists.
- **Save Selection to Text Box Gallery** — Create New Building Block dialog (Name, Gallery=Text Boxes, Category, Description, Save in, Options Insert-content/own-paragraph/own-page). **[exists? no / works? no / feasible? only if a building-block store exists]**. OOXML: `w:docPart` in `glossary/document.xml` with `w:gallery w:val='txtBox'`. NEEDS-USER (building-block subsystem).
- *Contextual tab after insert/select:* **Shape Format (Drawing Tools)** — currently absent for shapes.

**Quick Parts**
- **Quick Part Gallery items + Save Selection to Quick Part Gallery** — user-saved building blocks gallery + Create New Building Block dialog (Name, Gallery=Quick Parts, Category, Description, Save in, Options). Shortcut **Alt+F3**. **[exists? no / works? no / feasible? needs store]**. OOXML: `w:docPart` `w:gallery w:val='quickParts'`. NEEDS-USER (building-block subsystem).
- **Field…** — Field dialog: Categories (All, Date and Time, Document Automation, Document Information, Equations and Formulas, Index and Tables, Links and References, Mail Merge, Numbering, User Information), Field names list, Field properties (Format), Field options (per-field switches + Add to Field), Preserve formatting checkbox, Field Codes/Options toggle. **[exists? shallow / works? for 6 fields / feasible? yes]** — current dialog is a fixed 6-item list (page/numpages/date/author/filename/title) → real `d.fields.insert`. OOXML: `w:fldSimple w:instr='DATE \@ ...'` or complex `w:fldChar begin/separate/end`. AUTO-FIX expanding to the categorized field list + switches NO-FORK over the existing `xeQuickPart`/`insertField` path. Shortcut **Ctrl+F9** = empty `{ }` braces (missing).
- **Building Blocks Organizer…** — manager: list (Name | Gallery | Category | Template), live preview, Insert / Delete / Edit Properties / Close. **[exists? no / works? no / feasible? needs store]**. OOXML: reads/writes `w:docPart` across glossary documents. NEEDS-USER (building-block subsystem) — there is nothing to organize without a store.
- **AutoText** — AutoText gallery + Save Selection to AutoText Gallery; F3 insert-by-name, Alt+F3 create. **[exists? no / works? no / feasible? needs store]**. OOXML: `w:docPart w:gallery w:val='autoTxt'`. NEEDS-USER (building-block subsystem).
- **Document Property** — submenu of 15 properties (Abstract, Author, Category, Comments, Company, Company Address/E-mail/Fax/Phone, Keywords, Manager, Publish Date, Status, Subject, Title) inserting databound content controls. **[exists? 1 of 15 / works? as plain field / feasible? partial NO-FORK]** — current code always inserts a DOCPROPERTY Title plain field. OOXML ideal: `w:sdt > w:sdtPr > w:dataBinding(w:xpath→docProps)`; clone has no content-control node, so AUTO-FIX = surface all 15 as DOCPROPERTY fields (honest, no databinding) until content controls exist.
- *Contextual tab:* none.

**WordArt**
- **WordArt gallery** — 15-thumbnail preset grid (Fill-text-color/no-outline, Fill+Outline shadow variants, Fill-White/Outline-Accent1/Shadow, Gradient-Fill accents, Pattern fills …); inserts "Your text here" placeholder. **[exists? 6 of 15 / works? yes / feasible? yes]** — `wordArtMenu` renders 6 CSS styles → real `insertWordArt` DrawingML. OOXML: `wps:wsp/wps:txbx` runs with `w14:textFill/w14:textOutline/w14:props3d`, transform via `wps:bodyPr a:prstTxWarp`. AUTO-FIX = add the missing 9 presets/names. Shortcut Alt > N > W. Raises **Shape Format**.
- **Edit WordArt text + apply effects** — Shape Format ▸ WordArt Styles: Text Fill, Text Outline, Text Effects (Shadow/Reflection/Glow/Bevel/3-D/Transform). Transform gallery (No Transform; Follow Path: Arch/Arch Down/Circle/Button; Warp: Triangle, Chevron, Inflate/Deflate, Slant, Fade, Wave, Double Wave, Cascade, Curve, Can, Stop, Ring …). **[exists? no / works? no / feasible? yes once tab exists]**. OOXML: `a:prstTxWarp prst`, `a:effectLst`, `w14:textFill/textOutline`. Blocked on the missing Shape Format contextual tab. Raises **Shape Format**.

**Drop Cap**
- **Drop Cap menu (None / Dropped / In margin)** — applies to active paragraph's first letter. **[exists? yes / works? yes / feasible? yes]** — `xeDropCap` writes real `w:framePr dropCap/lines/wrap/vAnchor/hAnchor`. OOXML: framed first-char paragraph, ST_DropCap = none|drop|margin. No contextual tab.
- **Drop Cap Options… (captured ✓)** — Position None/Dropped/In margin, **Font** dropdown, **Lines to drop** (default 3, min 2), **Distance from text** (measurement), OK/Cancel. **[exists? no / works? no / feasible? yes NO-FORK]** — ribbon-data.js:873 lists it but no dialog; `xeDropCap` hard-codes lines=3 and surfaces no font/distance. OOXML: same `w:framePr` with `w:lines`='Lines to drop', `w:hSpace`='Distance from text' (twips), drop-cap glyph font via `w:rFonts` + large `w:sz`. AUTO-FIX = add the dialog and thread font/lines/distance into `xeDropCap`.

**Signature Line**
- **Add a Signature Line > Microsoft Office Signature Line (captured ✓ — Signature Setup dialog)** — info/disclaimer dialog, then Signature Setup: Suggested signer, signer's title, signer's e-mail, Instructions, Allow comments, Show sign date, OK. Shortcut Alt > N > G. **[exists? dialog only / works? no / feasible? insert-graphic yes; e-sign no]** — current `xeSignatureLine` only toasts and returns; dialog inputs discarded. OOXML: `w:drawing > pic:pic` (or VML) carrying `o:signatureline` (suggestedsigner/2/email, signinginstructions, allowcomments, showsigndate, issignatureline='t'); a real digital signature lands in `_xmlsignatures/`. NEEDS-USER (signature-provider subsystem; the actual sign step needs a certificate/e-sign provider). Inserting the *visual* signature-line shape is a smaller NO-FORK slice but the "Sign" capability is the subsystem.
- **Stamp Signature Line** — East-Asian stamp variant, same Signature Setup. **[exists? no / works? no / feasible? same as above]**. Same `o:signatureline` construct. NEEDS-USER.

**Date & Time**
- **Date and Time dialog (captured ✓)** — ~17 format strings (date-only/time-only/combined), **Language** dropdown, Calendar type for some locales (Gregorian/Hijri/Lunar/Saka/Um-al-Qura), **Update automatically** checkbox **UNCHECKED by default** (unchecked → STATIC text run; checked → DATE/TIME field), **Set As Default** button, OK/Cancel. Shortcut Alt+Shift+D (DATE field) / Alt+Shift+T (TIME). **[exists? partial / works? field-only / feasible? yes NO-FORK]** — `xeDateTime` always inserts a DATE field and ignores the checkbox; format list is 6 fixed, no Language/Set-As-Default. OOXML: Update-OFF → literal `w:r/w:t`; Update-ON → `w:fldSimple w:instr='DATE \@ "..."'`. AUTO-FIX = honor the checkbox (static text vs field) + widen the format list; Language/Set-As-Default are secondary.

**Object**
- **Object… (Create New) (captured ✓ — Object dialog Create New tab)** — OLE 'Object type' list (Adobe Acrobat, Excel Worksheet/Chart, Bitmap Image, Word Document, PowerPoint Slide, Microsoft Equation …), 'Display as icon' + Change Icon…, OK/Cancel. Shortcut Alt > N > J. **[exists? no / works? no / feasible? no — needs OLE runtime]** — menu item is an inline toast. OOXML: `w:object > o:OLEObject(Type='Embed', ProgID, r:id→/word/embeddings/oleObjectN.bin)` + presentation image. NEEDS-USER (OLE/Office embedding runtime). Raises Picture Format / Drawing Tools on the embed frame.
- **Object… (Create from File) (captured ✓ — 'Create from File' tab)** — File name + Browse…, Link to file, Display as icon, OK. **[exists? no / works? no / feasible? embed-image partial; true OLE no]**. OOXML: embed `o:OLEObject Type='Embed'`; link `Type='Link'` external rel (LINK semantics). NEEDS-USER (OLE runtime).
- **Text from File…** — Insert File browser (file list, File name, Files of type), Range… (bookmark/cell), encoding/convert prompt, Insert | Insert as Link split. **[exists? shallow / works? plain insert / feasible? yes NO-FORK for plain; link=field]** — current `textFromFile` does open→HTML→paste only. OOXML: plain → merged inline `w:p/w:r/w:tbl`; link → `INCLUDETEXT` field. AUTO-FIX (small) = add Range/bookmark + "Insert as Link" (INCLUDETEXT via existing field path); fidelity ceiling is the docx→HTML conversion.

### Contextual tabs
- **Shape Format (Drawing Tools)** — Word raises this for any selected Text Box / Draw Text Box / WordArt (any `wps:wsp` shape). Groups: Insert Shapes | Shape Styles (Shape Fill/Outline/Effects + Format Shape) | WordArt Styles (Text Fill/Outline/Effects incl. Transform) | Text (Text Direction, Align Text, Create Link) | Accessibility (Alt Text) | Arrange (Position, Wrap Text, Bring Forward/Send Backward, Selection Pane, Align, Group, Rotate) | Size (Height, Width, Size/Layout launcher). **Clone: MISSING.** `state-sync.ts:282-285` only matches `selection.node.type.name==='image'`, so a `shapeTextbox`/`vectorShape` NodeSelection raises nothing. The target objects (text boxes, WordArt) already insert as real nodes — the tab is the missing piece. Needs: detect shapeTextbox/vectorShape selections in state-sync, a `shape-tools-pm.js` (mirroring picture-tools-pm.js), and ribbon-data groups (Shape Styles / WordArt Styles / Arrange / Size). This single tab covers both Text Box and WordArt selection.
- **WordArt Format / WordArt Styles** — in modern Word this is the WordArt Styles *group inside* Shape Format, not a separate tab. **Clone: MISSING** (folded into the Shape Format gap above).
- **Picture Format (Picture Tools)** — present in the clone (`picture-tools-pm.js`); Word also raises it for a selected embedded OLE Object's presentation image. Out of immediate Text-group scope but the same tab would serve Object-embeds once OLE exists.
- **Header & Footer Tools** — present in the clone; not raised by the Text group directly, but the same Date & Time / Field dialogs are shared inside header/footer editing.

### Fix plan
| gap | NO-FORK / fork | spec-kit / direct-TDD | bridge verb(s) | regression test | oracle check | severity | autonomy |
|-----|----------------|----------------------|----------------|-----------------|--------------|----------|----------|
| Date & Time "Update automatically" ignored (always a field) | NO-FORK | direct-TDD | extend `xeDateTime(fmt, {auto})` in insert-exotica.ts → field when auto, literal `insertText` when not | probe: auto=off inserts a `w:t` run (no fldChar), auto=on inserts a DATE field | Word: unchecked → static text, checked → DATE field (captured ✓) | high | AUTO-FIX |
| Drop Cap Options dialog (Font / Lines / Distance) missing; lines hard-coded 3 | NO-FORK | direct-TDD | thread `xeDropCap(kind, {lines, font, distance})` into the existing framePr write | probe: lines=4 → `w:framePr w:lines='4'`, hSpace set, glyph rFonts applied | Word framePr `w:lines`/`w:hSpace` round-trip (captured dialog ✓) | med | AUTO-FIX |
| WordArt gallery only 6 of 15 presets | NO-FORK | direct-TDD | reuse `xeWordArt` with 9 added preset style descriptors | probe: each of 15 named presets inserts WordArt DrawingML | Word 15-style gallery names | low | AUTO-FIX |
| Quick Parts > Field limited to 6 hard-coded fields | NO-FORK | spec-kit | reuse `xeQuickPart`/`insertField` with a categorized field list + switches | probe: insert from ≥1 field per category produces correct `w:instr` | Word Field dialog categories & instr strings | med | AUTO-FIX |
| Quick Parts > Document Property inserts Title only | NO-FORK | direct-TDD | `xeQuickPart('docproperty', {prop})` for all 15 properties (DOCPROPERTY fields, honest — no databinding) | probe: each property name inserts `DOCPROPERTY <name>` | Word submenu lists 15 properties | low | AUTO-FIX |
| Ctrl+F9 empty field braces `{ }` not implemented | NO-FORK | direct-TDD | new `xeFieldBraces()` inserting an empty complex field (begin/end fldChar) | probe: Ctrl+F9 inserts an empty editable field pair | Word: Ctrl+F9 → empty `{ }` | low | AUTO-FIX |
| Object > Text from File: no Range/bookmark, no Insert-as-Link | NO-FORK | direct-TDD | extend `textFromFile` + reuse field path for INCLUDETEXT | probe: Insert-as-Link emits `INCLUDETEXT "path"` field | Word INCLUDETEXT instr | low | AUTO-FIX |
| Text Box built-in gallery (~40 styled) + Draw Vertical | NO-FORK | spec-kit | `xeTextBox` with preset shape props; thumbnail menu | probe: a named built-in inserts a styled wps:wsp box | Word ~40-item gallery (captured ✓ ~40) | low | AUTO-FIX |
| Draw Text Box = rubber-band click-drag placement | NO-FORK (renderer interaction) | spec-kit | `xeTextBox` + a draw-mode placement layer | probe: drawn box uses the drag rect's `a:xfrm` size | Word crosshair draw geometry | med | AUTO-FIX |
| Shape Format (Drawing Tools) contextual tab absent for text-box/shape/WordArt selection | NO-FORK | spec-kit | detect shapeTextbox/vectorShape in state-sync.ts; `shape-tools-pm.js` + ribbon groups | probe: selecting a text box / WordArt raises Shape Format; selecting image still raises Picture Format | Word raises Shape Format for any wps:wsp | high | AUTO-FIX |
| Fix false docs claims (Signature ✅, Text Box ✅, Drop Cap ✅, Quick Parts ✅, Date & Time ✅) | NO-FORK | direct-TDD | n/a (docs only) | n/a | n/a — match repo evidence | high | AUTO-FIX |
| Signature Line is a toast stub behind a full dialog | NO-FORK insert-shape slice; e-sign = fork/subsystem | spec-kit | `xeSignatureLine` → insert real `o:signatureline` graphic | probe: insert writes an `o:signatureline` shape with signer attrs | Word `o:signatureline` attrs / Sign dialog | med | NEEDS-USER |
| Object > Create New / Create from File OLE embed | needs external OLE runtime | n/a | n/a | n/a | n/a | high | NEEDS-USER |
| Quick Parts building-block store (Save-to-gallery, AutoText, Building Blocks Organizer) | new subsystem (glossary docPart store) | spec-kit | new building-block store + `xeQuickPart` save/insert | n/a until subsystem | Word `w:docPart` glossary | med | NEEDS-USER |
| WordArt Text Effects / Transform (needs Shape Format tab first) | NO-FORK after tab | spec-kit | shape/run effect verbs (`a:prstTxWarp`, `a:effectLst`) | probe: Transform writes `a:prstTxWarp prst` | Word Transform presets | low | NEEDS-USER |


## Section 9 — Symbols
*Real-Word group:* Insert ▸ **Symbols** (the rightmost Insert-tab group: Equation split-button + Symbol dropdown) · *State:* Symbol is the strong half — the quick grid and More-Symbols dialog both write through the real bridge verb (`WC.PM.insertSymbol`), but the dialog has no Font selector, no code-point box, and no Special Characters tab; Equation is a styled-text approximation (`WC.PM.insertEquation` inserts Cambria-Math-italic TEXT, not OOXML `<m:oMath>`) with Ink Equation, Save-to-Gallery, and the Alt+= shortcut all unwired, and no Equation contextual tab.

### Feature table
| feature | cmd id | file:line | state | gap vs real Word |
|---|---|---|---|---|
| Equation — primary button / Insert New Equation | `equation` | `src/renderer/public/js/commands.js:428` | shallow | Inserts Cambria-Math-italic **styled text**, not a structured `<m:oMath>` math zone; no equation editor, no professional/linear, no contextual tab. |
| Equation — Built-in equation gallery (dropdown) | `equation` | `src/renderer/public/js/commands.js:509` | shallow | 5 hardcoded Unicode preset strings inserted as styled text vs Word's ~9–30 named built-ins as real math objects; no Save Selection to Gallery, no professional render. |
| Equation — Ink Equation… | `equation` | `src/renderer/public/js/commands.js:509` | missing | Declared in `ribbon-data.js:925` but never rendered by `equationMenu` and no handler exists; Word opens an ink-to-math recognition canvas. |
| Equation — Save Selection to Equation Gallery… | `equation` | `src/renderer/public/js/commands.js:509` | missing | Declared in `ribbon-data.js:926` but never rendered, no handler, no user gallery to save to; Word saves a reusable building block. |
| Equation — Alt+= professional / linear / convert shortcut | `equation` | `src/renderer/public/js/app.js:96` | missing | `ribbon-data.js:920` advertises Alt+= but app.js only binds Ctrl+=→zoomIn; no Alt+= equation-insert, no Professional/Linear/Convert toggles. |
| Symbol — recent-symbols quick grid (dropdown face) | `symbol` | `src/renderer/public/js/dialogs.js:67` | working | Fixed curated ~48-glyph grid, NOT a true persistent MRU; the only MRU is the dialog's sessionStorage list (lost on restart). |
| Symbol — More Symbols… dialog (subset map + recently used) | `symbol` | `src/renderer/public/js/insert-features.js:288` | working | No Font selector, no Unicode/ASCII code-point box, 6 fixed subset lists (not the full font map), sessionStorage MRU (non-persistent), no symbol-font run (Wingdings won't round-trip). |
| Symbol — Special Characters tab (em dash, NB hyphen, …) | `symbol` | `src/renderer/public/js/insert-features.js:288` | missing | Dialog is single-tab (Subset + grid + MRU); Word's named Special-Characters tab with shortcut keys is entirely absent. |

### Flows

**Equation — Insert blank equation / Insert New Equation** (`H.equation` → `WC.Dialogs.equation()` → `WC.PM.insertEquation`, bridge/insert.ts:177)
- *Insert blank equation (top half of split / Alt+=)* — Word inserts an empty "Type equation here" math-zone placeholder and switches into the equation editor, raising the **Equation** contextual tab; OOXML inline `<m:oMath>`, display `<m:oMathPara>`. **[exists? partial — top half opens a textarea+6 preset-chip dialog, not a math zone / works? yes-as-styled-text — inserts Cambria-Math-italic TEXT via insertContent (KNOWN DEVIATION, insert.ts:181), not `<m:oMath>` / feasible-in-clone? real `<m:oMath>` is a large NEW math subsystem (fork-gated, NEEDS-USER); wiring Alt+= to today's dialog is NO-FORK and feasible]**
  - *Shortcut Alt+= (alt: Alt+Shift+=)* — **(captured ✓)** owner saw Alt+= toggle an inline equation in real Word. **[exists? no — app.js:96 binds only Ctrl+=→zoomIn / works? no / feasible-in-clone? YES, NO-FORK — add an Alt+= branch in app.js that calls `H.equation`]**

**Equation — Built-in equation gallery (dropdown)** (`equationMenu`, commands.js:509-512 → `WC.PM.insertEquation`)
- *Open gallery and insert a built-in* — **(captured ✓)** owner saw a gallery of built-in equations + an "Insert New Equation" command. Word's built-ins: Area of Circle; Binomial Theorem; Expansion of a Sum; Fourier Series; Pythagorean Theorem; Quadratic Formula; Taylor Expansion; Trig Identity 1; Trig Identity 2; plus "Insert New Equation", "More Equations from Office.com", "Save Selection to Equation Gallery…". OOXML: `<m:oMathPara>/<m:oMath>` with structure children (`<m:f>`, `<m:rad>`, `<m:nary>`, `<m:sSup>/<m:sSub>`). **[exists? partial — 5 hardcoded preset strings + "Insert New Equation" / works? yes-as-styled-text — inserts Unicode strings as Cambria-Math-italic text / feasible-in-clone? expanding the named list + inserting styled text is NO-FORK; real math-object rendering is NEEDS-USER]**

**Equation — Ink Equation** (advertised `ribbon-data.js:925`, NOT rendered)
- *Ink Equation modal* — **(captured ✓)** owner saw "Ink Equation" in the gallery. Word opens a yellow ruled "Write math here" canvas with a live Preview and Write / Erase / Select-and-Correct / Clear / Insert / Cancel tools; Insert commits the recognized equation as normal OMML (ink discarded). **[exists? no — `equationMenu` never appends the item, no handler / works? no / feasible-in-clone? NO — handwriting-recognition canvas is a NEW external/ML subsystem, NEEDS-USER]**

**Equation — Save Selection to Equation Gallery** (advertised `ribbon-data.js:926`, NOT rendered)
- *Save as New Equation… → Create New Building Block dialog* — fields: Name, Gallery (default Equations), Category, Description, Save in (Building Blocks.dotx / Normal.dotm), Options (content only / own paragraph / own page); shortcut Alt+F3; stored as a `w:docPart`/`w:docPartObj` building block. **[exists? no — never rendered, no handler, no user gallery / works? no / feasible-in-clone? depends on a Building-Blocks store + a selectable math object — NEEDS-USER until equation objects exist]**

**Equation contextual tab — Tools / Conversions group** (no clone equivalent)
- *Professional / Linear / Normal Text / Convert ▾ / Unicode / LaTeX / Equation Options* — **(captured ✓)** owner saw the Equation contextual tab raise Symbols + Structures groups and noted professional/linear/convert; shortcuts Ctrl+= (build up) / Ctrl+Shift+= (build down). OOXML: Professional/Linear are display modes over the same `<m:oMath>`; Normal Text sets `<m:rPr><m:nor/>`. **[exists? no contextual tab at all / works? no / feasible-in-clone? NEEDS-USER — requires a selectable math zone + math editor]**

**Equation contextual tab — Symbols group** (no clone equivalent)
- *Math-symbol mini-gallery with category subsets*: Basic Math, Greek (Lower/Upper), Letter-Like, Operators (Common/Basic-N-ary/Advanced Binary/Relational), Arrows, Negated Relations, Scripts, Frakturs, Double-Struck, Geometry; each inserts a math run `<m:r><m:t>`. **(captured ✓)** owner confirmed the Symbols group on the Equation tab. **[exists? no / works? no / feasible-in-clone? NEEDS-USER — part of the math subsystem]**

**Equation contextual tab — Structures group** (no clone equivalent)
- *11 structure galleries*: Fraction, Script, Radical, Integral, Large Operator, Bracket, Function, Accent, Limit and Log, Operator, Matrix — each inserts an OMML template (`<m:f>`, `<m:sSup>`, `<m:rad>`, `<m:nary>`, `<m:d>`, `<m:func>`, `<m:acc>`, `<m:limLow>`, `<m:m>`). **(captured ✓)** owner confirmed the Structures group on the Equation tab. **[exists? no / works? no / feasible-in-clone? NEEDS-USER — the core of the math subsystem]**

**Symbol — dropdown quick grid + More Symbols** (`H.symbol` → `WC.Dialogs.symbol`, dialogs.js:67 → `WC.PM.insertSymbol`, bridge/insert.ts:166)
- *Recently-used grid* — **(captured ✓)** owner saw the recent-symbol grid in the flyout. Word shows the real MRU (up to ~20, pre-seeded € £ ¥ © ® ™ ± ≠ ≤ ≥ ÷ × ∞ µ α β π Ω ∑ …); click inserts a plain `<w:r><w:t>` run, NO contextual tab. **[exists? yes — 8-col ~48-glyph grid / works? yes — real insertContent collapse-to-end / feasible-in-clone? a true persistent MRU is NO-FORK (localStorage)]**
- *More Symbols…* — opens the full Symbol dialog. **[exists? yes — `WC.Insert.symbolDialog`, insert-features.js:288 / works? yes / feasible-in-clone? yes]**

**Symbol — More Symbols dialog, Symbols tab** (`WC.Insert.symbolDialog`, insert-features.js:288 → `WC.PM.insertSymbol`)
- **(captured ✓)** owner enumerated the real dialog: Font dropdown, Subset dropdown, character grid, Character-code box with `from:` Unicode(hex)/ASCII(dec/hex), Recently-used row, AutoCorrect… button, Shortcut Key… button. OOXML: plain `<w:r><w:t>` or legacy `<w:sym w:font=… w:char=…>` for symbol-font glyphs; Alt+X toggles a hex code in-document.
  - *Subset dropdown* — **[exists? yes — 6 fixed lists (Common/Arrows/Math/Greek/Currency/Dingbats) / works? yes / feasible-in-clone? expanding to real Unicode blocks is NO-FORK]**
  - *Character grid* — **[exists? yes / works? yes — cell click inserts via the bridge + pushRecent / feasible-in-clone? yes]**
  - *Recently-used row* — **[exists? yes — sessionStorage 'wc-recent-sym' / works? yes-but-non-persistent / feasible-in-clone? make persistent via localStorage, NO-FORK]**
  - *Font dropdown* — **[exists? NO / works? no / feasible-in-clone? YES, NO-FORK — add a font select + emit a symbol-font run so Wingdings/Webdings round-trip]**
  - *Character-code box + `from:` Unicode/ASCII* — **[exists? NO / works? no / feasible-in-clone? YES, NO-FORK — parse a hex/dec code point and insert the glyph]**
  - *AutoCorrect… button* — **[exists? no / works? no / feasible-in-clone? deferred — needs an AutoCorrect store; keep honest]**
  - *Shortcut Key… button* — **[exists? no / works? no / feasible-in-clone? deferred — needs a keyboard-customize subsystem]**

**Symbol — More Symbols dialog, Special Characters tab** (missing)
- **(captured ✓)** owner saw the Special Characters tab with em dash / en dash / nonbreaking hyphen / etc. and their shortcut keys. Word lists named typographic chars in a Character | Shortcut-key two-column list: Em Dash (Alt+Ctrl+Num−), En Dash (Ctrl+Num−), Nonbreaking Hyphen, Optional Hyphen, Em/En/¼-Em Space, Nonbreaking Space (Ctrl+Shift+Space), Copyright © (Alt+Ctrl+C), Registered ® (Alt+Ctrl+R), Trademark ™ (Alt+Ctrl+T), Section §, Paragraph ¶, Ellipsis … (Alt+Ctrl+.), curly quotes, No-Width Optional/Non Break. OOXML: mostly plain Unicode runs; Nonbreaking hyphen = `<w:noBreakHyphen/>`, Optional hyphen = `<w:softHyphen/>`, NB space = `<w:t xml:space="preserve"> </w:t>`, No-Width Optional Break = U+200B, No-Width Non Break = U+FEFF. **[exists? NO — `symbolDialog` builds one tab only / works? no / feasible-in-clone? YES, NO-FORK — add a second tab; em/en dash + copyright already insertable as Unicode; noBreakHyphen/softHyphen need bridge support]**

### Contextual tabs
- **Equation (modern) / Equation Tools | Design (Word 2016 and earlier)** — Word raises this whenever the caret enters a math zone (Insert ▸ Equation, Alt+=, a gallery equation, or a committed Ink Equation). Groups: **Tools** (mini gallery, Professional, Linear, Normal Text, Convert ▾ Unicode/LaTeX + to-Professional/to-Linear/All/Current, Equation Options launcher), **Symbols** (math-symbol mini-gallery with category subsets), **Structures** (11 template dropdowns). **(captured ✓)** owner confirmed the Equation tab raises Symbols + Structures groups. **Clone has NONE of this** — `ribbon.js:526` `showContextualTab`/`hideContextualTab` is wired only for Header & Footer Tools, Table Tools, and Picture Tools; `state-sync.ts` has no equation-context detection, and because equations are inserted as plain styled text there is no selectable math zone to detect. To add it the clone would need (a) a real math-zone node/object the cursor can be "inside", (b) `state-sync` detection that fires `showContextualTab('equation')`, and (c) the Tools / Symbols / Structures groups + their galleries. This is gated on the equation subsystem → NEEDS-USER.
- **Symbol** — raises **no** contextual tab in real Word; the clone correctly raises none. Nothing to add.

### Fix plan
| gap | NO-FORK / fork | spec-kit / direct-TDD | bridge verb(s) | regression test | oracle check | severity | autonomy |
|---|---|---|---|---|---|---|---|
| Symbol dialog: no Font selector (Wingdings/Webdings won't round-trip) | NO-FORK | direct-TDD | extend `WC.PM.insertSymbol(ch, {font})` (insert.ts:166) to emit a `w:rFonts`/`w:sym` symbol-font run | test:pm: insert a Wingdings glyph, assert exported run carries the symbol font / `<w:sym>` | COM read-back: open saved docx, assert the run's `.Font.Name == "Wingdings"` and glyph char | med | AUTO-FIX |
| Symbol dialog: no Character-code box + `from:` Unicode(hex)/ASCII | NO-FORK | direct-TDD | reuse `WC.PM.insertSymbol(ch)` (insert.ts:166) | test:pm: type "20AC"/Unicode → assert € inserted; "169"/ASCII-dec → © | COM: inserted char code-point matches the typed code | med | AUTO-FIX |
| Symbol dialog: Special Characters tab entirely missing | NO-FORK | spec-kit (small) | `WC.PM.insertSymbol` for plain chars; add `WC.PM.insertSpecialChar('noBreakHyphen'\|'softHyphen'\|'nbsp'\|'zwsp')` in insert.ts | test:pm: insert Em Dash, NB hyphen → assert `<w:noBreakHyphen/>`, optional hyphen → `<w:softHyphen/>`, NB space `xml:space="preserve"` | COM: read back special chars / open-and-repair clean | high | AUTO-FIX |
| Symbol quick grid + dialog MRU not persistent (sessionStorage) | NO-FORK | direct-TDD | none (UI/storage only) | unit: pushRecent then reload → MRU survives (localStorage) | n/a (UI persistence) | low | AUTO-FIX |
| Equation Alt+= shortcut unwired (only opens nothing) | NO-FORK | direct-TDD | `WC.PM.insertEquation` (insert.ts:177) | test:pm: simulate Alt+= → assert `H.equation` fires / equation dialog opens | manual: Alt+= inserts an equation (parity with captured ✓ behavior) | med | AUTO-FIX |
| Equation gallery: 5 fixed presets vs Word's named built-ins | NO-FORK | direct-TDD | `WC.PM.insertEquation(text)` (insert.ts:177) | test:pm: assert the gallery lists the named built-ins (Quadratic Formula, Pythagorean Theorem, …) and each inserts text | n/a (styled-text approximation; honest naming) | low | AUTO-FIX |
| Equation: styled text, not real `<m:oMath>` (no math editor) | fork (NEW math subsystem) | spec-kit (large) — SPIKE-FIRST | NEW `WC.PM.insertMathZone` / OMML export path | suite: round-trip `<m:oMath>`/`<m:oMathPara>` through the converter | COM: `Range.OMaths.Count`, build-up form matches | high | NEEDS-USER |
| Equation contextual tab (Tools/Symbols/Structures) absent | fork (depends on math subsystem) | spec-kit (large) | `showContextualTab('equation')` via state-sync equation detection | test:pm: caret in equation → equation tab shown/hidden | COM: equation editing parity | med | NEEDS-USER |
| Ink Equation… (advertised, not rendered, no engine) | fork / external | NEEDS-USER decision | n/a | n/a | n/a | low | NEEDS-USER |
| Save Selection to Equation Gallery… (advertised, not rendered) | fork (Building Blocks store) | NEEDS-USER decision | n/a (would reuse a Quick-Parts/Building-Blocks store) | n/a | COM: building-block persisted | low | NEEDS-USER |
| False docs: INSERT_TAB.md / FEATURES.md claim Equation+Symbol fully working | NO-FORK (docs only) | direct edit | none | n/a | n/a | med | AUTO-FIX |


## Section 10 — E-signature
*Real-Word group:* Insert ▸ Text ▸ **Signature Line** split control (native Microsoft Office Signature Line) + the Adobe Acrobat **Esignature** add-in group (non-native). · *State:* Picker UI is complete but the document write is a **no-op toast** — `xeSignatureLine()` writes nothing and discards the dialog values; no real signature-line object, no signing pipeline, no contextual surface. Native part is AUTO-FIXable to a real VML/OOXML object; the cryptographic signing + Adobe provider are NEEDS-USER.

### Feature table
| feature | cmd id | file:line | state | gap vs real Word |
|---|---|---|---|---|
| Signature Line (main button — opens Signature Setup dialog) | `signatureLine` | src/renderer/public/js/commands.js:483 | stub | Word inserts a real signature-line graphic (signer/title/email/instructions/allow-comments/show-date) that round-trips; clone shows a dialog then a toast and inserts nothing. |
| Signature Line dropdown arrow (split — 'Microsoft Office Signature Line…', 'Add Signature Services…') | `signatureLine` | src/renderer/public/js/commands.js:1859 | stub | Word's split offers the MS Office signature line (real object) and a provider marketplace; clone surfaces neither as a menu item and routes both to the same no-op dialog. |

### Flows

**Microsoft Office Signature Line (Insert ▸ Text ▸ Signature Line) — captured ✓**
- **Add a Signature Line → 'Signature Setup' dialog** *(captured ✓: dialog has Suggested signer, Title, E-mail, Instructions, 'Allow the signer to add comments', 'Show sign date'; inserts a signature-line graphic; OOXML = `w:object` + VML `o:signatureline`/`issignatureline='t'`)* — **[exists: yes (dialog UI) / works: NO (OK → `xeSignatureLine()` no-op toast, values discarded) / feasible-in-clone: YES, NO-FORK]**. The placeholder graphic (X over a line, signer name/title beneath) is an inline VML `<w:pict>/<v:shape>/<o:signatureline …>` wrapped in `mc:AlternateContent` with a DrawingML `<w:object>` fallback. The clone can emit the VML `o:signatureline` object directly via the bridge (insert-exotica.ts) — it is bounded body content, no external runtime needed.
  - Dialog field — **Suggested signer:** **[exists / collected but discarded / feasible: YES — map to `o:suggestedsigner`]**
  - Dialog field — **Suggested signer's title:** **[exists / discarded / feasible: YES — `o:suggestedsigner2`]**
  - Dialog field — **Suggested signer's e-mail address:** **[missing from clone dialog / feasible: YES — `o:suggestedsigneremail`]**
  - Dialog field — **Instructions to the signer** (default 'Before signing the document, verify that the content you are signing is correct.') **[missing / feasible: YES — `signinginstructions` + `signinginstructionsset='t'`]**
  - Dialog field — **Allow the signer to add comments** (checkbox) **[missing / feasible: YES — `allowcomments='t'|'f'`]**
  - Dialog field — **Show sign date in signature line** (checkbox) **[missing / feasible: YES — `showsigndate='t'|'f'`]**
  - Gallery item — **Microsoft Office Signature Line** (default top item) **[exists as plain string ribbon-data.js:884, no distinct handler / works: NO / feasible: YES]**
  - Gallery item — **Add Signature Services…** (Office marketplace for 3rd-party providers) **[exists as plain string ribbon-data.js:885, no menu entry / works: NO / feasible: keep honest stub — provider marketplace is external]**
  - Gallery item — **Stamp Signature Line** (East-Asian builds only) **[N/A for en-US target — omit]**
  - Shortcut — no dedicated hotkey; ribbon KeyTip **Alt, N,** then Signature-Line split-button **[feasible: KeyTips are generated; not load-bearing]**
  - Contextual tab — **NONE** raised on insert; selecting the inline placeholder shows the standard Picture/Shape Format tab **[feasible: clone already has Picture Tools — could be wired to the inserted object]**
  - OOXML: `<w:r><w:pict><v:shape …><v:imagedata o:title=…/><o:lock v:ext='edit'/><o:signatureline v:ext='edit' issignatureline='t' id='{GUID}' provid='{0…0}' o:suggestedsigner='…' o:suggestedsigner2='…' o:suggestedsigneremail='…' signinginstructions='…' signinginstructionsset='t' allowcomments='t' showsigndate='t'/></v:shape></w:pict></w:r>`, `mc:AlternateContent` Fallback `<w:object><w:drawing>…<a15:signatureLine …></w:drawing></w:object>`; placeholder image stored as media via `v:imagedata/r:id`.

- **Sign the signature line (Sign dialog)** — **[exists: NO / works: NO / feasible-in-clone: NO — needs a cryptographic signing subsystem (digital ID / XML-DSig)]**. Double-click → Sign; type printed name or 'Select Image…', optional 'Purpose'/'Commitment Type', 'Signing as <name> Change…', 'Details…', Sign. OOXML: adds OPC parts `/_xmlsignatures/origin.sigs`, `/_xmlsignatures/sig1.xml` (XML-DSig/XAdES with Office `SignatureInfoV1`: `SetupID` = the line GUID, `SignatureText`, `SignatureImage`, `SignatureComments`), package rel of type `…/digital-signature/origin`, and a `digital-signature+xml` content-type override; marks the doc Final/read-only. Contextual surface = the **Signatures task pane** (not a ribbon tab) + Message Bar banner.

- **View / manage signatures (View Signatures)** — **[exists: NO / works: NO / feasible-in-clone: NO — depends on the signing subsystem above]**. File ▸ Info ▸ Protect Document ▸ View Signatures, or the Message Bar, opens the Signatures pane (Requested/Valid sections; per-item Sign / See Signature Details / Remove Signature / Signature Setup). OOXML: removing a signature deletes the matching `sigN.xml` and (if last) drops the digital-signature parts + content-type override and leaves Final; editing the placeholder rewrites the `o:signatureline` attributes.

**Adobe Acrobat eSignature group (add-in) — NOT native Word**
- **Get Signatures / Request signatures (Adobe Acrobat Sign)** — **[exists: NO (no such ribbon group in clone) / works: NO / feasible-in-clone: NO — external Adobe cloud runtime + Office add-in manifest]**. Insert ▸ Esignature ▸ 'Get Signatures' launches the Adobe task pane: Adobe sign-in, recipients/signing order, message, field-placement canvas (Signature/Initials/Date/Text), Send. Variants: 'Fill & Sign', 'View signed PDF'. OOXML: **none** — no `.docx` body construct; the artifact is a PDF agreement in Adobe's cloud, declared via an Office Add-in manifest (`<OfficeApp>/VersionOverrides`) or a COM add-in. Contextual surface = the add-in's own task pane (COM variant adds a top-level 'Acrobat' tab). The clone has no Adobe group (only 'DocuSign' as an inert label in the Add-ins store list, commands.js:902).

### Contextual tabs
- Real Word raises **NO ribbon contextual tab** for this group. The native UI is the right-hand **Signatures task pane** (sections: Requested Signatures / Valid Signatures) plus a document **Message Bar** banner; selecting the inserted placeholder shows the ordinary **Picture/Shape Format** Drawing Tools tab (it is an inline graphic).
- The **Adobe Acrobat Sign** surface is an add-in task pane (COM variant adds a top-level **'Acrobat'** tab), not a Word-native contextual tab.
- **Clone has:** none of these. `state-sync.ts` has no signature context detection; `ribbon.js showContextualTab` is wired only for Header & Footer, Table Tools, Picture tools.
- **Missing tab(s):** there is no *required* signature ribbon tab to add (Word raises none). The honest target is a **Signatures task pane** (a non-ribbon panel) — NEEDS-USER, gated on the signing subsystem. If the inserted placeholder is selectable, it should reuse the existing **Picture Tools** contextual tab (no new tab needed).

### Fix plan
| gap | NO-FORK / fork | spec-kit / direct-TDD | bridge verb(s) | regression test | oracle check | severity | autonomy |
|---|---|---|---|---|---|---|---|
| `xeSignatureLine()` is a no-op toast — insert a REAL signature-line VML object instead, carrying all six Signature Setup values | NO-FORK | direct-TDD (small spec if VML emit is novel) | replace `WC.PM.xeSignatureLine()` (insert-exotica.ts:181) with a real `insertSignatureLine({signer,title,email,instructions,allowComments,showDate})` writing `<w:pict>/<o:signatureline>` via insert.ts | scripts/test-suite-pm.js: insert sig-line → assert `o:signatureline issignatureline='t'`, `o:suggestedsigner`, `allowcomments`, `showsigndate` round-trip through the PM converter | Word COM: open the saved `.docx`, read `Shapes`/`SignatureSet` or unzip and assert `o:signatureline` attributes == authored (signer/title/email/allowcomments/showsigndate) | high | AUTO-FIX |
| Signature Setup dialog is missing E-mail, Instructions (with default text), Allow-comments, Show-date fields; collected Signer/Title are discarded | NO-FORK | direct-TDD | `WC.Insert.signatureLine()` (insert-features.js:219-227) → pass all fields to the new bridge verb | scripts/test-suite-pm.js: dialog→bridge plumbs all 6 fields (no value dropped) | unzip saved docx: `signinginstructions`/`signinginstructionsset='t'` present when Instructions set | high | AUTO-FIX |
| Split dropdown items ('Microsoft Office Signature Line…', 'Add Signature Services…') are inert strings with no menu/handlers | NO-FORK | direct-TDD | wire `Commands.dropdown` (commands.js:1859) → a real `WC.Insert` menu: item 1 = the Signature Setup flow; item 2 = honest 'provider marketplace unavailable' notice | test: dropdown exposes 2 distinct items; item 1 inserts the object, item 2 is an explicit stub | n/a (menu wiring) | med | AUTO-FIX |
| False docs claim: INSERT_TAB.md:56 marks Signature Line ✅ (works) but it is a stub | NO-FORK | direct edit | n/a | n/a — doc-only | n/a | low | AUTO-FIX |
| Sign / View-Signatures pipeline (digital ID, XML-DSig/XAdES OPC parts, Final/read-only, Signatures task pane) | fork/new subsystem | NEEDS-USER decision | none (no document-body verb; needs a crypto signing + OPC-parts subsystem) | n/a until scoped | Word COM: verify `Signatures.Count`/validity after signing | high | NEEDS-USER (recommend: keep honest — insert the placeholder object now, defer signing; surface signing as an explicit stub) |
| Adobe Acrobat 'Esignature' group / 'Get Signatures' (no native OOXML — external Adobe cloud + Office add-in manifest) | new external runtime | NEEDS-USER decision | none | n/a | n/a (no .docx construct) | low | NEEDS-USER (recommend: remove from ribbon / do not add — not native Word; or keep a single honest add-in-entry stub) |


