# Layout-Engine (Paged Render Migration) — Execution Runbook

> **The single live doc for EXECUTING the Option-B layout-engine migration.** It holds
> (1) the locked decisions, (2) how we work (the per-milestone loop), (3) the milestone
> checklist, and (4) the **Current Status** (bottom) — where we are + what's next.
>
> **A new session resumes by reading this file top-to-bottom and continuing from
> "Current Status".** Keep the **Current Status** section up-to-date EVERY session
> (this is part of the `plan-tracking` discipline). [plan.md](plan.md) /
> [last-point.md](last-point.md) / [execution-map.md](execution-map.md) point HERE for
> the layout-engine execution state, so this is the one place that must not go stale.

**Pointers**
- Spec (the WHAT): [../../specs/001-paged-render-migration/spec.md](../../specs/001-paged-render-migration/spec.md) — US1–US4, FR-001..013, SC-001..007
- What's already proven + the retarget surface: [../layout-engine-standup-findings.md](../layout-engine-standup-findings.md)
- Memory: `layout-engine-redesign`, `merge-mode-ff`

## How to start a session (resume here)

> A new session can begin by reading THIS file and following the steps below — no external
> prompt needed. (One-liner that's enough to paste: *"Read docs/plan/layout-engine-runbook.md
> and follow it; start from Current Status."*)

1. Confirm you are on the `layout-engine` branch: `git branch --show-current`.
2. Read this runbook in full + orient via the **Pointers** above (spec, standup findings, memory). Then read **[Current Status](#current-status--keep-this-up-to-date-every-session)** (bottom) and continue from there.
3. Follow the **per-milestone loop** below EXACTLY. Non-negotiable guardrails:
   - **ONE milestone at a time.** Do not run ahead.
   - **PLAN FIRST:** run `/speckit-plan` scoped to the **CURRENT milestone ONLY** (not all 6 — spec-kit defaults to the whole feature; constrain it). Ask the user clarifying questions, present the plan, and **WAIT for approval. No code before approval.**
   - **VERIFY before "done":** `npm run build` + the 3 gates (overlay/default MUST stay **268 / 9 / 27**) + COM-oracle validation + **`/code-review`**. If `/code-review` finds ANYTHING, **FIX it and re-verify** — never proceed with unresolved findings.
   - When verified AND the user is satisfied: **ff-merge the slice into `layout-engine`**, **update Current Status** below, then go to the next milestone.
   - **Never touch `main`.** Keep the model **page-free**. All new render behind `WC_LAYOUT=paged`.

## Locked decisions

- **Methodology:** GitHub **spec-kit** — `/speckit-specify` → `/speckit-plan` → `/speckit-tasks` → `/speckit-implement` (+ optional `/speckit-clarify`, `/speckit-analyze`).
- **Scope = A:** umbrella spec covers ALL milestones; detailed design is produced **per slice** via `/speckit-plan` as each is reached (not all up front).
- **End-state = A:** paged becomes the faithful **default** later, via a **separate gated flip** (all milestones green + full COM-oracle parity). The flip is a **non-goal** of this work; default stays overlay throughout.
- **Branch model:** long-lived **`layout-engine`** integration branch off `main`. Each slice branches off `layout-engine` and **ff-merges back into `layout-engine`**. `layout-engine` merges to **`main`** ONLY when the whole migration is complete + parity-proven.
- **Commit cadence:** consolidated — commit at **slice boundaries** (feature/fix/slice done), NOT per doc tweak. Fewer, coherent commits.
- **Strangler-fig:** all new render work lives behind `WC_LAYOUT=paged`; the default (`overlay`) is the **byte-identical shipping app** (the regression gate).
- **Invariant:** the document model stays **page-free** (no `page` nodes) so it keeps round-tripping to `.docx`. Never break this.

## How we work — the per-milestone loop

Repeat for each milestone, ONE at a time (never run ahead):

1. **PLAN** — run `/speckit-plan` for THIS milestone only. Ask clarifying questions FIRST, then present the plan (architecture, files, contracts, approach) and **WAIT for the user's approval before writing any code.**
2. **TASKS** — `/speckit-tasks`: break the approved plan into an ordered task list.
3. **IMPLEMENT** — on a slice branch off `layout-engine`; build incrementally behind `WC_LAYOUT=paged`; the default (overlay) must stay byte-identical.
4. **VERIFY (mandatory before "done"):**
   - `npm run build`, then the 3 gates — `npm run test:pm`, `npm run test:smoke`, `npm run test:roundtrip` — all green (overlay/default must stay **268 / 9 / 27**).
   - Validate the milestone's behavior against the **Word-for-Windows COM oracle** (`scripts/oracle/word-oracle-win.ps1`) where applicable.
   - Run **`/code-review`** on the slice diff. **If it surfaces ANY problems/findings, FIX them**, then re-run build + the 3 gates (and re-validate) before moving on. **Do not proceed with unresolved findings.**
5. **CHECK IN** — show the user the results. Only after the user is satisfied: **ff-merge the slice into `layout-engine`**, **update the Current Status section below**, then move to the next milestone.

## Milestones (order C) — checklist

- [x] **M1** — shared per-page coordinate adapter + dynamic-import of the paged path ✓ DONE 2026-06-19
- [x] **M2** — pointer click hit-test routing into the hidden inner editor ✓ DONE 2026-06-19
- [x] **M3** — status bar → `presentation.getPages()` ✓ DONE 2026-06-20
- [x] **M4 COMPLETE (2026-06-20)** — all 6 overlays retargeted/disabled for the painted per-page DOM, across M4a–M4d. **M4a** comments-ui + track-chrome; **M4b/.1** image-resize + live preview; **M4c** ink-overlay (the SPIKE showed in-app ink ALREADY worked in paged — `#pm-editor` IS the visible paint host — so M4c fixed the multi-page `.docx` EXPORT: `clientToOverlayLocalPt`/`overlayPageBox` + page-local `posOffset` + re-anchor to the draw page + forward-first render page resolution); **M4d** notes-area DISABLED in paged (PE paints footnotes per-page at the foot, editable inline — the overlay was a double-render; 2-point gate in notes-area.ts; `refShowNotes` scrolls to the painted footnote) + header-footer = a paged integration TEST (proven mode-agnostic, ZERO production change). **Carry-forwards (all out of scope, documented):** M4b.1 rotated/flipped drag-preview cosmetic; M4c docx-REOPENED multi-page stroke re-render (pre-existing `customGeometry.inkPos`-recovery gap, both modes); M4d header-session LIVE interop test (no public PE activation API — parity proves the verbs).
- [ ] **M5** — paged-mode `.docx` export COM-oracle round-trip parity
- [ ] **M6** — glyph-metric tolerance vs the Word COM oracle
- [ ] **(later, separate)** flip the default to paged + retire the overlay (gated on all of M1–M6 + full parity)

---

## Current Status  ⟵ KEEP THIS UP TO DATE (every session)

- **Last updated:** 2026-06-20
- **Branch:** `layout-engine` (off `main` @ 7f15724) — **local, kept off origin until the migration endgame.** M1 + M2 + M3 + M4a + M4b + M4b.1 + M4c + M4d + **M5** ff-merged in → **M5 COMPLETE** (+ the `test:pm` flaky-abort fix @ `904335c`). M1–M4d docs archived under `specs/001-paged-render-migration/milestones/`.
- **Done so far:**
  - Root-caused the old engine (decoration overlay) + chose Option B (adopt SuperDoc's real layout engine).
  - Vendored the 10 engine packages; build-proven; **standup spike PASSED** (real per-page DOM, pagination 1→12, model page-free, caret/typing) — see findings doc.
  - Installed spec-kit; wrote the umbrella spec (scope A, milestone order C, end-state A).
  - **M1 COMPLETE (2026-06-19):** shared `WC.PM.coords` coordinate adapter (`src/renderer/layout/coordinate-adapter.ts`, installed once in `preinstallBridge`) + dynamic-import of the PE path (overlay bundle code-split back to ~8.16 MB). Pure infra — NO consumer rewired (that's M2/M3/M4). New `scripts/paged-coords-probe.js` (overlay 9/9 parity + paged 10/10 round-trip Δ=0/0) and `scripts/check-overlay-bundle.js` gate (`test:bundle`, 4/4). `/code-review` xhigh → 5 findings fixed + re-verified.
  - **M2 COMPLETE (2026-06-19):** pointer click hit-test routing. Root cause = `focus.ts`'s margin-click handler clobbered PE's already-complete click pipeline in paged mode (it ran `view.posAtCoords` on the hidden off-screen view). Fix = `focus.ts` bails in paged mode (PE's `EditorInputManager` owns all paged clicks) + migrated its overlay hit-test to `WC.PM.coords.clientToPos` (first real consumer of the M1 seam). 3-line product change; no fork edits. New `scripts/paged-pointer-probe.js` (`probe:pointer`): paged 13/13 — single-click correct on page 0 **and page 1 (Δ=0)**, double-click word-select, focus + insert follow; **triple-click + drag are PE-native and INFO-only** (synthetic DOM events can't replicate native multi-click/drag detection — our change provably can't break them). Overlay parity 6/6. `/code-review` high → 4 probe-honesty gaps fixed + re-verified. Real-app gesture verification was BLOCKED (computer-use can't target the CLI-launched dev Electron build via the installed-app allowlist; "Word" title collides with MS Word) → user approved merge on the probe evidence.
  - **M3 COMPLETE (2026-06-20):** status bar → real page count. In paged mode the bar showed "Page 1 of 1" (both X/Y read the overlay `__pagination`, null in paged). Fix = added `WC.PM.coords.getCurrentPage()` (paged: `PE.computeCaretLayoutRect(caret).pageIndex+1`; overlay: the break-scan moved verbatim) + rewired `statusbar.js` to read `getPageCount()`/`getCurrentPage()` (never `__pagination` directly) + a paged `presentation.onLayoutUpdated → StatusBar.update` trigger (idempotent) for no-transaction re-paginations. No fork edits. New `scripts/paged-statusbar-probe.js` (`probe:statusbar`): paged 11/11 ("Page 1 of 8" → "Page 2 of 8" tracking the caret; trigger fires) + overlay parity 9/9 (9-page doc, page 1→9 advance, render byte-identical). `/code-review` high → 4 fixes (onLayoutUpdated unsub leak; probe trigger hard-gated; overlay parity made non-tautological; statusbar simplified).
  - **M4a COMPLETE (2026-06-20):** overlay retarget — comments-ui + track-chrome to the painted pages. Added `WC.PM.coords.posToOverlayLocalY` + `posToOverlayLocalRect` (verbatim ports of the overlays' `localY`/`localRect` with the only change `view.coordsAtPos` → the M1 `posToClientRect`; `#pages`'s own scale resolves the zoom baseline → mode-agnostic, overlay byte-identical). comments-ui/track-chrome `localY`/`localRect` now delegate to the seam; both listen to a new `wc:paged-relayout` window event dispatched from the (leak-hardened) `onLayoutUpdated` handler. No fork edits. `scripts/paged-overlays-probe.js` (`probe:overlays`): paged 10/10 (painted-aware Δ=278px vs hidden view; relayout fires) + overlay parity 7/7 (byte-identical 6/6 each). `/code-review` high → 5 fixes (onLayoutUpdated teardown moved outside the guard = leak + stale-dispatch fix; independent handler try/catch; 3 probe-honesty gaps).
  - **M4b COMPLETE (2026-06-20):** image-resize overlay → painted image. Research: PE's `ImageInteractionLayer` draws NO handles (only drag metadata) → RETARGET (not disable). Added `WC.PM.coords.nodeBoxFor` (overlay: `view.nodeDOM` — byte-identical; paged: `editor.getElementAtPos` → the painted image via `[data-pm-start]` index) + `overlayScale()` (ONE scale source). image-resize `boxFor`→`nodeBoxFor`, `zoom()`→`overlayScale`, + `wc:paged-relayout` listener. **KEY discovery (probe-first): the `ImageResize` plugin was never installed in the paged PE** (standup excluded it with Pagination) → added it to `constructPresentationEditor` (it's an independent NodeSelection overlay, verified safe — no measuring/pagination; NOT Pagination). `scripts/paged-image-resize-probe.js` (`probe:imageresize`): paged 10/10 (handle overlay on the painted image **Δ=0px**; painted-aware Δ=10303px) + overlay parity 10/10. `/code-review` high → 4 fixes (overlayScale no-#pages fallback → WC.PM.zoom; 3 probe de-flake/honesty). (computer-use can't verify the live drag — blocked for the dev build; verified by a synthetic-drag probe instead.)
  - **M4b.1 COMPLETE (2026-06-20):** live image resize PREVIEW on the painted image. The M4b carry-forward — the drag preview wrote to the HIDDEN image node, so in paged the painted image snapped to size only on release — is RESOLVED. `onHandleDown` now also captures `paintedDom = WC.editor.getElementAtPos(pos)` (paged; null in overlay) and `onDragMove` previews onto BOTH the editable node and the painted image via a new `applyPreviewSize` helper, so the image resizes DURING the drag. Overlay byte-identical (paintedDom null → no-op; the helper == the deleted inline write). Probe strengthened: measures the inner `<img>` (not just the wrapper) live, asserts the commit HOLDS the dragged-to size after pointer-up, try/finally pointer-up. paged 12/12 (live `<img>` 120→200 + commit holds 200) + overlay 10/10. `/code-review` high → product cleared (no post-commit/mid-drag staleness — PE rebuilds the fragment on commit); 3 probe-honesty fixes. **New minor cosmetic (out of 4b's aspect-locked scope):** a ROTATED/flipped image's drag preview can look mis-sized (wrapper geometry transform not recomputed mid-drag; self-corrects on pointer-up) — fold into M4c if wanted.
  - **M4c COMPLETE (2026-06-20):** ink-overlay. **The probe-first spike OVERTURNED the premise:** in paged `#pm-editor` is the VISIBLE paint host (fills `#pages`, offset 0,0), so the ink overlay's `localPt`/`sizeLayer` already resolve to `#pages`-local → **in-app capture/render/erase/select/lasso ALREADY worked on all pages.** The ONE real defect was the multi-page `.docx` EXPORT: the persisted `pos` is `#pages`-local but `synthesizeInkDrawing` writes it as `relativeFrom='page'` `posOffset` → page-2+ strokes saved at the wrong on-page offset/page. Fix (all paged-gated; overlay byte-identical): added `WC.PM.coords.clientToOverlayLocalPt` (page under the pointer) + `overlayPageBox` (painted page origin); `onDown` resolves the draw page once + re-anchor docPos (`clientToPos`) + pointer capture; `onUp` persists PAGE-LOCAL `pos` + re-anchors the node onto the draw page (`dInsertInk` insertPos → `TextSelection.near`); `renderInk` re-adds the node's page origin via FORWARD-FIRST `computeCaretLayoutRect(nodePos+[1,2,3,-1,-2,-3,0])` (a top-of-page stroke resolves its OWN page); single-page short-circuit + debounced `wc:paged-relayout`. No fork edits. `scripts/paged-ink-probe.js` (`probe:ink`): paged 15/15 (capture page 0/1 + page-2 top-anchor boundary regression; eraser-by-identity; `inkPos` page-local; exported `posOffsetV` page-local EMU) + overlay parity 7/7. `/code-review` xhigh → page-resolution asymmetry (backward-scan resolved the prior page for top-of-page strokes) + re-anchor `.create`-throws fixed + re-verified; efficiency (single-page short-circuit + relayout debounce) + probe-honesty fixes. **Carry-forward (pre-existing, out of scope):** re-render of a docx-REOPENED multi-page stroke needs the importer to recover `customGeometry.inkPos` (page-local), which it doesn't (both modes). **Spike (deleted) findings recorded in research.md.**
  - **M4d COMPLETE (2026-06-20):** notes-area DISABLE + header-footer TEST — the last 2 overlays. **Spike: PE DOES paint footnote bodies per-page at the page foot (`"1"@942 | "ZZMARKERFOOT"@942`) AND the overlay rendered them too** → a double-render. So disable the `#pm-notes-area` overlay in paged (2-point gate in `notes-area.ts`: `renderInner()` hides+tears-down the region; `installNotesArea()` skips the per-keystroke driver — the `WC.NotesArea` facade stays defined as guarded no-ops). `references.ts` `refShowNotes()` paged branch scrolls to the painted footnote (Word's "jump to notes"; iterate notes, skip empty, prefer the page-foot match). **header-footer = PURE TEST, ZERO production change** — `header-footer.ts` is mode-agnostic (4 doc-model verbs, no coords; binds to `presentation.editor`). `scripts/paged-notes-probe.js` (`probe:notes`): paged 12/12 (PE-painted footnote + overlay absent + Show-Notes scroll + forced-render gate holds + endnotes) + overlay 9/9. `scripts/paged-headerfooter-probe.js` (`probe:headerfooter`): paged 15/15 (set/get + `word/headerN.xml`+`sectPr headerReference`+rels + REPLACE) + overlay 15/15. `/code-review` → `refShowNotes` hardened (3 findings) + 2 probe checks strengthened (force-render → exercise the gate) + hf-baseline export-throw closed. **Carry-forward:** the LIVE PE header-session interop test is deferred (no public activation API; fork hook out of scope — parity proves the verbs).
  - **M5 COMPLETE (2026-06-20):** paged `.docx` export ↔ Word-for-Windows COM-oracle PARITY. **Dev-box-only TEST INFRA — NO `src`/fork change** (M5 only VALIDATES the existing, mode-independent export path: `exportDocxBytes`→`editor.exportDocx()` serializes ONLY `state.doc`; PE never overrides it). Built the **Node→Word-COM bridge** (`scripts/oracle/com-validate.js` — `spawnSync powershell -File validate-*-win.ps1` SANDBOX-DISABLED, PID-safe via each ps1's spawned-PID kill, JSON-stdout; the validators were HAND-RUN before) + hardened `validate-open-win.ps1` to pass `OpenAndRepair:=false` (positional arg 13) so a SILENT auto-repair surfaces as `ok=false` (was: only an INTERACTIVE repair). New `scripts/test-roundtrip-paged.js` (`test:roundtrip:paged`): builds paged then overlay (ENDING leak-free on overlay), runs a kitchen-sink probe (`paged-export-m5-probe.js`: image+comment+track+footnote+endnote+header/footer) + a multi-page ink probe (`paged-export-m5-ink-probe.js`), then via REAL Word COM: validate-open all 3 saved `.docx` (no repair) + per-construct read-back (comments/notes/header-footer == seeded M5* markers) + **TIER-1 UNZIP-THE-SAVED-BYTES paged-vs-overlay structural equality** (binary `word/media/*` BYTE-equal; xml id/timestamp-normalized with a STABLE per-value remap preserving rId-bridge/paraId referential integrity; same-part-set + mask-hit-symmetry honesty guards) + **TIER-2 ink** Word-valid + page-local `posOffset` on the correct page + PID-safety (no leaked WINWORD). **19 pass / 0 fail** on the dev box. **Determinism finding:** exportDocx is deterministic EXCEPT comment timestamps/ids + drawing ids (the masked set) — confirmed by diffing a paged-vs-overlay export of the identical edit (mask hits symmetric 22/22, 0 diff after normalize). `/code-review` xhigh → fixed the soundness gap (was diffing a re-export `getUpdatedDocs` partsMap that OMITS `word/media` + nulls `commentsIds` → now unzips the actual saved `.docx`, 20 parts incl. byte-compared media), the rId-bridge single-placeholder integrity loss (→ stable remap), `parseLastJson` result-shape guard, + mode assertions. **`test:roundtrip:paged` is DEV-BOX-ONLY / excluded from headless CI** (Word COM hangs at `New-Object` in a sandbox; needs real foreground Word run sandbox-disabled). **OUT OF SCOPE:** the docx-REOPENED multi-page ink import gap (no export→reopen leg). M5 spike (deleted) findings in research.md.
  - **M6 COMPLETE (2026-06-21):** glyph-metric tolerance vs the Word-for-Windows COM oracle — **REPORT-ONLY** (measures the divergence + SETS the tolerance; not a pass/fail gate). **Dev-box-only VALIDATION INFRA — NO `src`/fork change.** No tolerance number existed (FR-009/SC-005 said "the agreed tolerance"; the spike was never run), so M6 quantifies it. THE GAP: M1–M5 proved the engine is self-consistent + exports correctly, never that its TYPESETTING matches Word. New `scripts/paged-glyphgeom-probe.js` (paged, REAL renderer — headless = the mock-canvas stub; since paged CANNOT reopen a `.docx` [replaceEditor/openDocx/newBlank permanently lose the PE], it authors+measures IN-SESSION per font via `chain().selectAll().clearNodes().insertContent(TEXT).run()` [the PE survives transactions] + `setFontFamily/Size/Align`, reads each painted `.superdoc-page .superdoc-line` `getBoundingClientRect`, exports the SAME live model) + `scripts/oracle/validate-glyphgeom-win.ps1` (PID-safe skeleton; **SELF-VERIFIES the `Range.Information()` enum ints in-run** — empirically PINNED X-page=5, Y-page=6, line#=10; asserts line-1 X≈leftMargin AND line-2 Y>line-1 Y; iterates `doc.Words` for per-line wrap+X/Y in points) + `scripts/paged-glyphgeom-validate.js` (`report:glyphgeom`; reuses `com-validate.js`; aligns PE↔Word by LINE SEQUENCE + punctuation-normed first word — `data-pm-start` is a PM pos, NOT Word's char offset; px→pt ×0.75; per-font distribution). **RESULT (15 Office fonts + justified + multipage):** wrap-point agreement **100% (0 mismatch)**, per-line start-X **0.75pt = 1px grid floor for ALL**, per-line Y **p95 ≤ 4.91pt (Segoe UI worst; most ≤1.7pt)**, single-page page-count **exact** → the paged engine's typesetting matches Word remarkably well on body text. **The one divergence:** the multipage giant-paragraph fixture paginated **PE=2 vs Word=3 pages** (same 98 lines) — flagged for a CALIBRATION follow-up (the `measuring-dom/src/index.ts:139-143` systematic-offset hook), out of report-only M6. Tolerance WRITTEN into `contracts/m6-glyph-tolerance.md`. `report:glyphgeom` emits `ok=true enumVerified fontVerified scaleVerified pidSafe`. `/code-review` xhigh → fixed the font-application HONESTY hole (the read-back: `applied` only checked for a throw, but a no-op returns false → would measure+export the DEFAULT font on both sides → false agreement; now reads the painted run's computed font-family + a `fontVerified` gate), `build('overlay')`-in-`finally` (was happy-path-only → could leave a paged build), the enum single-line hole (require ≥2 lines so Y is actually disambiguated), `total=max` wrap denominator + `firstWord===''` guard + a `scaleVerified` (zoom≠1.0) check. Also pinned: PowerShell vars are CASE-INSENSITIVE (`$ln`/`$LN` collision silently broke line detection — renamed `$curLine`). **`report:glyphgeom` is DEV-BOX-ONLY / REPORT-ONLY / NOT a gate** (needs the real renderer + sandbox-disabled Word COM). **Aptos (Word 16's default) was NOT canvas-detected** — likely not installed on this box; the 15 detected Office fonts are the set.
  - **PAGED OPEN/NEW FIX (2026-06-21, slice `slice/paged-open-new`):** unblocks the paged-default flip. **Discovered during M6:** in paged mode the app could NOT open a `.docx` or `newBlank` — `replaceEditor` (`src/renderer/bridge/index.ts`) was mode-blind: it `current.destroy()`'d the PE's hidden inner editor + `mountEl.innerHTML=''`'d the painted `.superdoc-page` DOM + rebuilt a plain (unpaged) editor + left `w.WC.presentation` dangling → no page ever repainted (open/new DEAD in paged). **Fix (bridge-only, NO fork edit):** a paged branch in `replaceEditor` (inside the Phase-2 try) loads the doc INTO the live PE via the fork's `Editor.replaceFile(source)` (super-converter → a `replaceWith(0,size,newDoc)` transaction the PE's update listener repaints IN PLACE) — preserving the PE instance, mountEl, bridge closures, `presentation`, the relayout listeners + all 6 overlays. Spike-proven: openDocx/newBlank repaint in **50ms**, content swaps, `WC.presentation` SAME object (in-place, not reconstructed), bridge + export work. html/text/csv legs (which pass `blankArrayBuffer()`+`extra.html`) → `replaceFile(blank)` + `insertContent(html)`. `scripts/paged-opennew-probe.js` (`probe:opennew`): paged 15/15 + overlay parity 12/12; the 4 core gates unchanged (overlay path BYTE-unchanged). `/code-review` xhigh → fixed 2 HIGH: (a) **data-loss** — the html/text/csv legs returned `true` even when `insertContent` silently failed → `files.js` bound the path → a later save overwrote the original with a blank; now mirrors the overlay §5.3 guard (`lastImportBlanked=true`+return false if the markup had text but nothing landed); (b) **wrong catch scope** — the branch was in the Phase-1 try, so a `replaceFile` throw hit the "live editor untouched" catch + bypassed `failBridge` (but `replaceFile` is NOT validate-before-mutate); moved inside the Phase-2 try. **Follow-up (noted, not blocking):** the paged html legs use `insertContent`'s weaker html parser (vs overlay's `createDocFromHTML`) → possible list/table fidelity divergence on .html/.txt/.csv import; docx-open + newBlank (the common paths) are full-fidelity.
  - **PAGED-DEFAULT FLIP (2026-06-21, slice `slice/paged-default-flip`) — FR-013, the migration's PRIMARY GOAL.** Paged-readiness assessment first: the FULL suite passes in paged (`test:pm` 475/475, `test:smoke` 9/9, `test:roundtrip` 27/0, `test:bundle` 4/4) + all milestone paged probes green → paged is functionally ready. User confirmed a real-app smoke ("ok it works"). **The flip = a one-line default change:** `main.ts` `layoutMode` fallback `'overlay'` → `'paged'` (+ the `electron.vite.config`/`create-editor.ts` comments). **Paged is now the DEFAULT**; overlay stays reachable via `WC_LAYOUT=overlay npm run build` or a `localStorage('WC_LAYOUT','overlay')` override during the transition. Verified: default build → `__WC_LAYOUT_MODE='paged'` (page painted); `WC_LAYOUT=overlay` build → `'overlay'`; the 4 core gates PASS against the paged default. The PE chunk stays code-split (`test:bundle` 4/4 — entry under the ceiling, PE dynamic-imported). Self-reviewed (one-line flip + comments; grep confirms no other code hardcodes the overlay default — the only `'overlay'` hits are a comments-ui card-placement type + an EditorInputManager CSS `overflow` value). **This also fixes the parked overlay drag-from-gap bug for free** (overlay-only; paged has no decoration spacers).
  - **`main` PUSH + DOC RECONCILIATION (2026-06-21) — the migration is SHIPPED.** User chose "reconcile docs first, then push." (a) **Doc reconciliation** (`7af5590`): a parallel finder sweep across the 8 live orientation docs found 31 stale "overlay-default / multi-page-is-future" statements → fixed CLAUDE.md/AGENTS.md/PAGINATION.md/ARCHITECTURE.md/FEATURES.md/SCOPE.md/LAYOUT_ENGINE.md (TECH_STACK clean). Feature-level claims kept conservative — engine shipped = the blocker is gone + multi-page (#1) / image-resize (#2) verified-live; per-feature command wiring described as "being reconciled", not "done". (b) **The push:** final gates green on the paged default (pm 475/smoke 9/roundtrip 27/bundle 4), then `git checkout main && git merge --ff-only layout-engine` (26 commits, `7f15724..7af5590`) + `git push origin main`. `main == origin/main == layout-engine` @ `7af5590`.
- **Where we are:** **🚢 THE PAGED-RENDER MIGRATION IS COMPLETE & SHIPPED TO MAIN — M1…M6 + open/new + the paged-default FLIP + doc reconciliation done + PUSHED to `origin/main` @ `7af5590` (26 commits).** `main == origin/main == layout-engine`; paged is the shipping default; the migration is OVER. Active spec/plan/tasks (M6) in `specs/001-paged-render-migration/`; M1–M5 archived under `milestones/`. **`test:pm` 268-abort RESOLVED (@ `904335c`):** a load-sensitive race (the `[0b] New Document` `newBlank()` lacked the `sleep(100)` settle the 8 other sites have); back to **475/475** repeatably. **Gates baseline (now paged-default): pm 475 · smoke 9 · roundtrip 27 · bundle 4 + `test:roundtrip:paged` 19 + `report:glyphgeom` (dev-box-only) + per-milestone paged probes.** **`test:pm` 268-abort RESOLVED (@ `904335c`):** it was a load-sensitive race — the `[0b] New Document` test's `newBlank()` lacked the `// blank-template IO settle` `sleep(100)` that the 8 other `newBlank()` sites have, so under machine load (many concurrent probes) its deferred doc-replace raced the next ([10mm]) test and silently aborted the [10mm]/[4a] tail (suite still returned a partial 268, 0 fail). Added the settle → **test:pm back to 475/475/0-fail repeatably** (not a product regression — stash-verified). **NEXT = the migration ENDGAME (M6 done).** **Gates baseline: pm 475 · smoke 9 · roundtrip 27 · bundle 4 + `test:roundtrip:paged` 19 (dev-box-only) + `report:glyphgeom` (dev-box-only, report-only) + per-milestone paged probes.**
- **⚠️ GATE CORRECTION + 002 P1 (2026-06-21, branch `feature/headers-footers-paged`):** the migration's "paged gates 475" above was a **FALSE GREEN** — `localStorage WC_LAYOUT` overrides the build define in `main.ts` (proven), and the shared `wc-probe-profile` carried a stale `'overlay'`, so the flip's `test:pm` runs were actually OVERLAY. **`test:pm` is an OVERLAY-rendering suite** (~70 tests read overlay-only `PM.__pagination` / `#pm-notes-area` / overlay DOM); **genuine paged `test:pm` = 405/475** deterministically. FIXED (`0f828a7`): a boot-mode guard pins `test:pm` to overlay (`WC_LAYOUT=overlay npm run build` → **475**; a paged build → 1 loud "build overlay" failure, not 70 cryptic ones). Paged rendering is covered by the `probe:*` probes. Follow-up: port the ~70 overlay-only tests to paged-aware variants for real paged-suite coverage. **002 P1 DONE (`2f8d709`):** on-page header/footer editing + the "Header & Footer Tools" tab, fully no-fork; `/code-review` (52-agent) → 4 fixes (off-screen entry, materialize-clobber-race, focus-steal, stuck-tab-on-open/new); gates green (test:pm **overlay** 475, paged HF 23/23, opennew 15/12, smoke 9, roundtrip 27, bundle 4); 0 regressions. Memory: [[paged-testpm-overlay-suite]].
- **002 P2 DONE (2026-06-21, `feature/headers-footers-paged`):** Different First Page + Different Odd & Even header/footer variants, fully NO-FORK. **Bridge** (`bridge/header-footer.ts`): `setHeaderText/getHeaderText` (+footer twins) take `opts.variant∈{default,first,even}`+`opts.section` (back-compat: no opts ⇒ default/0), routed through the existing story-runtime `HeaderFooterSlotStoryLocator.variant` — WRITE = `effective`+`materializeIfInherited` (materializes the variant's local part), READ = `explicit` for first/even (no inheritance bleed to the primary). `setDifferentFirstPage`→`editor.doc.sections.setTitlePage({enabled,target})` (sectPr `w:titlePg`); `setDifferentOddEven`→`sections.setOddEvenHeadersFooters({enabled})` (settings `w:evenAndOddHeaders`); `getHeaderFooterOptions`→`sections.get({address}).{titlePage,oddEvenHeadersFooters}`. **UI:** an "Options" group on the H&F Tools contextual tab (`header-footer-tools-pm.js`) — `differentFirstPage`/`differentOddEven` toggles whose checked state is painted from `getHeaderFooterOptions()` (`refreshOptionToggles`, DOM-queried so it survives hide→show); `commands.js` handlers flip+re-sync; un-deferred in `bridge/index.ts` `ENGINE_READY`. NOTE: the H&F Tools tab is runtime-injected (NOT `gen.js`), so the toggles live in `header-footer-tools-pm.js` (the tasks.md gen.js step was vestigial — Word exposes these only in the contextual tab). **The paged PE paints the first-page variant LIVE** (probe asserts page-1 band reflects the first variant). **Probe** `paged-headerfooter-probe.js` paged **42/42** / overlay 35/35 (parity; the 7 extra are paged-only on-page/paint checks). **Oracle**: new `paged-export-hf-p2-probe.js` + hardened `validate-headerfooter-win.ps1` (`OpenAndRepair:=false` + `.Index` enum self-check + `PageSetup.DifferentFirstPage/OddAndEven` flags + `Headers(2/3)/Footers(2/3).Range.Text`) wired into `test:roundtrip:paged` → **30/0** — all 8 P2 read-backs == authored vs **real Word for Windows** (DFP/odd-even flags + primary/first/even header & footer text, opens-without-repair). `/code-review` high (40-agent, 32 candidates → 1 kept) → fixed (a) fail-safe out-of-bounds section index (`sectionAddressAt` no longer falls back to section 0), and (b) a **pre-existing** gate bug surfaced by the rerun: the gate's `build('overlay')` set `WC_LAYOUT=''` which post-FR-013 boots PAGED (`main.ts:35` fallback) → made explicit `'overlay'`. Gates: pm **475** overlay / smoke 9 / roundtrip 27 / bundle 4 / `test:roundtrip:paged` **30**.
- **002 P3 DONE (2026-06-21, `feature/headers-footers-paged`):** page-number `PAGE` fields in the header/footer, fully NO-FORK. **Bridge** (`header-footer.ts`): `insertPageNumber({position:top|bottom|current})` runs `editor.doc.fields.insert({at, instruction:'PAGE', mode:'raw'})` on the SLOT STORY editor → a REAL OOXML PAGE field (fldChar begin + instrText PAGE + separate + end) in `word/headerN.xml`/`footerN.xml` (NOT static text); `removePageNumbers({position?})` clears only bands holding a PAGE field (`slotHasPageField` guard — never wipes a header title). A headless-safe `ensureInlineTarget` mints an `sdBlockId` when the freshly-materialized part's paragraph has none (the K-risk-1 hazard) so `fields.insert`'s target resolves. **UI:** Page Number dropdown on the H&F Tools tab + `H.pageNumber` flyout (Top/Bottom/Current/Remove) in `commands.js`; un-deferred `pageNumber` in `ENGINE_READY` (repointed the two D6 guards `[0a]`/`[11]` in `test-suite-pm.js` to still-deferred reps `columns`/`docInfo`). **Probe** `paged-headerfooter-probe.js` paged **52/52** / overlay 45/45. **Oracle**: new `paged-export-hf-p3-probe.js` + `validate-headerfooter-win.ps1` `Get-PageField` (wdFieldPage=33) wired into `test:roundtrip:paged` C3 → **36/0** — real Word reads the footer as a live `wdFieldPage` (type 33, code PAGE) whose result is a number (Word resolves it per page). **KNOWN LIMITATION (fork-gated, recorded):** the paged PE does NOT resolve a freshly-inserted footer field's number IN-APP (renders the empty `resolvedNumber` as "0"); Word + a reopen render correctly — the COM oracle (C3) carries the per-page-correctness proof. `/code-review` high (36-agent) → 1 fix (the K-risk-1 mint-and-retry) + 1 by-design (Word's "Current Position" body-caret is out of the header/footer P3 scope). Gates: pm **475** overlay / smoke 9 / roundtrip 27 / bundle 4 / `test:roundtrip:paged` **36** / `probe:headerfooter` paged 52 + overlay 45. **🏁 002 headers-footers P1+P2+P3 COMPLETE.** Remaining: P3's in-app PAGE resolution (fork-gated) + the Phase-6 doc reconciliation.
- **003 columns P1 DONE (2026-06-21, `feature/columns-paged`):** multi-column page layout, fully NO-FORK. Picked as the next feature after a feasibility probe (the paged engine renders columns + exports `w:cols`). **Bridge** `src/renderer/bridge/columns.ts` (NEW): `setColumns({count, gap?, equalWidth?})`→`editor.doc.sections.setColumns({count, gap(in), equalWidth, target})` (markDirty; NO_OP=success; count:1 normalizes to single); `getColumns()`→`sections.get().columns` (count-less `w:cols` import handled). **UI:** rewired `commands.js` `columnsMenu`/`setColumns(n)`→`WC.PM.setColumns({count})` + the More Columns dialog OK→`WC.PM.setColumns({count,gap})` (DELETED the dead `E()`/CSS-multicol legacy); un-deferred `columns` in `ENGINE_READY`; repointed the **three** D6 guards (`[0a]`×2 run+dropdown, `[11]`) off `columns`→`breaks` (the cascade lesson: a missed `[0a]` left a stale `columnsMenu` flyout open → 8 unrelated dropdown-content tests failed; caught by the overlay `test:pm`). **Probe** `scripts/paged-columns-probe.js` (`probe:columns`) paged **15/15** / overlay 12/12 (count 2→`[400,736]`, 3→`[400,624,848]`, gap 1"→`w:space="1440"`, count 1→single). **Oracle**: new `validate-columns-win.ps1` (`PageSetup.TextColumns.Count/EvenlySpaced/Spacing`, PID-safe, OpenAndRepair:=false) + `paged-export-columns-probe.js` → `test:roundtrip:paged` C4 → **42/0** (real Word: `TextColumns.Count==2`, `EvenlySpaced`, no repair). `/code-review` high (36-agent) → 1 fix (getColumns count-less robustness). **NEXT: 003 P2** (More Columns: spacing/equalWidth + Left/Right unequal + line-between — spike-bounded) → **P3** (column break), tasks T015–T029. Gates: pm 475 / smoke 9 / roundtrip 27 / bundle 4 / `test:roundtrip:paged` 42 / `probe:columns` paged 15 + overlay 12.
- **003 columns P2 DONE (2026-06-21, `feature/columns-paged`):** More Columns customize, NO-FORK. **Spike** resolved the no-fork reach: count/spacing/equalWidth via the public `sections.setColumns`; **line-between (`w:cols/@w:sep`)** + **unequal Left/Right (`<w:col>` children)** via an OWNED write to `editor.converter.bodySectPr`'s w:cols (NOT a fork-source edit; spike-proven to persist + round-trip); **column break** = `hardBreak{lineBreakType:'column'}` (P3). **Bridge** `columns.ts`: `setColumns` extended (lineBetween + `unequal:'left'|'right'`) as a **partial update** (a preset click / lineBetween-only toggle no longer clobbers prior sep/equalWidth); `bodyColsEl` = DIRECT-CHILD only (not a `w:sectPrChange`-nested w:cols); honest-fail when the body w:cols isn't reachable; `getColumns` reads lineBetween. **UI:** More Columns dialog + Equal-width / Line-between checkboxes; Left/Right presets → `setColumns({unequal})`. **Probe** `probe:columns` paged **35/35** / overlay 32/32 (incl. regression: sep survives a preset, equalWidth survives a lineBetween toggle). **Oracle**: `validate-columns-win.ps1` +LineBetween +per-column Width; `paged-export-columns-p2-probe.js` (Left+line-between) → `test:roundtrip:paged` C5 → **48/0** — real Word: `EvenlySpaced=false`, `LineBetween=true`, `columnWidths=[131.75,300.25]pt` (= **Word's exact Left preset 1.83"/4.17"**), opens without repair. `/code-review` high (39-agent) → 7 findings, fixed the 5 confirmed (partial-update clobber ×3, direct-child, honest-fail). **KNOWN (v1, recorded):** the owned bodySectPr write is outside PM history (undo best-effort) + a later body-section op can reset it (review #6); in-app paint of the separator/unequal split is best-effort (export + Word correct). **NEXT: 003 P3** (column break, T021–T026). Gates: pm 475 / smoke 9 / roundtrip 27 / bundle 4 / `test:roundtrip:paged` **48** / `probe:columns` paged 35 + overlay 32.
- **003 columns P3 DONE (2026-06-21, `feature/columns-paged`) → 🏁 003 COLUMNS COMPLETE:** the column break, NO-FORK. `insert.ts` `insertColumnBreak()` = `editor.chain().insertContent({type:'hardBreak', attrs:{lineBreakType:'column'}})` → a real `<w:br w:type="column"/>` (the hardBreak node already carries `lineBreakType` — spike-verified); `insertLineBreak()` = a plain text-wrapping break. **Un-deferred `breaks`** (the Layout→Breaks dropdown) → rewired its flyout OFF the retired `E()`: Column→`insertColumnBreak`, Text Wrapping→`insertLineBreak`, the local `insertPageBreak` helper→`WC.PM.insertPageBreak`, Continuous→honest toast; repointed the **three** D6 guards `breaks`→`lineNumbers`. **Probe** `probe:columns` paged **39/39** / overlay 36/36 (export carries `w:type="column"`). **Oracle**: `validate-columns-win.ps1` +columnBreakPresent (Word char 14) folded into the P2 doc + `test:roundtrip:paged` C5 → **49/0** (real Word sees the column break). `/code-review` (focused) → clean (Breaks path E()-free; `lineNumbers` still deferred). **Polish:** docs reconciled (SCOPE/FEATURES/LAYOUT_ENGINE Columns ✅). **003 columns P1+P2+P3 COMPLETE.** Gates: pm 475 / smoke 9 / roundtrip 27 / bundle 4 / `test:roundtrip:paged` **49** / `probe:columns` paged 39 + overlay 36. **NEXT: 004+** (overlay-retirement / residual-isBlocked-layout [position/align/group/rotate/lineNumbers/hyphenation] / paged-test-coverage / M6-gate+pagination-calibration / import-fidelity — user picks).
- **NEXT (post-migration cleanup/hardening — the migration is shipped; no active endgame):** (1) **Overlay retirement** — remove the decoration-overlay pagination engine + the `WC_LAYOUT=overlay` path once the transition is settled (also deletes the parked drag-from-gap bug's code). (2) **Per-feature layout reconciliation** — the `isBlocked`/`notifyBlocked` gates on layout-page / layout-arrange / header-footer / text-effects are now RESIDUAL (the engine shipped); un-block + wire each against the paged engine, validating vs the Word-COM oracle. (3) **M6 tolerance → pass/fail gate** + the multi-page pagination calibration (PE 2 vs Word 3 pages — the `measuring-dom/src/index.ts:139-143` systematic-offset hook). (4) **Paged html/text/csv import fidelity** (the import legs use `insertContent`'s lighter parser vs overlay's `createDocFromHTML`). Pick from this backlog with the user — these are independent, not a sequenced endgame.
- **Open questions:** none blocking. Carry-forward: triple-click/drag in paged mode are PE-native and unverified-by-automation — worth a real-app spot-check when a controllable run is available (not blocking).
- **Gates baseline:** **test:pm 475 — OVERLAY ONLY** (`WC_LAYOUT=overlay npm run build`; the suite validates the overlay engine, a boot-guard enforces it — genuine paged = 405/475 by design, see the GATE CORRECTION above) / smoke **9** / roundtrip **27** (both mode-agnostic) / `test:bundle` **4** + the per-feature paged `probe:*` probes (the real paged coverage) + `test:roundtrip:paged` 19 + `report:glyphgeom` (dev-box-only).
