{
  "summary": "Analyze each Home-tab Word feature: real behavior + concrete implementation plan for the contenteditable clone",
  "agentCount": 8,
  "logs": [],
  "result": {
    "specs": [
      {
        "feature": "Font dialog (Ctrl+D / Home > Font group dialog-box-launcher): a real modal character-formatting dialog with a Font tab and an Advanced tab, plus a live preview, applied to the current selection as a single CSS span.",
        "wordBehavior": "In MS Word, the Font group launcher (or Ctrl+D) opens a tabbed modal \"Font\" dialog reflecting the formatting of the current selection (or the caret's typing attributes), with a live Preview box at the bottom and a [Set As Default] / [Text Effects…] / OK / Cancel footer.\n\nFont tab:\n- Font (family) list with a search/edit box + scrolling list. Picks the typeface.\n- Font style: a 4-value list — Regular, Italic, Bold, Bold Italic — driving font-weight + font-style together.\n- Size: edit box + scrolling list of point sizes.\n- Font color: dropdown swatch picker (Automatic + theme + standard + More Colors).\n- Underline style: dropdown — (none), single, words only, double, dotted, dashed, wavy, thick, etc.\n- Underline color: swatch picker, enabled only when an underline style is chosen.\n- Effects checkboxes: Strikethrough, Double strikethrough, Superscript, Subscript, Small caps, All caps, Hidden (and in real Word also Outline/Shadow/Emboss/Engrave which we omit). Superscript and Subscript are mutually exclusive; Small caps and All caps are mutually exclusive.\n- Preview: a sample line (\"The quick brown fox…\" or selected text) rendered with all chosen attributes live.\n\nAdvanced tab (Character Spacing):\n- Scale: percentage (200%/150%/100%/90%/80%/66%/…) horizontally stretches glyphs.\n- Spacing: Normal / Expanded / Condensed + a \"By:\" point value → adds/removes inter-letter tracking.\n- Position: Normal / Raised / Lowered + a \"By:\" point value → raises/lowers text baseline without changing size.\n- (Real Word also has Kerning and an OpenType Ligatures/Number forms block — we omit/stub these.)\n\nOK applies every attribute at once to the selection; Cancel discards. \"Set As Default\" persists as the document default (we scope to a session default for new typing). Hidden text is not shown/printed (toggled by Show/Hide ¶ in real Word).",
        "uiNeeded": "A modal dialog (WC.dialog, width ~540px) titled \"Font\" containing a reusable two-tab strip (\"Font\", \"Advanced\") built with the existing .tabs/.t/.t.active CSS already in dialogs.css.\n\nFONT TAB body (grid layout):\n- Row: \"Font:\" search input (filters list) + a scrolling list box (div, max-height ~150px, overflow auto) populated from WC.FONTS; \"Font style:\" list box (Regular/Italic/Bold/Bold Italic); \"Size:\" number input + scrolling size list from WC.SIZES.\n- Row: \"Font color:\" a swatch button that opens WC.colorPalette in a WC.flyout; \"Underline style:\" a <select> (none/single/words/double/dotted/dashed/wavy/thick); \"Underline color:\" swatch button (disabled until a style != none).\n- \"Effects\" fieldset: 7 checkboxes — Strikethrough, Double strikethrough, Superscript, Subscript, Small caps, All caps, Hidden (two columns).\n\nADVANCED TAB body:\n- \"Scale:\" <select> (200/150/100/90/80/66/50%). \"Spacing:\" <select> (Normal/Expanded/Condensed) + \"By:\" number (pt). \"Position:\" <select> (Normal/Raised/Lowered) + \"By:\" number (pt).\n\nPREVIEW (shared, below the tab bodies): a bordered box (~70px tall, centered) whose inner span's style is recomputed on every control change via a single buildStyle() function — same function used on OK.\n\nFOOTER: [Set As Default] (left-aligned, optional), [OK] (primary), [Cancel]. Reuse existing .btn / .btn.primary and WC.dialog footer.",
        "implementationPlan": "Add D.font in dialogs.js and wire it from the launcher + Ctrl+D. Concrete steps:\n\n1) WIRING.\n- commands.js: split the launcher from the combo. Keep H.font for the launcher opening the dialog, and make the font-family combo flyout use a separate path. Simplest: add `H.fontDialog = () => WC.Dialogs.font();` and change the launcher control's cmd to \"fontDialog\" in ribbon-data.js (line ~236), OR detect the launcher in H.font: `H.font = (c, node) => (c && c.id === 'home.font.font-2') ? WC.Dialogs.font() : openFontList(node);` — the launcher passes the control object with id 'home.font.font-2', while comboDropdown calls openFontList directly. Use the id check to avoid touching ribbon-data.js.\n- app.js line 72: replace the stub with `if (k === 'd') return () => WC.Dialogs.font();`.\n\n2) READ CURRENT STATE (prefill). In D.font, before building, capture E().queryState() and the caret/selection's computed style via getComputedStyle on the selection's anchor element (mirror commands.js currentInlineStyle()). Derive: family (st.computedFontFamily or queryCommandValue fontName), sizePt (st.computedFontSizePt), bold/italic (st.bold/st.italic → style list selection), color (computed color → rgbToHex), underline (st.underline + textDecorationStyle), strike (st.strikethrough), sub/super (st.subscript/st.superscript), small/all caps (fontVariant/textTransform), hidden (display:none ancestor / data-hidden), and advanced (letterSpacing, transform scaleX, verticalAlign/position). Add a small WC-local rgbToHex(rgb) helper in dialogs.js.\n\n3) buildStyle() → returns a style object/string from all controls:\n- fontFamily: chosen family.\n- fontSize: size + 'pt'.\n- fontWeight: 'bold' if style is Bold/Bold Italic else 'normal'.\n- fontStyle: 'italic' if Italic/Bold Italic else 'normal'.\n- color: chosen hex (omit if Automatic).\n- textDecorationLine: join of ['underline' if underline style!=none] + ['line-through' if strikethrough or doubleStrike]; textDecorationStyle: 'double' when Double strikethrough OR when underline style is double; for distinct under+strike use textDecoration shorthand. textDecorationColor: underline color when set.\n- Effects: superscript→verticalAlign:super;fontSize:smaller (or use exec('superscript')); subscript similarly; smallCaps→fontVariant:'small-caps'; allCaps→textTransform:'uppercase'; hidden→add class 'hidden-text' (CSS: .hidden-text{display:none} when not in Show/Hide mode) and data-hidden=\"1\".\n- Advanced: letterSpacing from Expanded(+By pt)/Condensed(-By pt); transform/display:inline-block + transform:scaleX(scale/100) for Scale (note: inline transforms need inline-block, so wrap with display:inline-block when scale!=100); verticalAlign offset via position:relative; top:(Lowered +By / Raised -By)pt for Position.\n\n4) PREVIEW: keep one preview span; an update() handler attached (change/input/click) to every control calls Object.assign(previewSpan.style, buildStyle()) and toggles caps classes; preview text = selected text (sel.toString()) or 'AaBbCc — The quick brown fox'.\n\n5) APPLY (OK): focus editor, restore E().savedRange (capture it at dialog open since opening the modal blurs the editor — store `const saved = E().savedRange` and restore before applying). Then apply as ONE span to avoid nested-span churn:\n   - If selection is non-collapsed: build a span, set span.style via buildStyle(), surroundContents/extractContents like applyInlineStyle does, set caps classes + data-hidden. Implement a local applyFontSpan(styleObj, classes) in dialogs.js (modeled on Editor.applyInlineStyle but applying a whole style object) — or extend editor.js with Editor.applyInlineStyles(obj) that does the same wrap with Object.assign(span.style,obj). Recommended: add Editor.applyInlineStyles(styleObj, classList) to editor.js (one new method) so the wrap logic stays in one place; the Font dialog calls it once.\n   - If collapsed caret: same zero-width-span path as applyInlineStyle so subsequent typing inherits, and remember as session default.\n   - For super/subscript specifically, prefer E().exec('superscript')/('subscript') first (so it toggles cleanly and queryState reflects it), then apply remaining props via the span.\n   - After apply: E().dirty=true; E().repaginate(); E().updateStatus(); E().emit(); WC.Ribbon.setComboValue('font',family); WC.Ribbon.setComboValue('fontSize',String(size)); WC.Ribbon.setColorBar('fontColor',color).\n\n6) Color pickers: reuse WC.colorPalette inside WC.flyout anchored to the swatch button; onPick sets the swatch background + a local var and calls update(). Use opts {automatic:true,autoLabel:'Automatic'} for font color and {noColor:true} for underline color.\n\n7) CSS: add to dialogs.css a small block: a .font-dlg grid, .font-list/.size-list (border, max-height:150px, overflow:auto, .opt.sel highlight), .fx-grid (2-col checkbox grid), .preview-box (border, height:70px, display:flex, center). Add to editor.css: .hidden-text{display:none} and .show-marks .hidden-text{display:inline;color:#999;text-decoration:underline dotted} so Show/Hide reveals hidden text (matches H.showHide which toggles .show-marks on #editor).\n\nThis is a full, working subset: family/style/size/color/underline-style+color/all 7 effects/scale/spacing/position + live preview, applied atomically to the selection.",
        "testPlan": "Add cases to the in-renderer test battery (the WORD_CLONE_READY harness):\n1) Open: call WC.Dialogs.font(); assert a .dialog with title \"Font\" and a .tabs strip with two .t tabs exists; assert preview-box present.\n2) Tab switch: click the Advanced .t; assert Advanced controls (Scale/Spacing/Position) visible and Font-tab list hidden; click back.\n3) Prefill: select a run that is bold+italic 14pt red (insert known HTML, select it, open dialog); assert style list = 'Bold Italic', size input = '14', color swatch background ≈ #FF0000, family matches.\n4) Apply basic: select plain text, open dialog, set family=Georgia, size=18, style=Bold, color=#0070C0, underline=double; OK; assert the selection is now wrapped in a single span whose computed fontFamily contains Georgia, fontSize≈24px (18pt), fontWeight≥600, textDecorationLine includes underline, textDecorationStyle='double', color rgb(0,112,192). Assert NOT nested 5-deep (querySelectorAll('span span span span span') length 0 within the applied run).\n5) Effects: check Small caps → assert span has class/fontVariant small-caps; check All caps → assert it cleared Small caps (mutual exclusion) and textTransform uppercase; check Superscript then Subscript → assert mutual exclusion and queryState().subscript true.\n6) Hidden: check Hidden, OK; assert run has .hidden-text and is display:none; toggle WC.Commands H.showHide (#editor.show-marks) → assert it becomes visible.\n7) Advanced: set Spacing=Expanded By 2pt, Scale=80%, Position=Raised By 3pt; OK; assert span letterSpacing≈2pt, an inline-block scaleX(0.8) transform present, and a positive top offset (raised).\n8) Live preview: programmatically toggle Bold checkbox/style and assert previewSpan computed fontWeight updates WITHOUT calling OK (no document mutation: editor.getHTML() unchanged until OK).\n9) Ctrl+D: dispatch a ctrl+d keydown; assert the Font dialog opens (app.js wiring).\n10) Cancel: open, change controls, Cancel; assert editor.getHTML() unchanged.",
        "feasibility": "full",
        "apis": [
          "document.execCommand('styleWithCSS')",
          "document.execCommand('foreColor'|'subscript'|'superscript'|'strikethrough'|'fontName')",
          "document.queryCommandState / queryCommandValue",
          "window.getSelection / Range.surroundContents / extractContents / insertNode",
          "getComputedStyle",
          "CSS: font-family/weight/style, color, text-decoration-line/style/color, font-variant:small-caps, text-transform:uppercase, letter-spacing, transform:scaleX + display:inline-block, position:relative+top, display:none"
        ],
        "subOptions": [
          "Font family (WC.FONTS list + search filter)",
          "Font style: Regular/Italic/Bold/Bold Italic",
          "Size (WC.SIZES list + numeric entry)",
          "Font color (WC.colorPalette, Automatic + theme/standard/More)",
          "Underline style: none/single/words/double/dotted/dashed/wavy/thick",
          "Underline color (enabled when style set)",
          "Effects: Strikethrough, Double strikethrough, Superscript, Subscript, Small caps, All caps, Hidden",
          "Live preview box",
          "Advanced > Scale (%)",
          "Advanced > Spacing: Normal/Expanded/Condensed + By (pt)",
          "Advanced > Position: Normal/Raised/Lowered + By (pt)",
          "Set As Default (session-scoped) / OK / Cancel"
        ]
      },
      {
        "feature": "Text Effects and Typography",
        "wordBehavior": "In real MS Word this is the \"A\" with a glowing blue outline button in the Font group. Clicking the dropdown arrow opens a menu:\n1) TOP GALLERY (15 preset WordArt-style text effects in a 5x3 grid) — each applies a bundled combination of fill color, outline, and shadow at once.\n2) OUTLINE submenu (cascades): a color palette (Theme Colors / Standard Colors / No Outline / More Outline Colors) plus \"Weight\" (a sub-list of stroke weights: ¼pt, ½pt, ¾pt, 1pt, 1½pt, 2¼pt, 3pt, 4½pt, 6pt, More Lines) and \"Dashes\" (solid/dash patterns — visual only on glyph outline).\n3) SHADOW submenu (cascades): \"No Shadow\"; an Outer group (9 directional presets: offsets in 8 compass directions + center), Inner group (9 presets), Perspective group (Top-Left/Top-Right/etc.), and \"Shadow Options…\" (a Format Text Effects task pane with Color, Transparency, Size, Blur, Angle, Distance sliders).\n4) REFLECTION submenu: \"No Reflection\" + a Reflection Variations grid (Tight/Half/Full reflection at 4pt/8pt offset — 9 presets) + \"Reflection Options…\".\n5) GLOW submenu: \"No Glow\" + Glow Variations grid (5 sizes x theme accent colors), a \"Glow Color\" palette, and \"Glow Options…\".\n6) NUMBER STYLES submenu (OpenType numeral forms, applied via font-feature-settings/font-variant-numeric): Default, Proportional Lining, Tabular Lining, Proportional Oldstyle, Tabular Oldstyle. (Sub-options: lining vs oldstyle figure shapes; proportional vs tabular spacing.)\n7) LIGATURES submenu: None, Standard Only, Standard and Contextual, Historical and Discretionary, All.\n8) STYLISTIC SETS submenu: a gallery of \"Individual\" alternate glyph sets (Set 1…Set 20 in Word) plus \"Default\", applied via font-feature-settings 'ss01'..'ss20'. Word previews each set live in the menu.\nAll effects are character-level (apply to the selected run) and combine; e.g. you can have outline + shadow + glow simultaneously, and the preset gallery applies several at once. Setting a \"No X\" / \"Default\" sub-item removes only that effect's CSS, leaving the others.",
        "uiNeeded": "A dropdown (WC.flyout) anchored on the Text Effects button. Because WC.flyout only keeps ONE flyout open at a time (closeFly() runs on each open), do NOT attempt true OS-style cascading submenus. Instead use the established two-level pattern: the top-level flyout has section header + items; clicking a category item (Outline/Shadow/Reflection/Glow/Number Styles/Ligatures/Stylistic Sets) opens a SECOND flyout anchored on that same row (re-using WC.flyout(rowNode, builder)), exactly like the existing colorMenu/underlineMenu do. Top-level flyout contents, in order:\n- fly-header \"Text Effects\"\n- A preset gallery: a grid (el div, gridTemplateColumns repeat(5,1fr)) of 15 cells, each cell an \"A\" styled with that preset's combined CSS (fill/outline/shadow), onClick applies the bundle.\n- fly-sep\n- flyItem \"Outline\" (icon optional) -> opens outline sub-flyout (color palette + Weight list + No Outline)\n- flyItem \"Shadow\" -> sub-flyout: \"No Shadow\" + Outer(9) + Inner(9) + Perspective(4) preset grid + \"Shadow Options…\"\n- flyItem \"Reflection\" -> sub-flyout: \"No Reflection\" + 9 variation cells + \"Reflection Options…\"\n- flyItem \"Glow\" -> sub-flyout: \"No Glow\" + 5 size variations x accent colors grid + \"Glow Color\" (colorPalette) + \"Glow Options…\"\n- fly-sep\n- flyItem \"Number Styles\" -> sub-flyout listing the 5 numeral modes (use flyItem; show a check on the active one)\n- flyItem \"Ligatures\" -> sub-flyout listing the 5 ligature modes\n- flyItem \"Stylistic Sets\" -> sub-flyout with \"Default\" + a grid of \"Set 1\"..\"Set 20\" cells, each previewing the word \"ffi 0123\" with that ss0n applied\n\"Shadow/Reflection/Glow Options…\" open WC.dialog modals (Format Text Effects) with range sliders that live-apply to the selection. The preset grids reuse the .color-grid / cell visual idiom already in CSS; add a small .te-cell class.",
        "apis": [
          "document.execCommand('styleWithCSS', true) — already set in Editor.init",
          "Selection / Range API (window.getSelection, range.surroundContents, range.extractContents) — via WC.Editor.applyInlineStyle",
          "CSS text-shadow (Shadow, Glow via multiple stacked shadows, Reflection fallback)",
          "CSS -webkit-text-stroke / element.style.webkitTextStroke (Outline) — reliable in Electron 31 Chromium",
          "CSS -webkit-text-fill-color (so outline can show with a hollow/colored fill)",
          "CSS -webkit-box-reflect (true Reflection, Chromium-only — safe since single engine)",
          "CSS font-feature-settings (Number Styles 'lnum'/'onum'/'pnum'/'tnum', Ligatures 'liga'/'clig'/'dlig'/'hlig', Stylistic Sets 'ss01'..'ss20')",
          "CSS font-variant-numeric / font-variant-ligatures (higher-level equivalents)",
          "DOM element.style[camelProp] assignment via WC.Editor.applyInlineStyle and a new applyInlineStyles helper"
        ],
        "subOptions": [
          "Outline (color palette + No Outline + Weight ¼..6pt + More Outline Colors)",
          "Shadow (No Shadow + Outer x9 + Inner x9 + Perspective x4 + Shadow Options dialog)",
          "Reflection (No Reflection + 9 tight/half/full variations + Reflection Options dialog)",
          "Glow (No Glow + 5 sizes x accent colors + Glow Color palette + Glow Options dialog)",
          "Number Styles (Default, Proportional Lining, Tabular Lining, Proportional Oldstyle, Tabular Oldstyle)",
          "Ligatures (None, Standard Only, Standard and Contextual, Historical and Discretionary, All)",
          "Stylistic Sets (Default + Set 1..Set 20)",
          "Preset WordArt gallery (15 bundled fill+outline+shadow presets)"
        ],
        "implementationPlan": "FILE A — /home/ogutd/msword-clone/src/renderer/js/editor.js: add a multi-property helper next to applyInlineStyle (so effects that need >1 CSS prop, e.g. outline = webkitTextStroke + webkitTextFillColor, or glow = several stacked text-shadows, apply atomically):\n  applyInlineStyles(styles) { this.node.focus(); this.restoreRange(); const sel=getSelection(); if(!sel.rangeCount) return; const range=sel.getRangeAt(0); const span=el('span'); for(const k in styles){ if(styles[k]==null||styles[k]==='') span.style.removeProperty(k.replace(/[A-Z]/g,m=>'-'+m.toLowerCase())); else span.style[k]=styles[k]; } if(range.collapsed){ span.appendChild(document.createTextNode('​')); range.insertNode(span); const r=document.createRange(); r.setStart(span.firstChild,1); r.collapse(true); sel.removeAllRanges(); sel.addRange(r);} else { try{range.surroundContents(span);}catch(e){span.appendChild(range.extractContents()); range.insertNode(span);} sel.removeAllRanges(); const r=document.createRange(); r.selectNodeContents(span); sel.addRange(r);} this.saveRange(); this.dirty=true; this.repaginate(); this.updateStatus(); this.emit(); }\n  Also add removeInlineStyle(prop) for the \"No X / Default\" items: walk the selection's spans and clear that one style prop (or set to '' / 'normal'). Simplest robust approach mirroring clearAllFormatting: applyInlineStyles({ [prop]: '' }) wrapping the run sets the property to empty on a fresh span which overrides the inherited effect — acceptable; for a cleaner removal, query span ancestors of the selection and delete style[prop]. Spec the simple override path first.\n\nFILE B — /home/ogutd/msword-clone/src/renderer/js/commands.js: \n1) Add handler: H.textEffectsAndTypography = (c, node) => textEffectsMenu(node);\n2) In Commands.dropdown add: if (cmd === 'textEffectsAndTypography') return textEffectsMenu(node);  (place near the other custom-dropdown branches so the split/dropdown generic path is bypassed.)\n3) Implement the menu + sub-builders. Concrete CSS mappings (camelCase DOM keys; values are CSS strings):\n   OUTLINE: applyInlineStyles({ webkitTextStroke: weight+' '+color, webkitTextFillColor: 'currentColor' }). Default weight '1px'. Word weights map: ¼pt→0.33px, ½pt→0.67px, ¾pt→1px, 1pt→1.33px, 1½pt→2px, 2¼pt→3px, 3pt→4px, 4½pt→6px, 6pt→8px (px = pt*1.3333). \"No Outline\" → applyInlineStyles({ webkitTextStroke:'', webkitTextFillColor:'' }). Reuse WC.colorPalette for the color pick (like colorMenu) then re-apply stroke with chosen color.\n   SHADOW: each preset is a textShadow string. Outer presets (offset 2px, blur 2px, color rgba(0,0,0,.45)) in 8 directions: e.g. Bottom-Right '2px 2px 2px rgba(0,0,0,.45)', Bottom '0 2px 2px ...', Bottom-Left '-2px 2px 2px ...', Right '2px 0 2px ...', Center '0 0 3px ...', Left '-2px 0 2px ...', Top-Right '2px -2px 2px ...', Top '0 -2px 2px ...', Top-Left '-2px -2px 2px ...'. Inner presets simulated with a tighter darker shadow (Chromium has no real inner text-shadow, approximate with '0 0 1px rgba(0,0,0,.6) inset'-unsupported → use small dark offset). Perspective presets use larger blur + skew via transform on the span (transform:'perspective(...)' is for blocks; for inline use a stronger lower shadow '0 6px 6px rgba(0,0,0,.35)'). Apply via applyInlineStyles({textShadow: preset}). \"No Shadow\" → textShadow:''.\n   REFLECTION: prefer native applyInlineStyles({ webkitBoxReflect: 'below 4px linear-gradient(transparent 40%, rgba(255,255,255,.4) 100%)' }) — works because we ship one Chromium engine. Variations change the offset (4px/8px) and gradient stop (Tight 60%, Half 50%, Full 35%) and opacity. \"No Reflection\" → webkitBoxReflect:''.\n   GLOW: stack multiple same-color blurred text-shadows: const glow=(color,r)=> Array(3).fill('0 0 '+r+'px '+color).join(', '); apply applyInlineStyles({textShadow: glow(color,r)}). Sizes r = 4/6/8/10/12px = Word's 5pt/8pt/11pt/14pt/18pt variations. Accent colors from the existing THEME/STANDARD palette. NOTE: glow and drop-shadow both use textShadow — if both wanted, compose by reading current span textShadow and appending; spec: glow replaces textShadow (matches Word treating them in same property is acceptable for the clone, document the limitation). \"No Glow\" → textShadow:''. \"Glow Color\" reuses WC.colorPalette.\n   NUMBER STYLES (font-feature-settings + font-variant-numeric): Default → fontVariantNumeric:'normal', fontFeatureSettings:'normal'; Proportional Lining → fontVariantNumeric:'lining-nums proportional-nums' (feature 'lnum' 1,'pnum' 1); Tabular Lining → 'lining-nums tabular-nums' ('lnum','tnum'); Proportional Oldstyle → 'oldstyle-nums proportional-nums' ('onum','pnum'); Tabular Oldstyle → 'oldstyle-nums tabular-nums' ('onum','tnum'). Use fontVariantNumeric (cleaner) as primary, fontFeatureSettings as fallback string.\n   LIGATURES (font-variant-ligatures): None → 'none'; Standard Only → 'common-ligatures no-discretionary-ligatures no-historical-ligatures no-contextual'; Standard and Contextual → 'common-ligatures contextual'; Historical and Discretionary → 'discretionary-ligatures historical-ligatures'; All → 'common-ligatures discretionary-ligatures historical-ligatures contextual'. Apply via applyInlineStyles({fontVariantLigatures: value}).\n   STYLISTIC SETS (font-feature-settings): Default → fontFeatureSettings:'normal'; Set n → fontFeatureSettings:'\"ss'+String(n).padStart(2,'0')+'\" 1'. Build a grid of cells previewing e.g. 'A a g 1' with the candidate ss applied inline so the user sees the alternate (effect only visible with fonts that ship the set, e.g. Calibri/Aptos/Gabriola — document that on fonts without the set it is a no-op, exactly as Word behaves).\n4) Sub-flyout helper pattern (mirrors colorMenu): each category opens WC.flyout(rowNode, builder). For grids reuse el('div',{style:{display:'grid',...}}) and small cells like bulletMenu does; each cell onClick: WC.closeFlyouts(); applyEffect(...). Add a 'No …' / 'Default' row at top using WC.flyItem.\n\nFILE C — /home/ogutd/msword-clone/src/renderer/js/dialogs.js: add D.textEffectOptions(kind) modals (Shadow/Reflection/Glow Options) using WC.dialog: range inputs for Transparency(0-100), Size/Blur(0-20px), Distance(0-12px), Angle(0-360), plus a color swatch button (opens colorPalette in a nested flyout or a More Colors input[type=color]). On input, recompute the textShadow / webkitBoxReflect string and call E().applyInlineStyles(...) live against the saved selection (E().savedRange). Footer OK/Cancel; Cancel restores prior CSS snapshot taken on open.\n\nFILE D — /home/ogutd/msword-clone/src/renderer/styles/ribbon.css: add minimal styles after line 261:\n  .te-grid{display:grid;gap:4px;padding:6px 10px}\n  .te-cell{height:36px;display:flex;align-items:center;justify-content:center;font:600 18px Calibri,Aptos,sans-serif;border:1px solid #e1dfdd;cursor:pointer;border-radius:2px}\n  .te-cell:hover{outline:2px solid var(--word-blue);outline-offset:-1px}\n  .te-check{margin-left:auto;color:var(--word-blue)} (for showing the active Number/Ligature/Set).\nNo ribbon-data.js change strictly required (cmd/items already present at lines 189-204); the only functional gap is the missing handler + dropdown branch, which FILE B fixes. Optionally change \"feasible\":\"partial\" to \"full\".",
        "testPlan": "Add cases to the in-renderer test battery (the same harness that exercises WC.Commands). For each, programmatically select a known run of text in #editor (place a range over a span/paragraph via document.createRange + selection.addRange, set WC.Editor.savedRange), invoke the handler/sub-action directly, then assert the resulting span's inline style:\n1) Outline: call the outline-apply path with weight ¾pt + color #C00000 → assert the new span getAttribute('style') contains 'text-stroke' (or el.style.webkitTextStroke === '1px rgb(192, 0, 0)') and webkitTextFillColor set. Then \"No Outline\" → assert webkitTextStroke cleared.\n2) Shadow: apply Bottom-Right preset → assert span.style.textShadow includes '2px 2px'. No Shadow → textShadow==='' .\n3) Reflection: apply Full → assert span.style.webkitBoxReflect startsWith('below') and contains 'linear-gradient'. No Reflection clears it.\n4) Glow: apply size 8px accent #4472C4 → assert span.style.textShadow matches /0px 0px 8px/ and the color appears 3 times (stacked). No Glow clears.\n5) Number Styles: apply Tabular Oldstyle → assert span.style.fontVariantNumeric.includes('oldstyle-nums') && includes('tabular-nums'). Default → 'normal'.\n6) Ligatures: apply 'None' → fontVariantLigatures==='none'; 'All' → contains 'discretionary-ligatures'.\n7) Stylistic Sets: apply Set 4 → assert span.style.fontFeatureSettings === '\"ss04\" 1'; Default → 'normal'.\n8) Preset gallery: click preset index 2 → assert span has BOTH a fill/color and a textShadow (bundle applied).\n9) Combination/idempotence: apply Outline then Glow on overlapping selection → assert both webkitTextStroke and textShadow present on the resulting nested spans; getHTML() round-trips (load via setHTML, the inline styles survive because they are plain CSS — assert effect styles still present after setHTML(getHTML())).\n10) Menu wiring: spy that WC.Commands.dropdown({cmd:'textEffectsAndTypography'}, node) opens a flyout (document.querySelector('.flyout') exists and contains a 'Outline' fly-item and a 'Stylistic Sets' fly-item) rather than firing WC.notImplemented (assert no toast text 'not implemented').\nEach assertion uses the live computed/inline style on the inserted span; since we ship one Chromium engine, getComputedStyle for text-shadow/box-reflect/font-feature-settings is deterministic and can be asserted directly.",
        "feasibility": "full"
      },
      {
        "feature": "Multilevel List (Home ▸ Paragraph group dropdown) — cmd \"multilevelList\", id \"home.paragraph.multilevel-list\". Provides: (1) List Library gallery of multilevel patterns (decimal 1./1.1./1.1.1., bullet hierarchy ●/○/■, legal/outline 1.1.1, mixed I./A./1.), (2) Change List Level submenu, (3) Define New Multilevel List dialog, plus the standard Tab/Shift+Tab demote/promote behavior inside lists.",
        "wordBehavior": "In real MS Word, Multilevel List is a dropdown (not split) showing: a \"Current List\" thumbnail (the active pattern), a \"List Library\" gallery of 7 built-in galleries, \"Change List Level\" (a flyout of up to 9 level previews you click to move the current paragraph to that level), \"Define New Multilevel List…\" (a dialog with per-level Number format, Number style for this level dropdown [1,2,3 / a,b,c / A,B,C / i,ii,iii / I,II,III / bullet], \"Include level number from\" to build 1.1.1, aligned-at/indent-at, \"Start at\"), and \"Define New List Style…\". Applying a library pattern converts the selected paragraphs into a list and remembers the gallery so nested levels render that gallery's marker per level. Pressing Tab at the start of a list item demotes it one level (max 9); Shift+Tab promotes it (outdenting off level 1 leaves the list). The level controls indentation AND which numbering/bullet glyph is shown. Decimal-with-levels (\"Include level number from\") concatenates ancestor counters: 1, then 1.1, then 1.1.1. Each level can restart numbering, and a deeper level's counter resets when its parent increments. Change List Level on a multi-paragraph selection moves all of them. Legal style forces every level to arabic concatenated (1, 1.1, 1.1.1). The Numbering split-button's \"Change List Level\" item does the same level move for a single-level numbered list.",
        "uiNeeded": "A dropdown flyout (via WC.flyout) anchored on the Multilevel List ribbon button, structured top-to-bottom: (a) header \"List Library\"; (b) a CSS grid (gridTemplateColumns repeat(3,1fr)) of clickable preview cells, each rendering a tiny static 3-line preview of the pattern (built with a small DOM snippet, not images) — patterns: \"None\", \"1. / 1.1. / 1.1.1.\" (decimal), \"1) / a) / i)\" (outline mixed), \"● / ○ / ■\" (bullet hierarchy), \"I. / A. / 1.\" (legal-ish), \"1.1.1\" (legal all-arabic), \"Article I. / Section 1.01\" (chapter). Hover highlights the cell; click applies and closes; (c) WC.flySep; (d) WC.flyItem \"Change List Level\" with a hover/submenu — since the flyout helper is single-level, implement Change List Level as its own nested WC.flyout opened from this item (anchored on the item node) listing \"Level 1 … Level 9\" each as a WC.flyItem that calls setListLevel(n); (e) WC.flySep; (f) WC.flyItem \"Define New Multilevel List…\" → opens a WC.dialog; (g) WC.flyItem \"Define New List Style…\" → WC.notImplemented (kept honest; it is a thin wrapper of the same dialog and out of scope to fully build). The Define New Multilevel List dialog (WC.dialog, width ~520px): a \"Click level to modify\" 1–9 selector (row of buttons), a \"Number format\" text field showing the template e.g. \"%1.%2.\" , a \"Number style for this level\" <select> (1,2,3 / a,b,c / A,B,C / i,ii,iii / I,II,III / ●,○,■ / none), an \"Include level number from\" <select> (level 1..n-1, for building 1.1.1), \"Start at\" number, \"Aligned at\"/\"Indent at\" inches. OK builds a list-style definition object and applies it to the selection. Plus Change List Level is also added as an item under the existing Numbering split-button dropdown (commands.js dropdown branch).",
        "implementationPlan": "RENDERING MODEL: use real nested &lt;ol&gt;/&lt;ul&gt; (execCommand already produces these and the test battery asserts on them), and drive per-level glyphs with CSS counters keyed by a data-attribute on the OUTER list. Concretely:\n\n1) CSS (append to src/renderer/styles/editor.css). Define list-pattern classes on the outermost list, e.g. `ol.ml-decimal`. Use the standard CSS-counters technique so each level shows the concatenated number:\n   `#editor ol.ml-decimal { list-style:none; counter-reset:ml; }`\n   `#editor ol.ml-decimal li { counter-increment:ml; }`\n   `#editor ol.ml-decimal li::marker, ` — markers via ::before since we need the dotted concat: set `list-style:none` and use `li::before { content: counters(ml,\".\") \". \"; }` with `counter-reset:ml` on every nested `ol`. Because `counters(ml,\".\")` walks the counter stack, nesting `<ol class=\"ml-decimal\"><li>…<ol>…</ol></li></ol>` automatically yields 1, 1.1, 1.1.1 — exactly Word's \"include level number from\". For ml-legal use the same but no trailing period rules per level. For ml-bullet use `ul.ml-bullet` with `list-style:none` and `li::before { content:\"● \" }`, nested `ul ul li::before{content:\"○ \"}`, `ul ul ul li::before{content:\"■ \"}`. For ml-outline (1) a) i)) use `counter(ml,decimal)` at depth1, `counter(ml,lower-alpha)` depth2, `counter(ml,lower-roman)` depth3 via depth-specific selectors. Add `--ml-indent` padding-left per nesting depth (Word indents 0.25\"→0.5\"→…). Keep existing `#editor ul,#editor ol{padding-left:36px}` but override under the ml-* classes with `padding-left:0` on inner lists + per-li padding so counters align.\n\n2) editor.js — add a list-level engine (new methods on WC.Editor, mirroring applyBlockStyle style):\n   - `currentListItem()`: from saved range, walk up to nearest LI within this.node (null if not in a list).\n   - `listLevel(li)`: count ancestor OL/UL between li and editor → depth (1-based).\n   - `demoteListItem()` (Tab): for each selected top-level LI, if it has a previous-sibling LI, move it into a child list of that sibling: find/create `prevLi.querySelector(':scope>ol,:scope>ul')` (matching parent list tag + ml-class), append the LI. Cap at depth 9. Re-focus caret. This is the manual, reliable equivalent of `execCommand('indent')` inside a list (execCommand('indent') in Chromium wraps in blockquote — unreliable for lists, so we DON'T use it here).\n   - `promoteListItem()` (Shift+Tab): inverse — move LI up to its grandparent list right after the parent LI; if already at depth 1, unwrap (execCommand('outdent') or convert LI→P).\n   - `setListLevel(n)`: repeatedly demote/promote selected items until they reach depth n.\n   - `applyMultilevelPattern(patternKey)`: ensure selection is a list (if not, run `insertOrderedList`/`insertUnorderedList` to seed it), then find the OUTERMOST list(s) covering the selection and set its className to `ml-<patternKey>` and also stamp `data-ml=\"<patternKey>\"` so nested lists created later inherit (in demote, copy the outer list's ml-class onto any new child list). Store last-used pattern in a module var for \"Current List\".\n   All of these end with the standard tail: this.saveRange(); this.dirty=true; this.repaginate(); this.updateStatus(); this.emit();\n\n3) commands.js — wire dispatch:\n   - Add `H.multilevelList = (c,node)=>multilevelMenu(node);` and add `if (cmd==='multilevelList') return multilevelMenu(node);` in Commands.dropdown so it doesn't fall through to the generic notImplemented menu.\n   - `multilevelMenu(node)`: builds the flyout described in uiNeeded; each library cell calls `E().applyMultilevelPattern(key)`; \"Change List Level\" opens `changeListLevelMenu(itemNode)`; \"Define New Multilevel List…\" → `WC.Dialogs.defineMultilevelList()`.\n   - `changeListLevelMenu(node)`: `WC.flyout(node,(fly)=>{ for(let i=1;i<=9;i++) fly.appendChild(WC.flyItem('Level '+i,{onClick:()=>E().setListLevel(i)})); })`.\n   - Reuse for the Numbering split-button: in Commands.dropdown's existing numbering path, append a \"Change List Level\" entry that calls changeListLevelMenu. (bulletMenu(node,true) is the numbering dropdown — add the Change List Level + a small library that also calls applyMultilevelPattern for single-level decimal.)\n\n4) dialogs.js — add `D.defineMultilevelList = function(){…}` building the WC.dialog from uiNeeded. Keep a 9-slot `levels[]` array of {format,style,includeFrom,startAt}. The level buttons swap which slot the controls edit (read/write into levels[idx]). OK: synthesize a pattern object and a generated class name `ml-custom-<id>`, inject a small <style> block implementing the chosen per-level counter styles (lower-alpha/upper-roman/etc.) via document.head, then call `E().applyMultilevelPattern('custom-<id>')`. This makes Define New Multilevel List genuinely produce the chosen numbering, not a stub. (Number-style→CSS map: '1'→decimal, 'a'→lower-alpha, 'A'→upper-alpha, 'i'→lower-roman, 'I'→upper-roman, '●'→bullet via ::before content.)\n\n5) app.js — keyboard: add a Tab/Shift+Tab handler. The editor's input listeners exist but no keydown for Tab. Add in Editor.init() (or bindKeys, but better on E().node to scope to editor): `this.node.addEventListener('keydown', e=>{ if(e.key==='Tab' && this.currentListItem()){ e.preventDefault(); e.shiftKey?this.promoteListItem():this.demoteListItem(); }});`. This gives the real Word Tab-to-demote behavior and must run BEFORE contenteditable's default Tab (which would move focus / insert a tab). Also keep Tab working normally outside lists (insert a tab or move focus per current behavior — currently none, so guard with the currentListItem() check).\n\n6) icons.js already has a `multilevelList` glyph — no change needed.\n\nFILES TO EDIT (all absolute):\n - /home/ogutd/msword-clone/src/renderer/js/editor.js (list-level engine methods + Tab keydown binding in init)\n - /home/ogutd/msword-clone/src/renderer/js/commands.js (H.multilevelList, dropdown branch, multilevelMenu, changeListLevelMenu, numbering menu Change List Level)\n - /home/ogutd/msword-clone/src/renderer/js/dialogs.js (D.defineMultilevelList)\n - /home/ogutd/msword-clone/src/renderer/styles/editor.css (ml-decimal / ml-legal / ml-bullet / ml-outline counter rules + indents)\nNo ribbon-data.js change needed — the control already exists (type \"dropdown\", items present); we just add the custom handler so it stops hitting the generic notImplemented branch.",
        "testPlan": "Add tests to /home/ogutd/msword-clone/scripts/test-suite.js using the existing `t(name,fn)` + `selectBlock`/`caretIn`/`H()` harness (electron . --probe-out=… --shot-evalfile=scripts/test-suite.js):\n 1. 'Multilevel: apply decimal pattern' — E.setHTML('<p>one</p>'); selectBlock(p); WC.Commands.run({cmd:'numbering'}) then E.applyMultilevelPattern('decimal'); assert `E.node.querySelector('ol.ml-decimal li')` exists.\n 2. 'Multilevel: Tab demotes to level 2 (nested ol)' — build '<ol class=\"ml-decimal\"><li>a</li><li>b</li></ol>', put caret in 2nd li, dispatch a real KeyboardEvent('keydown',{key:'Tab',bubbles:true}) on E.node (or call E.demoteListItem()); assert `E.node.querySelector('ol.ml-decimal > li > ol > li')` and that li text is 'b', and E.listLevel(thatLi)===2.\n 3. 'Multilevel: Shift+Tab promotes back to level 1' — from the nested structure, call E.promoteListItem() on the nested li; assert it is again a direct child (`ol.ml-decimal > li` count === 2) and no nested ol remains.\n 4. 'Multilevel: counters render 1.1.1' — build 3 nested ml-decimal levels, then read getComputedStyle(li::before).content via `window.getComputedStyle(deepLi,'::before').content` and assert it contains '1.1.1' (verifies CSS counters wired).\n 5. 'Multilevel: setListLevel(3) reaches depth 3' — caret in a flat ml-decimal li, E.setListLevel(3); assert E.listLevel(li)===3.\n 6. 'Multilevel: bullet hierarchy classes' — applyMultilevelPattern('bullet'); assert `ul.ml-bullet` and nested demote keeps ml-bullet on child ul.\n 7. 'Multilevel: Change List Level menu moves item' — open via WC.Commands.run({cmd:'multilevelList'},node) is hard headless; instead assert the dispatch no longer hits notImplemented: spy that WC.notImplemented is NOT called for cmd multilevelList (wrap WC.notImplemented, run dispatch, assert flag false / a .flyout appeared).\n 8. 'Multilevel: Define New Multilevel List dialog opens + applies' — WC.Dialogs.defineMultilevelList(); set a style select to 'a' for level 2; click OK; assert a generated style class applied and an <ol>/<ul> exists. Clean up: `document.querySelectorAll('#find-pane,#nav-pane,.modal-backdrop').forEach(n=>n.remove())` (matches existing teardown). All assertions return boolean/string per the harness convention.",
        "feasibility": "full",
        "apis": [
          "document.execCommand('insertOrderedList'/'insertUnorderedList'/'outdent') via WC.Editor.exec (styleWithCSS=true)",
          "Selection/Range API (getSelection, getRangeAt, range.collapse, setStart) for caret + walking up to LI",
          "DOM tree manipulation (appendChild/insertBefore/querySelector(':scope>ol,:scope>ul')) for demote/promote level moves",
          "CSS counters: counter-reset/counter-increment + content:counters(name,'.') on li::before for 1.1.1 numbering, with lower-alpha/lower-roman/upper-roman styles",
          "KeyboardEvent + node.addEventListener('keydown') on #editor for Tab/Shift+Tab",
          "getComputedStyle(el,'::before').content for test verification",
          "WC.flyout / WC.flyItem / WC.flySep / WC.flyHeader for the dropdown + Change List Level submenu",
          "WC.dialog for Define New Multilevel List",
          "document.head <style> injection for custom user-defined level patterns",
          "WC.Editor.selectedBlocks / saveRange / restoreRange / repaginate / emit (existing helpers)"
        ],
        "subOptions": [
          "List Library gallery (None, 1./1.1./1.1.1. decimal, 1)/a)/i) outline, ●/○/■ bullet hierarchy, I./A./1. mixed, 1.1.1 legal all-arabic, Article/Section chapter)",
          "Current List indicator (last-applied pattern remembered in module var, shown highlighted)",
          "Change List Level (Level 1–9 submenu) — moves current paragraph(s) to a chosen nesting depth",
          "Tab = demote one level (max 9), Shift+Tab = promote one level / leave list at level 1",
          "Define New Multilevel List… dialog (per-level Number format, Number style select, Include level number from, Start at, Aligned/Indent) producing a real injected counter style",
          "Define New List Style… (kept as WC.notImplemented — honest gap, documented)",
          "Change List Level also surfaced under the Numbering split-button dropdown"
        ]
      },
      {
        "feature": "Clipboard task pane (Clipboard group dialog-box-launcher) — the Office Clipboard collecting up to 24 cut/copied items, with Paste All, Clear All, and click-an-item-to-paste.",
        "wordBehavior": "In real MS Word, clicking the tiny dialog-box-launcher arrow at the bottom-right of the Home > Clipboard group opens the Office Clipboard task pane (docks left by default, can dock right/float). Behavior:\n- It silently collects every Cut and Copy you make (in Word and across Office apps) up to a maximum of 24 items. The 25th copy pushes out the oldest (FIFO).\n- Each entry shows an icon indicating source/type plus a preview: ~50 chars of text for text items, a thumbnail for pictures. Newest item is at the top.\n- Single-click an item pastes it at the current insertion point, preserving its formatting. Hovering an item reveals a dropdown chevron with Paste and Delete.\n- \"Paste All\" button pastes every collected item, in collection order (oldest first), into the document at the caret.\n- \"Clear All\" empties the whole clipboard collection (the list becomes empty with the placeholder text \"Clipboard empty. Copy or cut to collect items.\").\n- An \"Options\" button at the bottom holds toggles: \"Show Office Clipboard Automatically\", \"Show Office Clipboard When Ctrl+C Pressed Twice\", \"Collect Without Showing Office Clipboard\", \"Show Office Clipboard Icon on Taskbar\", \"Show Status Near Taskbar When Copying\". A header shows a counter like \"12 of 24 - Clipboard\".\nSub-options to model: per-item Paste / Delete (hover chevron menu), Paste All, Clear All, the item count header, the empty-state placeholder, and (as approximated menu entries) the Options toggles.",
        "uiNeeded": "A left-docked task pane (reuse the existing .taskpane component, NOT .taskpane.right, since Office Clipboard docks left). Exact contents, top to bottom:\n1. .tp-head: title \"Clipboard\" + close X (WC.icon('win_close', 12)).\n2. A toolbar row (.cb-actions) with two buttons: \"Paste All\" and \"Clear All\" (both use existing .btn class; disabled/greyed when the collection is empty).\n3. A counter line (.cb-count) reading \"N of 24 - Clipboard\".\n4. .tp-body containing the scrollable item list (.cb-list). Each item is a .cb-item row: a small type icon (paste/clipboard glyph) on the left, a text/thumbnail preview (max ~50 chars, ellipsised; images render a small <img> thumbnail), and a hover-revealed chevron (.cb-item-menu) that opens a WC.flyout with \"Paste\" and \"Delete (X)\" entries. Clicking the item body pastes it.\n5. Empty state: when the list is empty, .cb-list shows a centered placeholder div \"Clipboard empty. Copy or cut to collect items.\" and the Paste All / Clear All buttons are disabled.\n6. Bottom .cb-options: a single \"Options ▾\" WC.flyItem-style button opening a flyout of the five Office-Clipboard toggle labels (checkbox-styled flyItems; the toggles drive real local behavior where applicable, e.g. \"Show Office Clipboard Automatically\" auto-opens the pane on copy).\nNo modal WC.dialog is needed — this is a task pane, matching D.findPane / D.navPane / D.stylesPane patterns.",
        "implementationPlan": "All code goes in src/renderer/js. No new globals beyond WC.Clipboard and WC.Dialogs.clipboardPane.\n\nA) History store + capture (editor-level, in a new IIFE block or appended to commands.js):\n- Add WC.Clipboard = { items: [], MAX: 24 } where items[] = { html, text, kind: 'text'|'image', ts }. Newest unshifted to front.\n- Capture function: WC.Clipboard.capture(range) reads the current selection's HTML and text BEFORE the browser cut clears it:\n    const sel = window.getSelection();\n    if (!sel || sel.isCollapsed) return;\n    const r = sel.getRangeAt(0);\n    const frag = r.cloneContents();\n    const div = document.createElement('div'); div.appendChild(frag);\n    const html = div.innerHTML.replace(/​/g,''); // strip zero-width spans used by applyInlineStyle\n    const text = sel.toString();\n    if (!text.trim() && !div.querySelector('img')) return;\n    const kind = (!text.trim() && div.querySelector('img')) ? 'image' : 'text';\n    Dedup: if items[0] && items[0].html === html, skip. Else unshift({html,text,kind,ts:Date.now()}); if (items.length > MAX) items.length = MAX.\n    Then if pane open, re-render; if 'auto' option on, open pane.\n- Wire capture into the existing handlers in commands.js. Change:\n    H.cut = () => { WC.Clipboard.capture(); E().exec('cut'); };\n    H.copy = () => { WC.Clipboard.capture(); E().exec('copy'); };\n  Also intercept native keyboard Ctrl+C / Ctrl+X (since those bypass H.cut/H.copy): in editor.js init(), add\n    this.node.addEventListener('copy', () => WC.Clipboard.capture());\n    this.node.addEventListener('cut', () => WC.Clipboard.capture());\n  (These fire on the editor before the browser mutates the DOM for cut, so cloneContents still has the selection; capture reads the selection synchronously in the event, which is valid.) This single interception covers both ribbon buttons and keyboard, so the ribbon handlers can rely on it OR call capture directly — keep capture idempotent via the dedup-on-items[0] guard to avoid double-adds when both the DOM 'copy' event and H.copy fire.\n  Cleanest: leave H.cut/H.copy as-is and ONLY capture in the DOM 'copy'/'cut' listeners — that covers ribbon-triggered execCommand('copy') too, because execCommand('copy') dispatches a real 'copy' event. Dedup guard then prevents duplicates. Prefer this approach (one capture site).\n\nB) Paste an item: WC.Clipboard.paste(item):\n    E().focus(); E().restoreRange();\n    if (item.kind === 'image' || /</.test(item.html)) E().insertHTML(item.html);\n    else E().insertHTML(WC.escapeHtml(item.text).replace(/\\n/g,'<br>'));\n  (insertHTML routes through E().exec('insertHTML', ...) which keeps caret/repaginate/emit.)\n  WC.Clipboard.pasteAll(): iterate items from OLDEST to newest (items.slice().reverse()), calling paste() for each so document order matches collection order.\n\nC) Task pane: add D.clipboardPane to dialogs.js, mirroring D.navPane/D.stylesPane exactly:\n    D.clipboardPane = function () {\n      let pane = document.getElementById('clipboard-pane'); if (pane) { pane.remove(); return; } // toggle\n      pane = el('div', { class: 'taskpane', id: 'clipboard-pane' });\n      head = .tp-head with title 'Clipboard' + close X (onclick remove).\n      actions row: two el('button',{class:'btn'...}) Paste All -> WC.Clipboard.pasteAll(); Clear All -> WC.Clipboard.clear().\n      count line .cb-count.\n      body .tp-body > list .cb-list.\n      render() builds rows from WC.Clipboard.items; each .cb-item: icon span, preview span (text: item.text.slice(0,50) + (len>50?'…':'') OR an <img src> thumbnail at max-height 40px for image kind), chevron .cb-item-menu opening WC.flyout with Paste / Delete.\n      item body click -> WC.Clipboard.paste(item).\n      Disable Paste All/Clear All buttons + show placeholder when items empty.\n      Append pane to document.getElementById('workarea') (same host the other panes use).\n      Register WC.Clipboard.onChange = render so capture()/clear()/delete re-render live (guard for pane removed: re-query getElementById and bail if gone).\n    }\n  WC.Clipboard methods clear() (items.length=0; onChange()), remove(idx) (items.splice(idx,1); onChange()).\n\nD) Dispatch wiring: add handler in commands.js so the existing launcher (ribbon-data home.clipboard.clipboard, cmd 'clipboard') opens the pane instead of falling through to notImplemented:\n    H.clipboard = () => WC.Dialogs.clipboardPane();\n  No ribbon-data change needed — the launcher already exists with cmd:'clipboard' and Commands.run dispatches H[cmd].\n\nE) CSS: append to src/renderer/styles/dialogs.css (reuse .taskpane shell):\n    .cb-actions { display:flex; gap:6px; padding:8px 0; }\n    .cb-actions .btn { flex:1; }\n    .cb-count { font-size:12px; color:#666; padding:0 0 6px; }\n    .cb-item { display:flex; align-items:flex-start; gap:8px; padding:8px; border:1px solid #edebe9; border-radius:2px; margin-bottom:6px; cursor:pointer; position:relative; }\n    .cb-item:hover { background:var(--word-blue-tint); border-color:var(--word-blue); }\n    .cb-item .cb-ic { flex:0 0 18px; color:#666; }\n    .cb-item .cb-prev { font-size:12px; line-height:1.35; color:#222; overflow:hidden; }\n    .cb-item img.cb-thumb { max-height:40px; max-width:100%; }\n    .cb-item-menu { position:absolute; top:4px; right:4px; opacity:0; cursor:pointer; }\n    .cb-item:hover .cb-item-menu { opacity:1; }\n    .cb-empty { color:#888; text-align:center; padding:24px 8px; font-size:13px; }\n  Optionally add a 'clipboard' icon to icons.js (a simple clipboard path) and use WC.icon('clipboard',16) for .cb-ic; otherwise reuse WC.icon('_generic',14).\n\nLoad order: util.js/editor.js/dialogs.js/commands.js are all already loaded; WC.Clipboard can live at the top of commands.js (or its own small file added to index.html script list) since it is referenced lazily inside handlers/the pane.",
        "testPlan": "Add to the in-renderer test battery (same harness that exercises WC.Commands/WC.Dialogs). Assertions:\n1. Capture on copy: set #editor selection over a known run, dispatch a synthetic 'copy' event (or call WC.Clipboard.capture() with a Range selected) — assert WC.Clipboard.items.length === 1 and items[0].text matches, items[0].html contains expected markup.\n2. Dedup: capture the same selection twice -> items.length stays 1.\n3. FIFO cap: programmatically push 25 distinct captures -> assert items.length === 24 and the oldest was dropped (items[23] is the 2nd inserted, items[0] is the newest).\n4. Pane open/toggle: WC.Dialogs.clipboardPane() -> document.getElementById('clipboard-pane') exists; call again -> it is removed (toggle).\n5. Render count: with N items, .cb-count text === N + ' of 24 - Clipboard'; number of .cb-item nodes === N.\n6. Click-to-paste: record #editor.innerHTML length, click a .cb-item (or call WC.Clipboard.paste(items[i])) at a known caret -> editor HTML now contains the item's text/markup.\n7. Paste All: clear editor to '<p><br></p>', set caret, call WC.Clipboard.pasteAll() with 3 known items -> editor contains all three in collection (oldest-first) order.\n8. Clear All: WC.Clipboard.clear() -> items.length === 0, pane shows .cb-empty placeholder, Paste All/Clear All buttons have :disabled.\n9. Per-item delete: WC.Clipboard.remove(0) -> items.length decremented and that preview no longer in the list after re-render.\n10. Launcher dispatch: WC.Commands.run({cmd:'clipboard'}, fakeNode) opens the pane (no notImplemented toast).\nRun via the existing test runner; each assertion uses direct DOM/state checks (no real OS clipboard needed, so it is deterministic in the Electron renderer).",
        "feasibility": "full",
        "apis": [
          "window.getSelection / Selection.toString",
          "Range.cloneContents",
          "document.execCommand('copy'/'cut'/'insertHTML') via WC.Editor.exec/insertHTML",
          "DOM 'copy' and 'cut' events on #editor (fire for both keyboard and ribbon-triggered execCommand)",
          "WC.el / WC.escapeHtml / WC.toast / WC.flyout / WC.flyItem / WC.icon",
          "WC.Editor.focus/restoreRange/insertHTML",
          "WC.Dialogs (task pane host #workarea)"
        ],
        "subOptions": [
          "Per-item Paste (hover chevron menu)",
          "Per-item Delete (hover chevron menu)",
          "Paste All (collection order, oldest-first)",
          "Clear All",
          "Item count header 'N of 24 - Clipboard'",
          "Empty-state placeholder 'Clipboard empty...'",
          "Text preview truncated to ~50 chars",
          "Image items rendered as thumbnails",
          "Options menu: Show Automatically / On Double-Ctrl+C / Collect Without Showing / Taskbar Icon / Status Near Taskbar (auto-open toggle wired live; others approximated)"
        ]
      },
      {
        "feature": "Format Painter (Home ▸ Clipboard) — double-click sticky/locked mode, cursor feedback, and the exact set of inline styles copied. The single-click \"copy once, apply once\" path already exists in commands.js (H.formatPainter → currentInlineStyle + armPainter).",
        "wordBehavior": "In real MS Word: a single click on Format Painter copies formatting from the current selection/caret and applies it to the next selection, then turns off (one-shot). A double-click locks the tool on, applying the copied formatting to every subsequent selection until the user presses Esc, clicks the button again, or re-presses Ctrl+Shift+C. The paintbrush cursor (I-beam + brush) shows while active. It copies character formatting (font, size, bold/italic/underline incl. style+color, strikethrough, sub/superscript, font color, highlight, small caps, spacing, language) and, when the source includes a paragraph mark/whole paragraph, paragraph formatting (alignment, indents, spacing, list, style). It never copies the text itself. Ctrl+Shift+C copies formatting; Ctrl+Shift+V applies it.",
        "uiNeeded": "No dropdown, dialog, or task pane. UI surface is entirely on the existing single ribbon button (ribbon-data.js id \"home.clipboard.format-painter\", cmd \"formatPainter\", currently type \"button\"):\n- Pressed/locked visual: reuse the existing \".toggled\" class (ribbon.css:131 .rbtn.toggled). Single-click arms a transient highlight; double-click keeps \".toggled\" on until cancelled.\n- Cursor feedback over #editor: a paintbrush-with-I-beam cursor. Implement via a body/#editor class \".fp-active\" that sets cursor to an inline SVG data-URI paintbrush (cursor: url(\"data:image/svg+xml,...\") 4 2, text;). Add a tiny visual hint: keep WC.toast guidance on arm (single: \"select text to apply once\"; double: \"locked — select text repeatedly, Esc to stop\").\n- Optional status hint: none required; the toast plus persistent toggled button is the Word-faithful affordance.",
        "implementationPlan": "All changes live in commands.js (the painter state already exists there) plus one ribbon wiring hook and a small CSS rule. Concrete steps:\n\n1) commands.js — STATE. Keep the existing module var `let painter = null`. Extend its shape to `{ inline, block, sticky }`. Add `let fpArmed = false` only if needed; simpler is to treat `painter !== null` as \"armed\".\n\n2) commands.js — CAPTURE. Replace currentInlineStyle() with a richer copier `captureFormat()` returning `{ inline:{...}, block:{...} }`:\n   - inline (from getComputedStyle of the run at sel.anchorNode→ nearest element): fontFamily, fontSize, fontWeight, fontStyle, textDecorationLine, textDecorationStyle, textDecorationColor, color, backgroundColor, verticalAlign (for sub/superscript), fontVariantCaps, letterSpacing.\n   - block (from the nearest block via E().selectedBlocks()[0]): only captured when the selection spans/touches a whole block (sel.isCollapsed OR selection covers block start→end). Capture textAlign, lineHeight, marginTop, marginBottom, textIndent, paddingLeft (indent). Store the block tagName/className too so a paragraph \"style\" can be re-applied.\n\n3) commands.js — H.formatPainter (single click). Keep current behavior but route through a new `armPainter(node, sticky)`:\n   H.formatPainter = (c, node) => armPainterFromSelection(node, false);\n   where armPainterFromSelection(node,sticky){ painter = { ...captureFormat(), sticky }; setPainterUI(node, true, sticky); WC.toast(sticky ? 'Format Painter locked — select text to apply repeatedly; Esc or click to stop.' : 'Format Painter — select text to apply the copied formatting once.'); }\n\n4) commands.js — DOUBLE-CLICK detection. ribbon.js renders plain buttons with a single 'click' listener (ribbon.js:211). Add a hook so cmd==='formatPainter' also binds 'dblclick'. Minimal, localized change in ribbon.js renderControl else-branch:\n   if (c.cmd === 'formatPainter') node.addEventListener('dblclick', () => { WC.closeFlyouts(); WC.Commands.run({ cmd: 'formatPainterLock' }, node); });\n   Then add H.formatPainterLock = (c, node2) => { const node = node2 || WC.Ribbon.controlIndex['formatPainter'].node; armPainterFromSelection(node, true); }.\n   Note: the single 'click' fires before 'dblclick'; that is fine because both just (re)capture and re-arm — the second event upgrades sticky=false→true. To avoid the click also consuming a one-shot apply, capture happens on the button, not the editor, so the leading click does not apply anything.\n\n5) commands.js — APPLY (the editor mouseup handler). Bind ONE persistent handler on E().node when arming, removed on disarm. On editor mouseup:\n   - if !painter return; if selection collapsed return;\n   - build a span, Object.assign painter.inline onto span.style (skip backgroundColor when 'rgba(0, 0, 0, 0)'/'transparent'); surroundContents with extractContents fallback (same pattern as existing armPainter, commands.js:189-190);\n   - if painter.block was captured AND the selection covers whole blocks: E().selectedBlocks().forEach(b => Object.assign(b.style, painter.block)) and, if a style/tag was stored, optionally WC.applyNamedStyle / formatBlock.\n   - E().dirty=true; E().repaginate(); E().updateStatus(); E().emit();\n   - if (!painter.sticky) disarmPainter(node);  // one-shot: revert after first apply.\n\n6) commands.js — DISARM + UI. \n   function disarmPainter(node){ painter=null; E().node.classList.remove('fp-active'); E().node.removeEventListener('mouseup', applyHandler); if(node) node.classList.remove('toggled'); }\n   function setPainterUI(node, on, sticky){ E().node.classList.toggle('fp-active', on); if(node) node.classList.toggle('toggled', on); }\n   Use a single named module-level applyHandler so add/removeEventListener pair correctly (the current code creates a fresh closure each time, which leaks listeners across re-arms — fix by hoisting one handler).\n\n7) commands.js — Esc + re-click cancel. \n   - Re-click: in armPainterFromSelection, if painter already armed and same node clicked while sticky → toggle off (disarm). Practically: if (painter) { disarmPainter(node); return; } at the top of the single/lock arm path so a click on an armed button cancels it (Word toggles off on re-click).\n   - Esc: extend the existing global keydown in app.js:52. Add WC.Commands.cancelFormatPainter && WC.Commands.cancelFormatPainter(); inside the Escape branch (before/after closeFlyouts). Expose Commands.cancelFormatPainter = () => { if (painter) disarmPainter(WC.Ribbon.controlIndex['formatPainter'].node); };\n\n8) commands.js — Keyboard Ctrl+Shift+C / Ctrl+Shift+V. Add to app.js keymap: (k==='c' && shift) => arm one-shot capture (painter set, sticky=false, no UI lock needed but set fp-active); (k==='v' && shift) => apply captured format to current selection immediately (reuse the apply body, then if !sticky disarm). These are optional polish but advertised in the tooltip, so wire them for fidelity.\n\n9) CSS — editor.css. Add:\n   #editor.fp-active, #editor.fp-active * { cursor: url(\"data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='20' height='20' viewBox='0 0 20 20'><path d='M3 17l2-5 6-6 3 3-6 6-5 2z' fill='%232B579A'/><path d='M11 6l3-3 3 3-3 3z' fill='%23C0C0C0'/></svg>\") 3 17, text; }\n   (paintbrush hotspot near the tip; falls back to text I-beam.)\n\nFiles touched: src/renderer/js/commands.js (main logic), src/renderer/js/ribbon.js (one dblclick hook for cmd formatPainter), src/renderer/js/app.js (Esc cancel + optional Ctrl+Shift+C/V), src/renderer/styles/editor.css (fp-active cursor). No new globals beyond Commands.cancelFormatPainter; reuses WC.Editor, WC.toast, WC.Ribbon.controlIndex, .toggled class.",
        "testPlan": "In-renderer test battery (drive via the same DOM these handlers use; assert on resulting markup/state):\n1) Single-click one-shot: set #editor to '<p>aaa <span style=\"color:rgb(255,0,0);font-weight:700\">bbb</span> ccc</p>'. Select \"bbb\", call WC.Commands.run({cmd:'formatPainter'}, btn). Assert painter!=null and btn.classList.contains('toggled') and #editor.classList.contains('fp-active'). Programmatically select \"ccc\" and dispatch a 'mouseup' on #editor. Assert \"ccc\" is now wrapped in a span whose computed color is rgb(255,0,0) and fontWeight 700. Assert painter===null, btn NOT toggled, #editor NOT fp-active (one-shot disarmed).\n2) Double-click sticky: same setup, dispatch 'dblclick' on btn (or call H.formatPainterLock). Assert painter.sticky===true and btn stays toggled. Apply to \"ccc\" (mouseup) → formatted AND painter still !=null, btn still toggled. Select a second run, apply again → also formatted, still armed (proves multi-apply).\n3) Esc cancels lock: while sticky-armed, dispatch keydown {key:'Escape'} on document. Assert painter===null, btn not toggled, #editor not fp-active, and listeners removed (a subsequent mouseup on a new selection does NOT add formatting).\n4) Re-click cancels: arm sticky, then WC.Commands.run({cmd:'formatPainter'}, btn) again → assert disarmed (toggle off), matching Word.\n5) Inline-style fidelity: capture from a run with italic+underline+color+highlight; apply to plain text; assert the new span carries fontStyle italic, textDecorationLine includes underline, color matches, backgroundColor matches; assert transparent/'rgba(0, 0, 0, 0)' backgrounds are NOT written (no stray background).\n6) Block formatting: capture with collapsed caret inside a centered, indented paragraph; apply across a multi-paragraph selection; assert selectedBlocks() got textAlign:center and the indent margins; assert text content is unchanged (no text copied).\n7) Listener-leak guard: arm+disarm 5 times; assert only one mouseup handler is active (apply fires exactly once per selection, not N times) — measured by spying on the number of spans created for a single apply (must be 1).\n8) No-text-copy invariant across all cases: assert #editor.innerText for the target run equals its original text (formatting only).",
        "feasibility": "full",
        "apis": [
          "document.execCommand (styleWithCSS)",
          "Selection / Range (getSelection, surroundContents, extractContents, insertNode)",
          "getComputedStyle",
          "MouseEvent dblclick + mouseup",
          "KeyboardEvent (Escape, Ctrl+Shift+C/V)",
          "CSS cursor url() with inline SVG data URI",
          "classList toggle (.toggled, .fp-active)"
        ],
        "subOptions": [
          "Single-click: copy formatting, apply once, auto-disarm",
          "Double-click: lock/sticky, apply to multiple selections",
          "Cancel via Esc",
          "Cancel via re-click on the button",
          "Ctrl+Shift+C copy formatting / Ctrl+Shift+V apply formatting",
          "Inline (character) formatting copy",
          "Paragraph (block) formatting copy when a whole paragraph is captured"
        ]
      },
      {
        "feature": "Dictate (Home tab, Voice group) — real speech-to-text inserting recognized words at the caret with a live interim transcript and a recording indicator, built on the Web Speech API (webkitSpeechRecognition / SpeechRecognition) in Electron 31 Chromium.",
        "wordBehavior": "In real MS Word, Dictate is a split button in Home > Voice. Main click starts dictation: a floating mic toolbar appears, the button shows a pulsing red dot and 'Listening…'. As you speak, FINAL recognized phrases are inserted at the caret; spoken punctuation ('comma', 'period', 'new line', 'new paragraph') becomes characters, and sentence starts auto-capitalize. Clicking again (or the toolbar mic) stops. The dropdown arrow opens: 'Spoken Language' (locale list with current one checked) and 'Settings' (Auto-punctuation, Profanity filter toggles). Word streams interim results so text visibly forms, then commits the final transcript. With no mic/permission/connectivity it shows an error and stays off.",
        "uiNeeded": "dropdown + floating recording bar (no modal). DROPDOWN (split-button arrow → WC.Commands.dropdown → dictateMenu): WC.flyout anchored to the button with WC.flyHeader('Spoken Language') + a WC.flyItem per locale (en-US, en-GB, fr-FR, de-DE, es-ES, it-IT, pt-BR, ja-JP, zh-CN) with a check glyph on the active one (onClick→WC.Dictate.setLang); WC.flySep; 'Auto-punctuation' and 'Profanity filter' toggle rows reflecting boolean state; 'About Dictation…' info item. RECORDING INDICATOR/INTERIM TRANSCRIPT: a fixed-position floating bar (class 'dictate-bar', appended to document.body — deliberately NOT a modal so the editor keeps focus and the caret) containing a pulsing red dot (CSS @keyframes), a 'Listening…'/language label, a live .interim transcript line updated on every interim result and cleared on each final commit, and a Stop/mic toggle button. The ribbon split button gets .toggled/.recording so its icon reflects the active state.",
        "implementationPlan": "PREREQUISITE (main process): Web Speech recognition in Electron Chromium captures audio via getUserMedia, gated by the session permission handler — currently absent in /home/ogutd/msword-clone/src/main/main.js, so recognition fails with 'not-allowed'. In createWindow() add mainWindow.webContents.session.setPermissionRequestHandler((wc, perm, cb) => cb(perm === 'media' || perm === 'audioCapture')) and session.setPermissionCheckHandler(() => true). Chromium's recognition uses a network speech service, so connectivity is the one external dependency; handle 'network'/'service-not-allowed' errors gracefully.\n\nRENDERER (new module /home/ogutd/msword-clone/src/renderer/js/dictate.js, loaded after commands.js, exposing WC.Dictate), mirroring the existing toggleReadAloud (speechSynthesis) pattern in commands.js.\n\nState (module-scoped): rec=null; active=false; bar=null; state={lang:'en-US',autoPunct:true,profanity:false}. Allow an injectable factory (WC.Dictate._SR) so the test battery can stub recognition.\n\nFeature detect: const SR = WC.Dictate._SR || window.SpeechRecognition || window.webkitSpeechRecognition; if(!SR){WC.toast('Dictate requires the Web Speech API, which is unavailable in this build.');return;} — graceful fallback.\n\nstart(): rec=new SR(); rec.lang=state.lang; rec.continuous=true; rec.interimResults=true; rec.maxAlternatives=1. Capture caret BEFORE recognition steals focus via WC.Editor.saveRange() (editor.js exposes savedRange/saveRange/restoreRange). Handlers: onresult iterates event.results from event.resultIndex, separating interim vs final; interim → updateBar(text) only; final → insertFinal(formatTranscript(chunk)). onerror maps e.error: 'not-allowed'/'service-not-allowed'→toast 'Microphone access was blocked.', 'network'→toast 'Dictation needs an internet connection.', 'no-speech'→ignore, else generic; stop() on fatal. onend: if still active restart rec.start() (continuous sessions auto-end) else teardown. Then rec.start(); active=true; showBar(); markButton(true).\n\ninsertFinal(text) [load-bearing caret handling]: WC.Editor.node.focus(); WC.Editor.restoreRange(); WC.Editor.insertHTML(WC.escapeHtml(text)+' '). insertHTML→exec already focuses, restoreRange, then saveRange — so caret advances and the next phrase appends after it. escapeHtml prevents markup injection from spoken text. For 'new line'→insertHTML('<br>'); 'new paragraph'→insertHTML('</p><p>').\n\nformatTranscript(raw): pure string transform (unit-testable). If state.autoPunct map \\b(period|full stop)\\b→'.', comma→',', question mark→'?', exclamation (point|mark)→'!', semicolon/colon/quotes; collapse the space before inserted punctuation; capitalize sentence starts and after .!?.\n\nstop(): active=false; if(rec){rec.onend=null; rec.stop(); rec=null;} hideBar(); markButton(false). toggle(): active?stop():start(). setLang(l): state.lang=l; if(active){stop();start();}.\n\nUI: showBar()/updateBar()/hideBar() build a fixed div via WC.el (class 'dictate-bar') appended to document.body — NOT a modal, so editor keeps focus — containing span.dot (pulsing), a language label, a .interim line, and a mic/stop button (onClick→WC.Dictate.toggle). updateBar(text) sets .interim; clear after each final commit. markButton(on) toggles 'toggled'/'recording' on WC.Ribbon.controlIndex['dictate'].node (the registry ribbon.js fills at renderControl line ~214).\n\nDROPDOWN: in /home/ogutd/msword-clone/src/renderer/js/commands.js Commands.dropdown(), add `if (cmd === 'dictate') return dictateMenu(node);`. dictateMenu(node) uses WC.flyout: WC.flyHeader('Spoken Language') + a WC.flyItem per locale (check glyph on active, onClick→WC.Dictate.setLang), WC.flySep, then 'Auto-punctuation' and 'Profanity filter' toggle rows reflecting boolean state, then 'About Dictation…'.\n\nWIRE-UP: in commands.js add H.dictate = () => WC.Dictate.toggle(); (split main-click routes Commands.run→H.dictate; arrow-click routes Commands.dropdown→dictateMenu). In /home/ogutd/msword-clone/src/renderer/js/ribbon-data.js control id home.voice.dictate change \"feasible\":\"no\" → \"partial\".\n\nCSS (/home/ogutd/msword-clone/src/renderer/styles/ribbon.css): .dictate-bar{position:fixed;bottom:80px;left:50%;transform:translateX(-50%);display:flex;align-items:center;gap:10px;background:#fff;border:1px solid #d0cece;box-shadow:0 4px 16px rgba(0,0,0,.18);border-radius:20px;padding:8px 14px;z-index:9000} .dictate-bar .dot{width:10px;height:10px;border-radius:50%;background:#e23b3b;animation:dictpulse 1.1s infinite} @keyframes dictpulse{0%,100%{opacity:1;transform:scale(1)}50%{opacity:.35;transform:scale(.7)}} .dictate-bar .interim{color:#666;font-style:italic;min-width:160px} .rsplit.recording .main .ic{color:#e23b3b}\n\nAlso register dictate.js in /home/ogutd/msword-clone/src/renderer/index.html script list (after commands.js, before/with app.js).",
        "testPlan": "Add cases to /home/ogutd/msword-clone/scripts/test-suite.js. Real mic audio cannot run headless, so dependency-inject a fake recognition via WC.Dictate._SR — a stub exposing start/stop and settable onresult/onerror/onend, plus a helper to fire synthetic events ({resultIndex, results:[{0:{transcript}, isFinal, length:1}]}).\n\n1) 'Dictate feature-detects gracefully' — set WC.Dictate._SR=null (and delete window.*SpeechRecognition); WC.Commands.run({cmd:'dictate'}) must not throw and active stays false (graceful fallback toast).\n2) 'Dictate inserts final transcript at caret' — E.setHTML('<p>start </p>'); caretIn(p); install fake SR; WC.Dictate.start(); fire onresult FINAL {transcript:'hello world'}; assert E.node.innerText includes 'hello world' appended after 'start' (caret respected via savedRange).\n3) 'Interim results do not mutate the document' — fire onresult isFinal:false {transcript:'temp text'}; assert E.node.innerText excludes 'temp text' but document.querySelector('.dictate-bar .interim').textContent includes it.\n4) 'Recording indicator appears/disappears' — after start() assert document.querySelector('.dictate-bar') exists and ribbon node has class 'recording'; after WC.Dictate.stop() assert both gone.\n5) 'Auto-punctuation transform' — unit-test formatTranscript: 'hello comma world period'→'Hello, world.'; 'foo new paragraph bar' yields a paragraph break.\n6) 'Spoken text is HTML-escaped' — fire final {transcript:'a <script> b'}; assert no <script> element and literal angle brackets present.\n7) 'Language selection updates state' — WC.Dictate.setLang('fr-FR'); assert WC.Dictate state lang==='fr-FR' and a started rec.lang reflects it.\nCleanup: WC.Dictate.stop(); remove .dictate-bar; restore SR globals. Follow harness conventions (await t(...), boolean/string returns, sleep for async onresult).",
        "feasibility": "partial",
        "apis": [
          "window.SpeechRecognition / webkitSpeechRecognition (Web Speech API, recognition side)",
          "SpeechRecognitionEvent (results, resultIndex, isFinal, transcript)",
          "navigator.mediaDevices.getUserMedia (implicit, gated by Electron permission handler)",
          "Electron session.setPermissionRequestHandler / setPermissionCheckHandler (main process, for 'media'/'audioCapture')",
          "WC.Editor.saveRange/restoreRange/savedRange + insertHTML (caret-preserving insertion)",
          "WC.escapeHtml, WC.flyout, WC.flyItem, WC.flyHeader, WC.flySep, WC.toast, WC.el, WC.Ribbon.controlIndex"
        ],
        "subOptions": [
          "Spoken Language submenu (en-US, en-GB, fr-FR, de-DE, es-ES, it-IT, pt-BR, ja-JP, zh-CN) with active locale checkmarked",
          "Auto-punctuation toggle (maps spoken 'comma'/'period'/'new line'/'new paragraph' to characters + capitalizes sentences)",
          "Profanity filter toggle (sets recognition behavior / masks output)",
          "Live interim transcript line in the floating bar",
          "Pulsing red recording indicator + ribbon button .recording state",
          "Stop/mic toggle on the floating bar"
        ]
      },
      {
        "feature": "Styles task pane + Apply Styles + Create a Style (Styles group dialog-box-launcher and gallery extras)",
        "wordBehavior": "In MS Word the Styles group has three style entry points that all draw from one shared style registry (built-in named styles plus user-created custom styles):\n\n1. Styles gallery (Home > Styles): a horizontal scrolling gallery of \"Quick Styles\" with live previews (each cell rendered in the style it applies). Hover shows a live-preview of the style on the current paragraph/selection; click applies it. Paragraph styles apply to the whole paragraph(s) the selection touches; character/linked styles apply to the selection run. Below the gallery scroll arrows is a \"More\" (down-chevron) button that drops the full gallery plus three commands at the bottom: \"Create a Style\", \"Clear Formatting\", and \"Apply Styles...\" (Ctrl+Shift+S).\n   - Create a Style: opens \"Create New Style from Formatting\" — a small dialog with a Name field and a live preview of the current selection's formatting; OK adds a new Quick Style built from the formatting at the caret. A \"Modify...\" button expands to the full style dialog (paragraph/character type, based-on, font, etc.).\n   - Clear Formatting: removes all character and paragraph formatting, resetting the selection to the Normal style (same as Home > Font > Clear All Formatting / Ctrl+Spacebar + Ctrl+Q).\n   - Apply Styles (Ctrl+Shift+S): a tiny floating \"Apply Styles\" box with a style-name combo (type or pick a style name), Reapply/Modify buttons. Applies the typed style.\n\n2. Styles dialog-box launcher (small arrow at the group's corner, Ctrl+Alt+Shift+S): opens the Styles task pane docked on the right. It lists EVERY available style (built-in: Normal, No Spacing, Heading 1-9, Title, Subtitle, Quote, Intense Quote, Strong, Emphasis, Subtle/Intense Emphasis, List Paragraph, etc.) plus any custom styles, each row rendered in its own style (paragraph styles show a paragraph-mark glyph, character styles an 'a' glyph, linked styles both). The currently-active style is highlighted. Clicking a row applies it. Each row has a hover dropdown (Update to Match Selection, Modify, Select All Instances, Rename, Delete). There is a \"Show Preview\" checkbox (renders rows in their styles), an \"Options...\" link (which styles to show: recommended / in use / in current document / all), and three buttons at the bottom: New Style (= Create a Style), Style Inspector, Manage Styles.\n\nAll three surfaces share state: a custom style created via \"Create a Style\" immediately appears in the gallery, the pane, and the Apply Styles box, and is highlighted when the caret sits in text using it.",
        "uiNeeded": "Three pieces, all reusing existing helpers:\n\nA) Styles task pane (right-docked, WC class `.taskpane.right`, replaces the current bare D.stylesPane):\n   - Header \"Styles\" with close X (existing `.tp-head`).\n   - \"Show Preview\" checkbox row (toggles `.preview` class on the list so rows render in-style).\n   - Scrollable list `.styles-list`; each `.sl-item` shows: a leading type glyph (¶ for paragraph style, a for character style, ¶a for linked), the style name (rendered in its own style when preview on), and an active highlight when it is the style at the caret. Each row carries a hover \"…\"/chevron that opens a WC.flyout with: Update {name} to Match Selection, Modify…, Select All N Instances, Rename… (custom only), Delete (custom only). Click on the row body applies the style.\n   - Footer: three small buttons \"New Style\" (opens Create-a-Style dialog), \"Style Inspector\" (toast: approximated), \"Manage Styles\" (toast or maps to Modify). Plus an \"Options…\" inline link that toggles which set is shown (all vs in-use) — minimal: a filter <select>.\n\nB) Create New Style from Formatting dialog (WC.dialog, width ~440px), reusing the `.row`/`label`/`input` form pattern from D.insertLink:\n   - Name: text input (default \"Style1\", auto-incremented to first free name).\n   - Style type: <select> Paragraph | Character | Linked.\n   - A live preview box rendered with the captured formatting + the words \"The quick brown fox…\" / actual selected text.\n   - Footer: OK (creates + applies), Modify… (toast: full dialog approximated / or no-op expander), Cancel.\n\nC) Apply Styles floating box (Ctrl+Shift+S) — small WC.dialog or compact flyout near the selection:\n   - \"Style Name:\" combo (text input + datalist of all style names) with Reapply and Modify… buttons, applies on Enter/Reapply.\n\nD) Gallery More-menu (in renderStylesGallery, the existing `all` button): currently calls stylesPane(); change it to a WC.flyout listing all style cells PLUS a separator and the three commands \"Create a Style\", \"Clear Formatting\", \"Apply Styles…\". The inline gallery should also include any custom styles.",
        "implementationPlan": "Central registry + three surfaces. All code is real (CSS spans, no execCommand needed for custom styles — they apply a class whose CSS we inject).\n\n1) STYLE REGISTRY (new, in formatting.js). Define a single source of truth:\n   - `WC.Styles.BUILTIN` = ordered array of {name, type:'paragraph'|'character'|'linked', css (preview string), apply(blocks/range)}. Reuse the exact behaviors already in WC.applyNamedStyle (Normal, No Spacing, Heading 1-4, Title, Subtitle, Quote, Intense Quote, Strong, Emphasis, Subtle/Intense Emphasis, List Paragraph) — refactor that switch into per-style entries so the pane, gallery, and Apply box all read the same list. Add Heading 5/6 for completeness.\n   - `WC.Styles.custom = []` for user styles: each {name, type, className (e.g. 'wcs-style1'), declarations:{prop:val,...}}.\n   - `WC.Styles.all()` returns BUILTIN names + custom names. `WC.Styles.find(name)`.\n   - A `<style id=\"wc-custom-styles\">` element appended to <head> on init; `WC.Styles.injectCSS()` rewrites it from `custom` (selectors `.wcs-xxx{...}` for character styles and `p.wcs-xxx,h1.wcs-xxx,...{...}` / generic `.wcs-xxx` for paragraph styles). This makes styles real and reusable, and survives getHTML/setHTML because the class names persist in the document (round-trip note below).\n\n2) CAPTURE CURRENT FORMATTING (new helper `WC.Styles.captureSelection()`):\n   - Reuse the existing pattern from commands.js `currentInlineStyle()` (getComputedStyle of the anchor node's element). Collect fontFamily, fontSize (convert px→pt via the /1.3333 factor already used in editor.queryState computedFontSizePt), fontWeight, fontStyle, textDecorationLine, color, and (for paragraph type) textAlign, lineHeight, margin, backgroundColor of the block from E().selectedBlocks()[0].\n   - Return a declarations object {} of only the non-default props (skip values equal to the Normal baseline: Calibri/11pt/400/normal/none/rgb(0,0,0)) so the custom class is minimal and meaningful.\n\n3) APPLY A STYLE (`WC.applyNamedStyle(name)` — extend existing):\n   - If name is a BUILTIN, run its existing apply() (the refactored switch). Keep the existing active-cell reflection line.\n   - If name is a custom style: paragraph/linked → E().selectedBlocks().forEach(b => { strip other wcs-* classes; b.classList.add(className) }); character → wrap selection in a span.className=className (use the same surroundContents/extractContents fallback already used by applyInlineStyle and Comments.add). Then E().dirty=true; E().repaginate(); E().updateStatus(); E().emit().\n   - After applying, refresh pane highlight + gallery active cell via a new `WC.Styles.refreshActive()` driven off E().onStateChange / queryState.block + caret class detection.\n\n4) STYLES TASK PANE (rewrite D.stylesPane in dialogs.js):\n   - Build from `WC.Styles.all()`. Each `.sl-item`: leading `.sl-glyph` (¶ / a / ¶a), `.sl-name` (text). When Show-Preview checkbox on, add class `preview` and set inline `style` from the style's css preview string (same map the gallery uses, plus custom declarations). Click body → WC.applyNamedStyle(name) and close-or-keep open (Word keeps it open).\n   - Per-row dropdown: a small chevron that opens WC.flyout(rowChevron, fly => WC.flyItem('Update {name} to Match Selection',…), 'Modify…' → openCreateStyleDialog(prefillName), 'Select All N Instances' → select all nodes carrying that style/tag via a Range over document, 'Rename…'/'Delete' for custom → mutate registry + injectCSS + reflow document classnames + re-render pane).\n   - \"Update to Match Selection\": capture current formatting into the existing style's declarations (for custom) or toast for built-ins (Word allows it; minimal = custom only), injectCSS, repaginate.\n   - Footer buttons + Options select as described. Re-render the list after any registry mutation via a `renderList()` closure.\n\n5) CREATE-A-STYLE DIALOG (new D.createStyle in dialogs.js):\n   - On open: const decl = WC.Styles.captureSelection(); name input default = WC.Styles.nextName() (Style1, Style2…). Type <select>. Live preview <div> styled from decl + selected text (or sample sentence).\n   - OK: validate non-empty unique name (toast + keep open if dup, mirroring insertTable's `return false` keep-open pattern). Create className = 'wcs-' + slug(name) + uniquifier; push {name,type,className,declarations:decl} to WC.Styles.custom; WC.Styles.injectCSS(); WC.applyNamedStyle(name) to apply to current selection; refresh gallery (re-render the styles gallery cell list — expose `WC.Ribbon.refreshStylesGallery()` that rebuilds the grid, or append a cell directly); WC.toast('Style \"{name}\" created.'); close.\n   - Modify…: optional — toast 'Full Modify Style dialog is approximated' (document-only) OR reuse the same dialog with extra font/size/bold/italic/color controls (feasible, see subOptions).\n\n6) CLEAR FORMATTING command (wire the gallery/More entry and a dedicated handler):\n   - Reuse the already-working `H.clearAllFormatting` in commands.js (does removeFormat + strip block style + formatBlock('p')). Add the missing step: also strip custom wcs-* and doc-title/doc-subtitle classes from selectedBlocks so it truly returns to Normal. Map gallery \"Clear Formatting\" entry and pane usage to it.\n\n7) GALLERY WIRING (ribbon.js renderStylesGallery):\n   - Include custom styles in the rendered cells (append after built-ins; preview from declarations).\n   - Change the More (`all`) button from `WC.Dialogs.stylesPane()` to a WC.flyout containing all style cells + WC.flySep() + flyItems 'Create a Style' (→ D.createStyle), 'Clear Formatting' (→ H.clearAllFormatting), 'Apply Styles…' (→ D.applyStyles). Keep a separate path so the dialog-box launcher (already wired: commands.js launcher map `styles: () => WC.Dialogs.stylesPane()`) opens the full pane.\n   - Add `WC.Ribbon.refreshStylesGallery()` so Create-a-Style updates the inline gallery without a full ribbon rebuild.\n\n8) APPLY STYLES box (new D.applyStyles, Ctrl+Shift+S):\n   - Compact WC.dialog: text input + <datalist> of WC.Styles.all(), Reapply/Modify buttons. Enter or Reapply → WC.applyNamedStyle(input.value) if it exists else toast. Wire Ctrl+Shift+S in app.js keymap (next to the existing Ctrl+Shift+N for Normal).\n\n9) ROUND-TRIP / PERSISTENCE: custom-style class names live in the document HTML (E().getHTML keeps them). To make a saved doc reopen with its styles, persist `WC.Styles.custom` alongside the HTML — extend the save payload in files.js (the .docx/.json wrapper) with a `styles` array, and on load call WC.Styles.load(arr)+injectCSS(). If file format can't carry it this pass, document that custom styles persist within a session and embed an inline `<style>` block at the top of getHTML output as a fallback so a re-opened .html keeps them (feasible, low risk).\n\n10) CSS (ribbon.css / dialogs.css): add `.styles-list.preview .sl-item .sl-name` (no override of inline style), `.sl-glyph` styling, `.sl-item.active` highlight (reuse --word-blue-tint), `.sl-item .sl-menu` chevron (show on hover), `.style-create-preview` box, `.apply-styles-box` compact layout. Reuse existing `.style-cell` / `.styles-grid` for gallery custom cells.",
        "testPlan": "Add cases to the in-renderer test battery (the existing WC test harness that drives Editor + Commands directly):\n\n1) Registry: assert WC.Styles.all() contains all built-ins (Normal…List Paragraph) and is a superset of the old gallery items. After WC.Styles.custom push + injectCSS, document.getElementById('wc-custom-styles').textContent includes the new selector.\n\n2) Apply built-in: set editor HTML to '<p>abc</p>', place caret in it, WC.applyNamedStyle('Heading 1'); assert E().node.querySelector('h1') exists and queryState().block==='h1'. WC.applyNamedStyle('Normal'); assert it's a <p> with no style/class.\n\n3) Capture: select a run, applyInlineStyle bold + color, then const d = WC.Styles.captureSelection(); assert d.fontWeight==='bold'/'700' and d.color is the set color, and that default props (Calibri/11pt) are omitted.\n\n4) Create-a-Style end to end: select text with known formatting, open D.createStyle (or call the create function directly with a name), OK; assert (a) WC.Styles.custom has the entry, (b) the injected <style> has its rule, (c) the selected block/run now carries the className, (d) WC.Styles.all() now includes the name, (e) the gallery grid (.styles-grid) gained a cell with dataset.style===name after refreshStylesGallery, (f) the pane list (after open) shows the row.\n\n5) Duplicate-name guard: attempt to create a style with an existing name → create returns false / keeps dialog, custom array length unchanged.\n\n6) Clear Formatting: build '<h1 class=\"wcs-x\" style=\"color:red\">hi</h1>', select, run H.clearAllFormatting; assert block is now <p>, no style attr, no wcs-* class, computed not red.\n\n7) Pane apply + active highlight: open D.stylesPane(), click the 'Heading 2' .sl-item (dispatch click), assert block became h2 and the matching .sl-item has class 'active' after refreshActive().\n\n8) Apply Styles box: open D.applyStyles, set input to 'Title', trigger Reapply/Enter; assert the block became the Title style (h1.doc-title). Set input to a bogus name; assert no change + toast path taken.\n\n9) Round-trip: create a custom style, applied to a block; html = E().getHTML(); E().setHTML(html); after WC.Styles loaded, assert the block still renders with the custom declarations (getComputedStyle matches) — verifies class+CSS persistence.\n\n10) Idempotence/no-throw: run create→apply→clear→apply sequences on a collapsed selection and on a multi-block selection; assert no exception and document remains well-formed (single root paragraph invariant from editor.init holds).",
        "feasibility": "full",
        "apis": [
          "document.execCommand('removeFormat'/'formatBlock') via WC.Editor.exec",
          "window.getSelection / Range surroundContents/extractContents/insertNode",
          "getComputedStyle for formatting capture",
          "Element.classList for paragraph styles + injected <style> element for custom CSS",
          "WC.dialog (modal) for Create-a-Style and Apply Styles",
          "WC.flyout/WC.flyItem/WC.flySep for the gallery More-menu and per-row style menu",
          "WC.toast for approximated actions",
          "HTML5 <datalist> for the Apply Styles combo"
        ],
        "subOptions": [
          "Create a Style (name + type Paragraph/Character/Linked, live preview, captures current selection formatting into a reusable class)",
          "Modify... expander on Create-a-Style (font/size/bold/italic/color controls) — optional richer path",
          "Clear Formatting (reset selection to Normal, strip custom + doc-title/subtitle classes)",
          "Apply Styles box (Ctrl+Shift+S) with name combo + Reapply/Modify",
          "Styles pane: Show Preview toggle, Options filter (all / in use), per-row menu: Update to Match Selection, Modify, Select All Instances, Rename, Delete",
          "Pane footer: New Style, Style Inspector (approx), Manage Styles (approx)",
          "Gallery includes custom styles + More-menu with the three commands",
          "Active-style highlight synced to caret via Editor.onStateChange"
        ]
      },
      {
        "feature": "Home-tab \"extra\" groups: Sensitivity (label dropdown), Editor (proofing/Editor pane), Add-ins (Get/My Add-ins), Reuse Files. All four are declared in ribbon-data.js (cmds: sensitivity, editor, addIns, reuseFiles) with feasible:\"no\" and currently fall through to WC.notImplemented because none have a handler in commands.js H{}.",
        "wordBehavior": "SENSITIVITY: A split/dropdown showing org-defined Microsoft Purview labels (Public, General, Confidential, Highly Confidential, and sub-labels). Picking one stamps a label on the document that persists in the .docx, shows in the title bar / a sensitivity bar, can apply encryption + content marking (header/footer/watermark) and may enforce policy. The available labels come entirely from a tenant/Purview policy — there is no built-in fixed list. EDITOR: F7 opens the right-docked Editor task pane. M365 shows an Editor \"score\", correction counts grouped by category (Spelling, Grammar, Refinements: Clarity/Conciseness/Formality/etc.), and lets you step through each issue with suggestion cards (accept/ignore). Classic Word 2021 instead opens the Spelling & Grammar dialog. Underlying engine is a cloud/local proofing service. ADD-INS: Dropdown with \"Get Add-ins\" (opens the Office Store / AppSource dialog to browse and install web add-ins) and \"My Add-ins\" (lists installed add-ins, Recently Used; each add-in is a sandboxed web app using Office.js that can read/modify the document and show task panes/content). REUSE FILES: A right task pane (M365 + connected account only) that searches your OneDrive/SharePoint files and lets you insert content/sections from them into the current document. Pure cloud feature.",
        "subOptions": [
          "Sensitivity: label list Public / General / Confidential / Highly Confidential, plus a 'Remove label' entry and a footer note that labels are organizational (Purview)",
          "Editor: top summary (Editor score + total issue count), category rows (Spelling, Grammar, Clarity/Refinements), a 'Check spelling as you type' toggle bound to the contenteditable spellcheck attribute, and per-misspelling cards with native suggestions",
          "Add-ins: Get Add-ins (opens explanatory dialog), My Add-ins (empty-state list), Recently Used Add-ins",
          "Reuse Files: single button/pane showing a cloud-only explanation and a disabled search box"
        ],
        "apis": [
          "document.execCommand (existing WC.Editor.exec)",
          "Element.spellcheck / setAttribute('spellcheck')",
          "window.getSelection / Range / TreeWalker for word extraction",
          "Intl/RegExp word tokenization",
          "(optional) Electron webFrame.getWordSuggestions / contextmenu spellcheck suggestions — falls back to a small embedded common-misspelling map if unavailable",
          "WC.dialog, WC.flyout, WC.flyItem, WC.toast, WC.colorPalette (not needed), WC.icon"
        ],
        "feasibility": "partial",
        "uiNeeded": "FOUR distinct surfaces, all built from existing helpers:\n\n1) SENSITIVITY — a WC.flyout dropdown anchored on the ribbon button (cmd 'sensitivity'). Contents (WC.flyItem each): a header \"Sensitivity\"; the four labels Public / General / Confidential / Highly Confidential, each with a small colored swatch (green/blue/orange/red) and a check on the currently applied one; a WC.flySep; \"Remove label\"; then a non-clickable footer note \"Labels are defined by your organization (Microsoft Purview).\" Selecting a label persists it and renders a thin sensitivity bar under the ribbon plus a status-bar segment.\n\n2) EDITOR — a right-docked task pane (div.taskpane.right#editor-pane, mirroring D.stylesPane/D.navPane). Header \"Editor\" + close X. Body: (a) a summary card showing a computed \"score\" (100 minus weighted issues) and total issue count; (b) category rows \"Spelling\", \"Grammar\", \"Refinements (Clarity & Conciseness)\" each with a count badge — Spelling is real, Grammar/Refinements show \"Not available in this clone\"; (c) a labeled checkbox \"Check spelling as you type\" wired to the editor's spellcheck attribute; (d) a scrollable list of misspelled-word cards (the flagged word, up to 3 suggestions as buttons, and \"Ignore\"). Clicking a suggestion replaces that occurrence; the pane re-scans.\n\n3) ADD-INS — a WC.flyout with WC.flyItem \"Get Add-ins\" and \"My Add-ins\". \"Get Add-ins\" opens a WC.dialog explaining Office Add-ins are sandboxed Office.js web apps requiring AppSource/account, with an OK button. \"My Add-ins\" opens a WC.dialog with an empty-state list (\"No add-ins installed\") and a \"Recently Used Add-ins\" empty section. Honest placeholder — no runtime.\n\n4) REUSE FILES — clicking the button opens a WC.dialog (or small right pane) titled \"Reuse Files\" with a disabled search input and copy: \"Reuse Files finds and inserts content from your OneDrive/SharePoint documents. It requires a Microsoft 365 work or school account and cloud storage, which this offline clone does not provide.\" Single Close button.",
        "implementationPlan": "All handlers go in commands.js H{} (they currently fall through to WC.notImplemented). Pane builders go in dialogs.js (D.*). No new globals required.\n\nSENSITIVITY (real, persistent):\n- Add H.sensitivity = (c,node) => sensitivityMenu(node). sensitivityMenu builds a WC.flyout with the four labels (color map: Public #107C10, General #0078D4, Confidential #D83B01, Highly Confidential #A4262C), \"Remove label\", and the org-note footer.\n- Store state on WC.Files.sensitivity (string|null). On pick: set it, call WC.Files.updateTitle() augmentation, render bar.\n- renderSensitivityBar(): create/update <div id=\"sensitivity-bar\"> inserted right under the ribbon container (or as the top child of #workarea) with the label text + swatch; remove it when label is null. Add a status-bar segment in statusbar.js init (a span updated via a tiny StatusBar.setSensitivity(label)).\n- PERSISTENCE: the label must round-trip through save/open. Cleanest in this codebase: have WC.Editor.getHTML() prepend (and setHTML() read+strip) a marker comment/meta, OR store as a data attribute. Concrete approach: in Files.save/saveAs payload is just html, so embed an invisible marker element at the very top of #editor: <p class=\"wc-sensitivity\" data-label=\"Confidential\" hidden></p>. Editor.getHTML already clones; keep the marker in output. On setHTML, after assignment scan for .wc-sensitivity, lift data-label into WC.Files.sensitivity, call renderSensitivityBar(), and remove the node from the visible DOM is optional (hidden keeps it invisible). This makes the tag survive .docx/.html save and reopen without touching main.js.\n- Add a CSS rule for #sensitivity-bar in dialogs.css or base.css (thin 22px bar, left swatch, label, muted note).\n\nEDITOR PANE (real spellcheck summary + toggle):\n- Add H.editor = () => WC.Dialogs.editorPane(); also map the existing F7 shortcut / Review 'editor' cmd to the same so both entry points share it. Reuse the existing runSpellCheck logic conceptually but upgrade to a pane.\n- D.editorPane(): toggle pattern identical to D.stylesPane — if #editor-pane exists, remove() and return; else build div.taskpane.right#editor-pane with tp-head/tp-body.\n- Spell scan: walk E().node text nodes with TreeWalker (skip .pagebreak-guide/.find-hit/.wc-sensitivity), tokenize words via /[A-Za-z']+/g. For each unique token, get suggestions: try Electron spellcheck if exposed (window.wordAPI.spellSuggest(word) added to preload calling webFrame; if not present) fall back to a flagging heuristic: a built-in Set of ~400 common English words is too small, so instead rely on the OS spellchecker that already underlines via spellcheck=true and flag words by checking against a bundled common-misspellings map (e.g. {teh:'the', recieve:'receive', ...}) for the suggestion cards. Honest scope: the card list is driven by the misspellings map + any words the app can confirm; the pane header states \"Spelling check uses the built-in dictionary; grammar & refinements are not available.\"\n- Score: score = Math.max(0, 100 - issues*5); issues = spelling matches found.\n- Toggle: a checkbox bound to E().node spellcheck attribute (reuse toggle from runSpellCheck); checked reflects current attribute.\n- Fix action: clicking a suggestion runs a scoped find-and-replace for that exact word (reuse the TreeWalker replace approach from D.findPane's doReplace, replacing the specific text node occurrence), then re-runs the scan; \"Ignore\" pushes the word to an ignore Set for the session.\n\nADD-INS (document-only placeholder, honest):\n- H.addIns = (c,node) => addInsMenu(node). WC.flyout with WC.flyItem('Get Add-ins', {onClick: getAddInsDialog}) and WC.flyItem('My Add-ins', {onClick: myAddInsDialog}).\n- getAddInsDialog: WC.dialog explaining add-ins are Office.js sandboxed web apps needing AppSource + account; footer [Close].\n- myAddInsDialog: WC.dialog with empty list + \"Recently Used Add-ins (none)\"; footer [Close].\n\nREUSE FILES (document-only placeholder, honest):\n- H.reuseFiles = () => WC.Dialogs.reuseFiles(). D.reuseFiles: WC.dialog (width 420px) with a disabled tp-search input and the cloud-only explanation, footer [Close]. Optionally render as a right pane for fidelity, but a dialog is simpler and clearly communicates it is non-functional offline.\n\nWiring note: because dispatch in Commands.run already routes any cmd with an H[cmd] handler, simply defining these four H.* entries removes them from the notImplemented fallthrough. The dropdown-type ones (sensitivity, addIns) are handled because their H.* opens a custom flyout (same pattern as H.font/H.select).",
        "testPlan": "Use the in-renderer test battery (drive WC.* directly on the live #editor):\n\nSENSITIVITY:\n- Call WC.Commands.run({cmd:'sensitivity',label:'Sensitivity'}, ribbonNode); click the 'Confidential' fly-item; assert WC.Files.sensitivity === 'Confidential', that document.getElementById('sensitivity-bar') exists and its text contains 'Confidential', and the status-bar segment updated.\n- Round-trip: set a label, read html = WC.Editor.getHTML(); assert it contains data-label=\"Confidential\"; call WC.Editor.setHTML(html); assert WC.Files.sensitivity reparsed to 'Confidential' and the bar re-rendered.\n- 'Remove label' sets WC.Files.sensitivity to null and removes #sensitivity-bar.\n\nEDITOR PANE:\n- Seed E().setHTML('<p>teh recieve cat</p>'); call WC.Dialogs.editorPane(); assert #editor-pane exists, the summary count >= 2, and at least one card shows 'teh' with a 'the' suggestion button.\n- Click the 'the' suggestion; assert editor text now contains 'the' (not 'teh') and the pane re-scanned (count decreased by 1).\n- Toggle the \"Check spelling as you type\" checkbox; assert E().node.getAttribute('spellcheck') flips between 'true'/'false'.\n- Call editorPane() again; assert the pane toggles closed (removed from DOM).\n- 'Ignore' on a card removes it from the list and it does not reappear on re-scan within the session.\n\nADD-INS:\n- Run cmd addIns; click 'Get Add-ins' and assert a .dialog with the explanatory text appears and closes on Close. Repeat for 'My Add-ins' asserting the empty-state text.\n\nREUSE FILES:\n- Run cmd reuseFiles; assert a .dialog titled 'Reuse Files' opens with a disabled input and the cloud-only message, and closes on Close.\n\nREGRESSION: assert none of the four cmds now trigger WC.notImplemented (spy/replace WC.notImplemented to throw and confirm it is not called for these cmds)."
      }
    ]
  }
}